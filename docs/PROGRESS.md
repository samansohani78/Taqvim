# Taqvim — Progress Tracker

Source of truth: [`docs/PLAN.md`](PLAN.md). Deviations: [`docs/adr/`](adr/).
Status legend: `TODO` · `WIP` · `DONE` · `BLOCKED` — the implementation state only. "Open validation or data" lists what still
needs a device, a published source, human translation or the owner, so DONE rows can still carry open validation (code review I10).

| Task | Title | Status | Open validation or data | Branch/Commit | Tests added | Notes |
|---|---|---|---|---|---|---|
| T-000 | Repository bootstrap | DONE | — | main@f34ecdb | R: MainActivityTest · U: AppModuleTest (Koin graph verify), TaqvimIssueRegistryTest · L: ProjectStructureKonsistTest; spotless/detekt/lint/Kover gates green | ADR-0001–0004. Tests & detekt on JDK 21 toolchain; build resource budget after local OOM crashes (ADR-0004 §9) |
| T-001 | License gate | DONE | — | main@d6c7ac3 | U: AllowListParserTest, SpdxNormalizerTest, PomLicensesTest (XXE, parent inheritance, cache lookup), LicensePolicyTest, ClassificationAndCodecTest · Negative: LGPL canary (`-Ptaqvim.licenseGate.canary=true`) rejected | 427 modules, 0 violations. ADR-0005: jakarta.annotation-api substitution, trove4j excluded, EPL-1.0 test-only exception confirmed by owner 2026-09-13; kxml2 MIT override. Google Maven unreachable from dev network → POMs read cache-first |
| T-002 | Architecture rules (Konsist) | DONE | — | main@3ea66ba | Konsist: ArchitectureKonsistTest (10 rules on real code), ArchitectureRulesTest (17 unit), KonsistFixturesTest (8 end-to-end, planted violations) | package↔module, layering (imports + build scripts), Android-free pure core, files ≤400 / functions ≤50 lines, ViewModel/UiState contract, no global mutable state. `!!`/`try`/`as` → detekt + T-004 lint; complexity ≤12 → detekt. Rule 10 (main@0b13c78): expression-bodied `@Test` must declare `: Unit` |
| T-003 | CI workflows | DONE | local | main@8587d6b, main@42f7228, main@9a152e5, main@ce2bf17 | L: actionlint (4 workflows + composite action) · local: CycloneDX SBOM (192 runtime components, no test deps), signed release APK verified by apksigner (v2), full gate green | pr.yml (static+SARIF, unit+Kover, Roborazzi verify, license gate + LGPL canary negative test, dataset, assemble, actionlint), instrumented.yml (API 26/30/33/36, KVM), benchmark.yml (nightly), release.yml (signed AAB/APK, git-cliff changelog, SBOM). **"Green on main" pending first push to GitHub**. CI fixes after the first pushes (main@874506f KVM step, main@42f7228 SARIF upload and Python tool tests, main@d8e96ab baseline-profile job, main@2c8940d CI timing budgets). `./gradlew :app:pixel6Api34DebugAndroidTest` runs 12 instrumented tests on the Gradle Managed Device pixel6Api34 (API 34 aosp-atd) in ~8 min (main@9a152e5). Instrumented workflow green on API 30/33/36 after pinning the `pixel_6` emulator profile (main@ce2bf17; the default AVD was too narrow for 48dp month cells, so the accessibility check reported 25 touch-target errors) and keeping the emulator logcat with the benchmark reports (main@0be863a, main@68b2276) |
| T-004 | Custom lint module | DONE | — | main@c3fc147 | L: SourceRuleDetectorsTest (6), StateTextAndBackDetectorsTest (8), TaqvimIssueRegistryTest (3) — LintDetectorTest positive/negative per rule; Konsist: string-literal unit test | NoDoubleBang, NoTryCatch, UseRunCatching (quick fix), NoUnsafeCast, NoHardcodedNonLatinText, HardcodedComposeText (added: quality bar forbids hard-coded UI text), NoGlobalMutableState, PreferPredictiveBack (Kotlin + manifest); active in every module, full gate green |
| T-005 | Test infrastructure (`:core:testing`) | DONE | — | main@e10a0a2 | U: GoldenFilesTest, SnapshotAndTimeFakesTest, ScreenshotMatrixTest · R: ScreenshotEnvironmentRobolectricTest · S: ui_testing_self_test (1 reference) · Konsist: FixtureProvenanceKonsistTest | Golden fixtures with provenance header + repo audit, snapshots, FakeClock/FakeTimeZone, 54-environment screenshot matrix (phone/tablet/fold at mdpi), `:core:ui-testing` (ADR-0004 §2). Composables excluded from Kover (ADR-0004 §10) |
| T-100 | Value types | DONE | — | main@e94b47d | U: JdnTest (8), CalendarTypesTest (7) · P: jdn+n−n==jdn, weekday(jdn+7)==weekday(jdn), weekday+7, MinuteOfDay wrap | Overflow-checked Jdn + JdnRange, CalendarDate, ISO Weekday, validated Coordinates, integer MinuteOfDay; explicit API + KDoc 100%. **Correction (main@0b13c78):** its property tests were silently skipped by JUnit (expression-bodied `@Test` returning a value) until that fix; they now run and a Konsist rule prevents recurrence |
| T-101 | Gregorian ↔ JDN (A-01) | DONE | — | main@c266f45 | G: GregorianIcuOracleTest (1 000 000 random JDNs in [−1e6, 5e6] vs ICU4J proleptic) · U/P: GregorianCalendarSystemTest (1582-10-15, year 0, negative years, round-trip, successor) · U: PropertyTestingTest | Fliegel & Van Flandern + 400-year cycle shifting (exact for all Int years); CalendarArithmetic contract. **Correction (main@0b13c78):** its property tests were silently skipped by JUnit (expression-bodied `@Test` returning a value) until that fix; they now run and a Konsist rule prevents recurrence |
| T-102 | Persian calendar (A-02) | DONE | data partial | main@9b1b7bf, main@dfe6f17, main@c6f13e6 | G: PersianCalendarOfficialTest — official leap years 1206–1498 (293), every day of the official 1404/1405 calendars (730, incl. weekday), official Nowruz instants 1404/1405 → 1 Farvardin, Calendar Center century note · G: PersianCalendarAstronomyTest — table recomputed from equinoxes for SH −3000…3000, official table dates = equinox dates in Iran time, equinoxes within 1 min of official instants · G: PersianIcuOracleTest — ICU4J first day of every year 1200–1500 + 100 000 random days, no exceptions · U/P: PersianCalendarSystemTest (month lengths, invalid dates, boundaries, seam continuity, year-length and round-trip properties, 100 000 random days over JDN −1e6…5e6, B: 10⁶ conversions < 300 ms), BirashkArithmeticTest (4) · G/P (main@dfe6f17): PersianCalendarAstronomyTest — runtime rule equals the former table for all 6 001 years and the library season search; PersianYearStartsTest (4) — exact joins at ±3000, far years to ±100 000 and the Int limits vs a BigInteger reference, 365/366-day years and round trips, Int-limit acceptance and rejection beyond | ADR-0026 (main@dfe6f17): year starts computed at run time from the A-02 equinox rule for every Int year — true equinox for SH −3000…3000, mean equinox beyond; no table, no Birashk fallback; 1M conversions 24–63 ms (budget 300 ms); :core:calendar depends on cosinekitty (Wear APK size to re-check). Sources: owner-supplied Calendar Center PDFs (docs/sources, main@d048e48). Official data covers 1206–1498 and the 1404/1405 instants; 1499–1500 and Nowruz instants 1390–1403/1406–1420 still pending (DT-001). Official data is a golden oracle only (main@c6f13e6, ADR-0026 addendum); `PersianComputedOnlyKonsistTest` guards against a runtime override |
| T-103 | Islamic tabular (A-03) & UAQ (A-04) | DONE | — | main@97c9da4, main@2fed071, main@ef282be, main@9c33702 | G: TabularIslamicCalendarTest — type II vs ICU4J civil on 100 000 random days; UmmAlQuraCalendarTest — every month AH 1300–1600 and 100 000 random days (AH 1200–1700) vs ICU4J, table boundaries · P: round-trip for both tabular variants and UAQ across/beyond the table · U: epoch, leap cycles, month alternation, 10 631-day cycle, type I/II one-day difference, invalid input (15 tests) · G: UsnoIslamicObservancesTest (4) — every USNO Islamic New Year, 1 Ramadan and 1 Shawwal 622–9999 (28 996 records, AH 1–9666, Julian dates before 1582-10-15 converted) matches type II both ways; type I one day later exactly in cycle year 16 · G/P (main@ef282be): UmmAlQuraCalendarTest (6) — published months AH 1300–1450 and 100 000 random days vs ICU4J, joins to computed years; UmmAlQuraCriterionTest (7) — per-era agreement (1423–1450 334/336, 1420–1422 36/36), month-by-month rule over 1451–1650 and 1200–1300, mean-lunation edges, years to ±100 000 and the Int limits | ADR-0006: UAQ month masks embedded (obtained via ICU4J public API, Unicode-3.0 notice in `licenses/`); ICU4J remains test-only; ADR-0028 (main@ef282be): published calendar AH 1300–1450 as data, the post-1423 criterion (conjunction before sunset and moonset after sunset at the Kaʿba) for every other year AH −3000…3000, mean lunations beyond; no tabular fallback. After 1450 about a third of months are one day earlier than ICU/Android. Computed from AH 1420 at run time (main@9c33702, ADR-0028 addendum); only AH 1300–1419 bundled; the app differs from the printed calendar in two marginal months |
| T-104 | Islamic Iran official (A-05) + observational (A-06) | DONE | data partial | main@28a2d16, main@ac90cf8, main@842b720, main@27950c5, main@33d0418, main@7493b47 | G: IranIslamicCalendarTest — every lunar date of the official 1404/1405 calendars (730), 26 published month starts, table edges, 29/30-day months 1440–1455, 100 000 round-trips · U: HijriDateResolverTest — official > offset > estimate, 30-day expiry (FakeClock), ±2 bound · G: YallopTest — NAO TN 69 Table 4 (295 observations: q, classes, crescent width), real evenings around a new moon · G: ObservationalMonthStartsTest — Iran calibration 23/25 official month starts (≥ 90 %), Tehran single site never early, Makkah within ±1 day of Umm al-Qura (≥ 95 %) · G/P (main@842b720): IranCrescentCalendarTest (7) — 23/25 official starts, 29-if-seen/30 rule for AH 1440–1450, 29/30-day months, exact mean-month continuation vs BigInteger at the edges and ±2×10⁹ months, years to ±1 000 000 and the Int limits, timing (first use 43 ms, 1M cached conversions 41 ms); IranIslamicCalendarTest official days exact with crescent joins and shifted-table joins; EventVisibilityPolicyTest observational variant | ADR-0009 + addendum: official months, user offset, and since ADR-0027 (main@842b720) crescent-computed months for every Int year (A-06 calibration at run time, no tabular estimate); `IslamicVariant.CALCULATED_OBSERVATIONAL` uses `IranCrescentCalendar`; calibration fitted to the 25 published months — re-validate with more official years (DT-002). Computed by default (main@27950c5, ADR-0037); the official months are an optional override (bundled file off by default, import, remove) and every Hijri date is labelled computed / official / printed. Refit pipeline 2026-09-18 (ADR-0040): `tools/iran/official_calendar_import.py` imports any `Calendar-<year>.pdf` from `docs/sources` into the daily fixtures, the month starts, the index the tests iterate and `dataset/iran/islamic-iran-overrides.json`; `IslamicCalibrationReportTest` scores Yallop A–D, Odeh A–C and three site sets per region and writes `docs/data-todo/islamic-calibration-report.md` — Iran 23/25 (92.0 %), Saudi Arabia 370/372 (99.5 %), Afghanistan 5/5 (100 %); shipped defaults unchanged; `tools/iran/test_official_calendar_import.py` (11: day-by-day and month-change rules, Persian month arithmetic on the official leap table, lam-alef name keys, fixtures up to date with the stored PDFs) Official calendars 1381–1405 imported (main@33d0418): 25 editions in `docs/sources/iran/` (MANIFEST with SHA-256, kind and layout), importer `tools/sources/iran` for the three readable layouts (2002, 2008, 2025), 21 years / 7 670 days as `golden/persian/official/<year>.csv`, **0 Persian mismatches** (date, weekday, Gregorian) in `PersianCalendarOfficialTest`; Nowruz instants 1360–1403 from `tahvil-sal.txt` within 42.8 s of the computed equinox; 262 official Hijri month starts. 1395, 1396, 1401, 1402 rejected: their text layer maps several digits to one glyph (needs OCR). Calibration on the full history (main@7493b47, ADR-0041): fit 1381–1400 (218 decisions), held out 1403–1405 (37) — shipped Yallop ≤ D 90.8 % / 91.9 % / 91.2 % chained over 262 starts; Odeh ≤ B 89.0 % / 83.8 %; a logistic model on age, lag, ARCV, ARCL, W 92.2 % / 97.3 % (not adopted: 37 held-out decisions, and the Calendar Center's practice changed around AH 1428). Floors in `IslamicCalibrationReportTest`: 0.90 fit, 0.89 held out, 0.90 chained |
| T-105 | Nepali (A-07) | DONE | — | main@d47a10a, main@1a08566, main@58994dc | G: NepaliCalendarSystemTest (10) — official National Panchang 2082/2083 month starts and sankranti times ±1 min, night rules, Kathmandu sunrise/sunset ±3 min, cached years, far years to ±100 000 and the Int limits (29–32-day months, 365/366-day years, round trip), cache-edge joins, invalid input, timing; NtaNepaliMonthStartsTest (2) — 84 of 89 NTA month starts, the 5 others listed with reasons · U/S (main@1a08566, main@58994dc): BikramSambatNamesTest (2), DateFormatterTest in ne and Latin languages, BS dates on every wired screen, Wear shows and converts BS dates, 12 Nepali month screenshots now in BS and two new year views (ne dark, en with Latin names) | ADR-0030: computed from Surya Siddhanta sankrantis with the Nepal-time day rule inferred from official data; no month-length table. Shown in every screen (main@1a08566, main@58994dc): official Nepali names and era in ne, the government's English spellings elsewhere; one calendar-to-arithmetic lookup (`IslamicCalendarSelection.arithmeticFor`) replaces five copies; BS numbers are parsed only for users of that calendar. Feature modules lack `values-ne` strings (T-1702) |
| T-106 | Calendar utilities | DONE | — | main@5cbcf02, main@44f4346, main@bf69a9f | U: CalendarMathTest — 135 rows over Gregorian, tabular Islamic (incl. Dhul-Hijjah 29/30) and Umm al-Qura; PersianCalendarMathTest — 109 rows (29/30 Esfand from the official leap table, nth/last weekday and Saturday-start weeks on the official 1404/1405 calendars, clamping, seasons) · P: `addMonths(k)` then `−k` returns the month start (Gregorian, Islamic, Persian); y/m/d period components rebuild the target date · U/P (main@bf69a9f): NepaliCalendarMathTest (150 rows from the official National Panchang 2082/2083 golden, incl. the 32-day Asar 2082); addMonths(k)/−k property for Nepali and Hebrew 13-month years | Calendar-agnostic over `CalendarArithmetic`; 244 table rows (≥ 200 met). Acceptance met (main@bf69a9f): 394 table rows incl. Nepali 32-day months; `periodBetween` now counts a whole year as the months of the year it starts in, fixing Hebrew leap years |
| T-107 | `kotlinx.datetime` bridge | DONE | — | main@ba40f51 | U: DateTimeBridgeTest — LocalDate↔Jdn, out-of-range rejection, Asia/Tehran day boundaries before/after DST abolition (2022), Asia/Kabul fixed offset, Europe/Berlin & America/Los_Angeles DST, TodayProvider follows clock and zone · P: agrees with A-01 and round-trips; a zone shifts the civil day by ≤ 1 (8 tests) | `api(kotlinx-datetime 0.8.0)`; uses `kotlin.time.Clock`/`Instant` |
| T-108 | Hebrew calendar (A-15, ADR-0025) | DONE | — | main@b20c5c6, main@381f6c0, main@d15b2a4, main@3170881, main@76c6ddd | G: UsnoJewishObservancesTest (5) — all 57 839 USNO Jewish observances 360–9999 exact (Julian before 1582-10-15), year lengths from consecutive new years · P/U: HebrewCalendarTest (9) — JDN round trip up to ±1 000 000 and at the numeric limits, six year lengths, 19-year leap pattern, Rosh Hashanah never Sun/Wed/Fri, 689 472-year cycle · U (main@d15b2a4): HebrewCalendarSystemTest (3), Hebrew formatting in en/de/hi, HebrewMonthNamesTest (5) incl. the leap-cycle lookup, calendar sets in CalendarTypesTest/FormatTableTest/SettingsCatalogTest · U/S (main@3170881): CalendarMonthNamesTest (3), Hebrew cases in month grid, year view, go-to-date and countdown pickers, a picker test that month 13 becomes 12 in a common year, screenshots calendar_month_enhe_06 and year_enhe_light | `HebrewCalendar` (Tishri-based month numbers, `HebrewDate`), `JewishObservances` (six USNO observances; a Gregorian year can hold 0–2 Hanukkahs). Long arithmetic, no year tables. Selectable since F07 (main@d15b2a4): `CalendarSystem.HEBREW` (preference value 5), `HebrewCalendarSystem` (monthsPerYear null), settings picker, converter, year view, widget config, editor, Wear and `taqvim://` links; CLDR month names in 13 languages (DT-037); year-aware month names on every screen via `CalendarMonthNames` (main@3170881: month grid, year view, timeline, agenda, widgets, Wear, date pickers); the calendar screen's secondary-calendar chooser, which crashed on Hebrew after d15b2a4, is fixed there. F07 first step (main@381f6c0, ADR-0025 addendum): CLDR 48 Hebrew month names via the ICU4J public API for 13 of the 24 languages (`HebrewMonthNames`, `tools/i18n/HebrewMonthsGen.java`, HebrewMonthNamesTest); wiring done in main@d15b2a4. Machine-translated Hebrew month names (marked for review) for ps, ckb, kmr, az, ne, hi, tg, uz, id, ms, zh (main@76c6ddd, DT-037) |
| T-109 | Gregorian Easter and movable feasts (A-16, ADR-0025) | DONE | not shown in the app | main@f9d2140, main@330d795 | G: ChristianMovableFeastsTest — all 67 336 USNO Christian observances 1583–9999 exact; Meeus ch. 8 examples · P: Easter a Sunday 22 Mar–25 Apr and Advent a Sunday 27 Nov–3 Dec for random years up to 10 000 000 and near Int.MAX_VALUE, 5 700 000-year cycle, years before 1583 rejected (7 tests) · G/P: JulianComputusTest (9) — Meeus Julian examples, Easter 1582, JDN round trip ±10¹⁵, Sunday 22 Mar–25 Apr to ±10 000 000, 532-year cycle, reform switch, Orthodox Easter | `GregorianComputus.easter`, `ChristianMovableFeasts.forYear` (`MovableFeast` enum). Integration pending: a movable-feast dataset rule, translated feast names (24 languages), default visibility per ADR-0007. Every year (main@330d795): Julian computus before 1583 via `ChristianMovableFeasts.civilForYear`, `JulianComputus.orthodoxEaster` |
| T-110 | US daylight saving time (ADR-0025) | DONE | — | main@7b336c2 | G: UsDaylightSavingRulesTest (5) — all 16 066 USNO begin/end dates 1967–9999 exact, none before 1967, March 8–14/November 1–7 Sundays for random years to 10 000 000, any Long year equals its 400-year-cycle year · G: UsnoUsDaylightSavingTest (4) — daily scan 1967–9999: New York, Chicago, Denver and Los Angeles change offset only on the USNO dates with the right offsets; 23/25-hour days through `toJdn`/`atStartOfDayIn`; tz rules follow the national rule for random years to 10 000 000 | `UsDaylightSavingRules.forYear(Long)`: federal rule eras (15 U.S.C. §260a; Pub. L. 89-387, 93-182 as amended by 93-434, 99-359, 109-58 §110) as an algorithm with no upper bound. The tz database and the national rule agree everywhere checked; on devices the platform tz data is used |
| T-200 | Language table | DONE | — | main@cdf1a66 (model: main@7e4e798) | U: LanguageTableTest (29: approved list, every language has every field ×24, exact data-gap set, numeral/direction rules, lookup & and-joining, snapshot `snapshots/language-table.json`), LanguageTableParserTest (4: missing/malformed entries, CLDR date-pattern parsing) · G: LanguageTableCldrOracleTest (24: native name, script, direction, week start, weekend, AM/PM, "and", date order/separator/padding, Gregorian/Persian/Islamic month names vs ICU4J 78.3) · U: PrayerConventionsTest (2) | ADR-0007 (languages + product defaults). CLDR 48 data in generated `languages.properties` (no non-Latin text in Kotlin). Gaps not guessed → DATA_TODO DT-004…DT-007 (ckb "and"; Persian months ckb/kmr/ne/id/ms/zh; Islamic months ckb/ne/zh; Bikram Sambat months) |
| T-201 | Numerals | DONE | — | main@b05c636 | U: NumeralsTest — `parse("۱۲٫۵") == 12.5`, separators and grouping per system (incl. Indian grouping), lenient mixed-digit parsing, traditional Tamil (18 rows + malformed input) · P: Long and BigDecimal round-trip through every system × grouping, traditional Tamil round-trip (35 tests) | Latin, Persian, Eastern Arabic, Devanagari, Tamil; CLDR separators; Tamil ௰/௱/௲ signs |
| T-202 | Date & duration formatting | DONE | — | main@05bf3e6 | U + oracle: PluralRulesTest (27), FormatTableTest (4), DateFormatterTest (47), RelativeTimeFormatterTest (48) — against ICU4J PluralRules (n 0–1000), full date formats (40 random dates per language × calendar), relative phrases (0–1000 past/future, day/week/month/year), units and lists · S: DateFormatSnapshotTest — 24 languages × 4 calendars × {long, numeric, iso} (288 rows) | CLDR 48 via ICU4J 78.3 → `formats.properties` (generator in scratchpad, re-checked by oracle tests); integer CLDR plural evaluator (TR35); ICU4J test-only. Gaps degrade instead of guessing: numeric month, no era, `null` relative/duration text (DT-008…DT-010); NEPALI uses the Gregorian pattern until T-105. fa/prs Persian LONG pattern overridden to `EEEE d MMMM y` (ADR-0014, main@51648bb); ps/ckb/ne root-fallback patterns in DT-025 |
| T-203 | Persian text normalization & fuzzy match | DONE | — | main@94d2947 | U: PersianTextTest — 50 normalization + 50 fuzzy pairs, search keys, OSA distance with early exit · P: normalize/searchKey idempotent, distance symmetric and zero iff equal (104 tests) | Restricted Damerau–Levenshtein (OSA) ≤ 2 on search keys; hamza carriers folded in search keys only |
| T-204 | String resources plan | DONE | — | main@1a4d047, main@577c1f3 | L: MissingFarsiTranslationDetectorTest (4: untranslated string/plurals/array reported, missing values-fa folder, complete fa clean, other locales may be incomplete), TaqvimIssueRegistryTest updated · Konsist: FarsiTranslationKonsistTest (repo fa completeness = 100 %, negative temp-tree case) | docs/i18n/STRINGS.md: en source, fa 100 % at every commit (stricter than "before v1"), naming/placeholder/plural conventions, Weblate component config (project to be created by owner), en-XA/ar-XB pseudo-locales in debug. `config/i18n/same-as-source.txt` lists 138 entries that keep the English wording with a reason; `config/lint/lint.xml` ignores UnusedTranslation because Indonesian must use `values-in` while `locales_config` declares `id-ID` (AppLocalesTest and TranslationReportKonsistTest still cover the set) |
| T-300 | Rule engine | DONE | — | main@d7cf58e, main@d995b10, main@8676be7 | U: RuleEvaluationTest — 108 cases, ≥ 10 per rule (Fixed incl. 29 Feb and 30 Esfand, NthWeekday, LastWeekday ± offset, LastDayOfMonth, Single, NthDayOfYear, RelativeToEvent across calendars, Astronomical with time zones) against java.time/ICU4J · G: OccurrenceGoldenTest — last Friday of Ramadan 1440–1450 (ICU4J civil), 3rd Thursday of November 2020–2035, day 256 2015–2030, last Tuesday of the Persian year 1395–1410 (ICU4J), official 1447 last Friday of Ramadan (60) · U: cycle/self/unknown/duplicate rejection, unavailable calendar, missing astronomy source, model invariants · P: occurrences sorted & unique and dated in the rule's calendar | `CalendarProvider.DEFAULT` = Persian, Iranian official Islamic, Gregorian (Nepali after T-105); `AstronomicalEventSource` implemented by T-403; validity/visibility left to T-302. `Single` rules require `oneOffReason` and none ship (main@d995b10, ADR-0036). `LunarTithi` rule for Nepali lunar festivals (main@8676be7, ADR-0038) |
| T-301 | Year cache & day lookup | DONE | — | main@6db021b | U: EventLookupTest (6) — Persian/Islamic/Gregorian merge and order, disabled source vs ALWAYS_DISPLAYED, offset crossing into the next year, cache hits/misses/eviction, unavailable calendar · B: 10 000 warm `eventsOn` < 50 ms (best of 5, 60 definitions) | Thread-safe LRU of 64 per (calendar, year, enabled sources); years y−1…y+1 consulted; one calculator per source so each source can use its own Islamic calendar |
| T-302 | Policies | DONE | — | main@5c701af, main@76c6ddd | U: EventVisibilityPolicyTest — 192-row truth table (source × holiday-only × hide-abroad × abroad × validity none/in/out × holiday × ALWAYS_DISPLAYED) + 4 cases (non-religious abroad, validity calendar unavailable, variant per source, lookup integration) | Order: validity → ALWAYS_DISPLAYED → source enabled → holiday-only → religious-abroad; validity evaluated in its own calendar; ADR-0010 Islamic variant per source. Afghanistan official holidays always use the tabular type II calendar (matches all 4 dated Afghan announcements; ADR-0010 addendum) and every official Islamic holiday carries its date origin (main@76c6ddd, OfficialDateOriginsTest) |
| T-303 | Holiday determination & workday basics | WIP | — | main@12b54fa | U: HolidayCalendarTest (3) — reasons, enabled sources only, weekend parameter and per-language CLDR weekends · G: OfficialHolidaysGoldenTest (`:data:events`) — generated official holidays equal the official 1404 and 1405 calendars in both directions | `holidayReasons`/`isHoliday`/`isWeekend`/`isWorkday`; weekend is a parameter (e.g. `forLanguage` from T-200 CLDR data), never guessed. Pending: official calendars 1403 and 1406 |
| T-304 | Search index | DONE | — | main@c635f90 | U: EventSearchIndexTest (9) — نوروز / نوريز / nowruz, ranking exact > prefix > substring > fuzzy, two-edit fuzzy, short queries, ties, filters, limits, best matching text per event · B: 10 000 mixed queries over 1 000 events ≈ 40 ms per run (< 200 ms) | Suffix table plus single-deletion fuzzy index over PersianText search keys (T-203); fuzzy only for queries ≥ 4 characters and while fewer than `limit` stronger hits exist |
| T-305 | Repository impl (`:data:events`) | DONE | — | main@c8b422e, main@2ecb37a, main@1413bc2, main@cbd40b6, main@5f1d9db, main@7a90a76, main@9f23c0b | U: EventsRepositoryTest (6) — Nowruz 1405 holiday, weekend and official Hijri date; Turbine re-emission on preference change, none for identical results; Hijri precedence official > offset > estimate, none for Umm al-Qura; personal events spanning days and RRULE-lite expansion in their own calendar; device/iCalendar day assignment in the collection zone; empty range rejected · U: EventsSettingsTest (2) · R: RoomEventInputsTest (3) — Room personal records with recurrence, iCalendar window, Koin module end to end (44 data:events tests; lines 173/174, branches 30/34) · C (main@cbd40b6): CrossSurfaceContractTest (6 + 3 disabled) — timeline, reminder planner and search agree for cross-zone, moved/cancelled recurring, UTC, all-day and subscription events; ExportExpansionContractTest (5) — ICS export re-expands to the calendar's instants | `EventsRepository.days(range)`/`day(jdn)` combine the generated dataset (visibility + holidays per preferences) with personal, device and iCalendar events; dataset lookups rebuilt only on settings change. Enabled sources default to all except ANCIENT_IRAN (PLAN §5.1) until a preference exists (T-1500). No override table yet (D-07). Zone read at collection start; Hijri-offset expiry applies at the next emission. `eventsDataModule` not yet in `:app`. Default enabled sources are per language (ADR-0007 addendum, main@19deed9): international for all, plus fa → Iran, prs/ps → Afghanistan, ne → Nepal; hard-coded `EventsSettings.DEFAULT_ENABLED_SOURCES` removed, `toEventsSettings()` uses the stored sources; golden test: 28 Asad 1405 is a holiday only for prs/ps, Nowruz 1405 only for fa. Day assembly (and so widgets and search) drops excluded occurrences and shows overridden ones on their own days (main@e3a58b8). B03 fixed (main@2ecb37a, main@1413bc2, ADR-0031): personal and feed occurrences are dated by their instants in the display zone — all-day events keep their dates, timed events cover the display-zone days they touch, sources are read one day wider; TimelineZoneDatingTest (2) and EventDaysTest (4). I08 (main@cbd40b6) found three bugs, pinned as disabled tests: search dates personal events by their stored date (T-804), search loses the next occurrence of a started series (T-804), the reminder planner drops a regular occurrence on the day a moved one lands (T-1001, fixed in main@37ac1b2) — search fixes in main@6be22a5; all three pinned tests now enabled. I06 (main@5f1d9db): follows device time-zone changes through the shared `DeviceTimeZone.changes()` stream. I01 (main@7a90a76, ADR-0035): personal series are expanded by the shared `OccurrenceSeries` pipeline (calendar, agenda, timeline, widgets, search); calendar range for 1 000 series 58 → 28 ms. CalendarCompletenessTest (main@9f23c0b): every day of SH 1380–1480 in all calendars and Islamic variants with every dataset rule and the day assembler under each language default |
| D-01 | Dataset JSON Schema + validator CLI | DONE | — | main@ad81460 | U: DatasetValidatorTest — valid sample covering all 8 rule types, 30 invalid fixtures each reporting exactly one issue (all 6 issue kinds: malformed JSON, schema, duplicate id, unknown/self reference, day out of range, invalid validity), cross-file ids/references, semantic checks skipped on schema errors, fuzz 10 000 random/mutated documents without a crash (35 tests) · DatasetCliTest — exit codes 0/1/2 | `dataset/events.v1.json` (draft 2020-12, rule `type` discriminator via if/then so one defect = one error) + `dataset/README.md`; `./gradlew :tools:dataset:validate`. Dependency: networknt json-schema-validator 3.0.7 (Apache-2.0, Jackson 3); license gate 452 modules, 0 violations. Day ranges use each calendar's longest month (exact per-year checks come with D-08 generation) |
| D-02 | Iran official holidays 1403–1406 | WIP | — | main@945d0c5, main@c309fd4 | G: IranOfficialHolidaysTest — the dataset's rules reproduce exactly the 26 official holiday dates of 1404 and of 1405 (golden date sets and the official_holiday column of the daily fixtures); validator clean | `dataset/iran/iran-official-holidays.json`: 27 records (10 Persian Fixed; 16 Islamic Fixed + Imam Reza martyrdom as LastDayOfMonth of Safar), official Persian titles with PDF text-layer artefacts corrected, citations with pages for both years, no English titles (no primary source). Pending: official calendars 1403 and 1406, reviewer sign-off (open title questions in the D-02 report) History 1381–1405 (main@c309fd4): `IranOfficialHolidayHistoryTest` (24) evaluates the Iran rules against the 530 official holidays of 21 years — with the printed Hijri dates every year matches exactly; 0 rule errors; 2 law changes given `validity` with citations (Imam Hasan Askari martyrdom a holiday from AH 1440, `Calendar-1397.pdf` p. 13; 2 Shawwal from AH 1433, `Calendar-1391.pdf` p. 8); with the computed lunar calendar 40 lunar-calendar differences (1382–1393) and 10 announced shifts (1383–1385), all explained; importer fixed for wrapped holiday cells. Per-year diff in `docs/data-todo/iran-holiday-history.md` |
| D-03 | Afghanistan set | DONE | weekday-dependent one-off days (DT-031); further announcements and labour-law holidays (DT-032) | main@e031285, main@d995b10 | G: AfghanistanOfficialHolidaysTest (5) — validator clean; golden per Solar year (1404: 1, 1405: 6) equals the records' dates; dataset ids = golden ids; every record AFGHANISTAN_OFFICIAL, holiday, Single, Persian title, Bakhtar citation with page · data/events goldens green on regenerated sources (57 tools:dataset, 83 data:events tests) | Source: Bakhtar News Agency holiday announcements (owner-accepted primary source 2026-09-14; cited per record with issuing authority and publication date; not copied). Records: Soviet withdrawal 26 Dalw 1404; Arafa + Eid al-Adha 1447 (9, 10, 11, 13 Dhu al-Hijjah); Victory 24 Asad 1405; Independence 28 Asad 1405. Islamic days are ISLAMIC Single rules placed by the user's Islamic variant (ADR-0010); Afghan 1 Rabi al-Awwal 1448 = 2026-08-15 differs from Umm al-Qura (08-14) — Afghanistan month table needed (DT-033). Missing dates in DT-031; recurring rules need the Labour Law text (DT-032). Bakhtar's relaunched site starts Dec 2025; Internet Archive lookups rate-limited.. Afghan holidays now show only for prs/ps users by default or when the source is enabled (main@19deed9). 6 recurring `Fixed` rules with validity from the announced year (main@d995b10, ADR-0036); the one-off 13 Dhu al-Hijjah 1447 removed |
| D-04 | Nepal set | DONE | Gyalpo Lhosar, Fagu Purnima, regional and community holidays, Eid dates by sighting (see DATA_TODO) | main@8676be7 | G: NepalOfficialHolidaysTest — rules equal every dated day of the MoHA 2082 and 2083 notices in both directions; NepaliLunarDaysTest — 26 lunar dates incl. intercalary and skipped months and far years; LunarTithiRuleTest | 27 recurring rules (`dataset/nepal/nepal-official-holidays.json`): BS-fixed days, national days, Gregorian and Islamic fixed days and 11 `LunarTithi` rules computed from a Surya Siddhanta Moon (ADR-0038); Nepali titles only (no official English notice); on by default for `ne` (ADR-0007) |
| D-05 | UN international days | WIP | 102 of ≥ 150 (blocked on Persian titles, DT-019) | main@2648a53, main@5c281e0, main@c77c571 | G: UnInternationalDaysTest (4) — validator clean, golden count 102 (96 Fixed, 5 NthWeekdayOfMonth, 1 LastWeekdayOfMonth), 23 rules spot-checked against the cited pages (golden CSV), every record cites the UN list and a Persian-title source · CalendarCompletenessTest extended with the LastWeekdayOfMonth rule · data/events goldens (NowruzWorkdays 30, OfficialHolidays 2, OfficialEvents 1) green on the regenerated sources | 102 of 233 UN days; 10 weeks excluded (schema has no duration). fa titles: UNIC Tehran list via the Internet Archive copy of 2012-10-05 in docs/sources (89, three of them found on 2026-09-18 by re-reading the list by date), United Nations in Iran pages (9), archived UNIC Persian articles (4); nothing translated. Added 2026-09-18: clean energy, radio, sport for development and peace, migratory birds (2nd Saturday of May), telecommunication, maritime (last Thursday of September), toilet, chemical-warfare victims, soil. The whole UNIC archive (937 Persian articles) and all 576 Persian titles of iran.un.org were searched and name no further day, so the remaining 130 days stay in DT-019 (also DT-023, DT-024) until a Persian publication exists. Generated sources exempt from ktlint max line length (.editorconfig) |
| D-06 | Ancient Iranian festivals | WIP | 2 records | main@05d9fc1 | G: AncientIranianFestivalsTest (3) — validator clean; golden count 2 and rules (Fixed 1/6, Fixed 9/30) against the cited pages; every record PERSIAN, ANCIENT_IRAN, CULTURAL, not a holiday, with a Persian title and both official calendars cited · data/events goldens green on regenerated sources (83 data:events, 52 tools:dataset tests) | Records only where the official 1404/1405 calendars print the Persian title and date: Zoroaster's birthday (6 Farvardin), Yalda night (30 Azar; Encyclopaedia Iranica ČELLA as encyclopedic citation). Off by default via the ANCIENT_IRAN source (T-305). Iranica (iranicaonline.org without www; the www host returns 403) gives Mehragān, Sada, Čahāršanba-sūrī, Ābānagān and the 30-day-month feasts in transliteration only, with conflicting dates — no Persian primary titles (DT-029). The schema has no 30-day-month convention flag or "eve of the last Wednesday" rule (DT-030) |
| D-07 | Islamic Iran override table 1390–1410 | DONE | official months beyond Ramadan 1446 – Shawwal 1448 (DT-002) | main@6dfb3be, main@27950c5 | G: IslamicIranOverridesGoldenTest (5) — validator clean; table equals `IranOfficialMonthStarts` month for month; every printed start is day 1 of its Hijri month on the cited page of the official 1404/1405 daily fixtures; derived Ramadan 1446 = 19 days before the printed 20 Ramadan on 1 Farvardin 1404; every month cites a stored calendar PDF with a page · U/P: OverridesValidatorTest (14) — valid sample; 8 invalid fixtures, one issue each (malformed, schema ×2, duplicate month, gap, 28- and 31-day months, 30 Esfand in a common year); cross-file duplicates and gaps; 2 000-document fuzz; CLI exit codes (76 tools:dataset tests) | `dataset/iran/islamic-iran-overrides.json` (schema `dataset/islamic-iran-overrides.v1.json`): official Hijri month starts with Persian first day and page, re-extracted from the Calendar Center 1404/1405 calendars; `:tools:dataset:validate` runs `validateOverrides`. `IranOfficialMonthStarts` stays the runtime table (core cannot depend on data); the golden test prevents drift (ADR-0009 addendum). File named with hyphens like the other dataset files. Years 1390–1403 and 1406–1410 have no source (DT-002). Optional override only (main@27950c5, ADR-0037): bundled copy `islamic-iran-official.json`, off by default |
| D-08 | Dataset code generator (KotlinPoet) | DONE | — | main@49d1ead, main@fe060cc | S: EventsCodeGeneratorTest — snapshot of the synthetic sample (all 8 rule types, validity, flags, aliases, links), determinism/id order/file splitting, empty dataset · U: GenerateEventsCliTest — refuses invalid data and writes nothing, removes stale files, usage errors, committed output up to date · U: OfficialEventsTest — :data:events compiles, ids sorted and unique, citations and Persian titles, all 27 events evaluate in OccurrenceCalculator | `./gradlew :tools:dataset:generateEvents` → `data/events/.../generated/OfficialEvents*.kt` (8 events per file); file-level NoHardcodedNonLatinText/MaxLineLength suppression for dataset text; dataset-only fields (retrieved, updated, reviewedBy) not emitted |
| D-09 | Dataset CI + CONTRIBUTING-DATA.md | DONE | local | main@5ff3a12 | L: actionlint (5 workflows) · local: `:tools:dataset:validate` (1 file, 0 issues); the existing D-01 fixture 13-missing-citation proves a record without a citation is rejected | pr.yml "Dataset validation" runs validate, then `:tools:dataset:test :data:events:test` (golden sets + stale-generated-code check); fixed the job's wrong task name (`validateDataset` → `validate`). Weekly `dataset-links.yml` resolves every citation/link URL (D-05). `CONTRIBUTING-DATA.md` (rules, workflow, reviewer sign-off checklist), PR-template dataset checklist. **"Green on GitHub" pending first push** |
| T-400 | Solar position (A-09) | DONE | — | main@e408019 | G: NoaaSolarCalculatorTest — all 240 rows of NOAA's Solar Calculations spreadsheet (declination, equation of time, solar noon, sunrise, sunset, zenith, refraction, apparent elevation, azimuth) to 1e-6 (plan ±0.01) · U: polar day/night, Sun overhead and poles stay finite · P: declination/equation-of-time continuity 1901–2099, morning azimuth increases (5 tests) | Transcribed from NOAA's public-domain spreadsheet (Meeus-based); branch-free floor modulo; `null` rise/set on polar days |
| T-401 | Prayer times (A-10) | DONE | — | main@e6b65cb, main@7846949 | G: PrayerTimesOfficialTest — Institute of Geophysics official 1405 timetables, 31 Iranian cities × 365 days: Fajr, sunrise, Dhuhr, sunset, Maghrib within 1 min, midnight within 2 min (TEHRAN method) · U: PrayerTimesCalculatorTest — polar day/night → Unavailable, high-latitude rules (none, ½, ⅐, angle-based, Geophysics white nights), Makkah Isha +90, Hanafi Asr, four midnight modes · P: ordered times for every method at non-polar latitudes (37 tests) · U/P (main@7846949): SolarEphemerisTest (2) — NOAA agreement 1900–2100, 400-year shift to ±2.7 million years; PrayerModelTest (11) — horizon settings and elevation dip, events at their altitudes, Asr hand example, nearest-latitude rule at 60°/65°/70° N and Isha continuity into seasons without twilight, Diyanet precaution minutes, rounding modes, order and continuity for random years −2000…6000 at any latitude, days a trillion days away, a year of times in < 50 ms; PrayerTimesOfficialTest midnight tolerance tightened to 1 min; 9 screenshots re-recorded | Plan's six-city golden: Tehran and Mashhad covered (plus 29 more Iranian cities); Kabul, Istanbul, Berlin, Sydney need published timetables (DT-011). Precise model (main@7846949, ADR-0029): apparent Sun from cosinekitty for any year, Meeus rise/set and refraction, optional elevation dip, nearest-latitude rule, Diyanet method from its public statements, per-method adjustments and rounding; same minute as the 31 official timetables on 99.8–100 % of days (was 56–98 %) |
| T-402 | Qibla & great-circle (A-11) | DONE | — | main@ce4d495 | U/oracle: GreatCircleTest (13) — 10 sample cities, bearing ±1e-6° and distance ±1 m against an independent vector formulation; antipode, coincident points and poles → null; random-pairs property | Spherical model with IUGG mean radius; Kaaba 21.4225°N 39.8262°E (PLAN §6). Deviation: no primary Qibla table was available, so G became an oracle comparison; computed Tehran bearing is 218.4° (the plan's “≈217.6°” was not reproduced) |
| T-403 | Astronomy façade (A-13) | DONE | — | main@2facefb, main@456d49a, main@e3f4938, main@d30c4b3, main@e710000, main@e322bf1, main@0c9557e, main@5dea390 | G: EclipsesTest — NASA GSFC catalogs 2024–2030, all 16 solar + 16 lunar eclipses (type, peak ±5 min), local total eclipse 2024-04-08 and partial 2029-01-14 at catalog greatest-eclipse points · G: official Iranian equinox instants 2025/2026 ±5 min · U: SkyTest — season order and year length, Moon quarter cycle, southern-hemisphere phase mirroring, rise/transit/set incl. polar night, position and libration bounds · G/P: LunarLimbTest (5) — Meeus Example 48.a bright-limb position angle 285.0° ±0.1°, parallactic angle 0/180° on the meridian and sign of sin H (property), first/last quarter limb west/east through 2026, Tehran/Sydney tilt mirrored at transit, angles finite and in range over 2020–2030 (property) · G: USNO 1700–2100 (ADR-0025) — SkyTest seasons 1 604 (2020–2040 ±5 min, others ±2/3 min, max 140 s), UsnoApsidesTest 801 perihelia/aphelia (kinds exact, ±2.5 h, distance ±5 km), UsnoMoonPhasesTest 19 839 quarters (±2 min, max 84 s), UsnoSolarEclipsesTest 582 eclipses 1800–2050 (count, order, date, type exact) · U/P: SolarEclipseShadowTest (4) — γ/u classification, the eight non-central eclipses, −2000…4000 consistency · G: OfficialIranEclipsesTest (4) — the 9 eclipses of the official calendars 1399/1404/1405: stated weekdays, lunar type and umbral contacts ±2 min (measured −45…+8 s), Moon above/below the horizon at all 31 provincial capitals as stated, solar types and non-visibility | cosinekitty/astronomy 2.1.19 internal (no library types in the API). Lunar tilt added (main@456d49a): `Sky.moonTilt` — bright-limb position angle (Meeus 48.5), parallactic angle (14.1) and tilt from the zenith for the observer. USNO goldens (owner-downloaded; main@e710000 and later): DT-012 resolved. Non-central total/annular solar eclipses now classified by Meeus ch. 54 γ/u (main@e322bf1); Earth perihelion/aphelion `Sky.earthApsides` (main@0c9557e). Remaining: none in code |
| T-404 | Zodiac & moon-in-Scorpio | WIP | — | main@5ccdd5f | U: ZodiacTest (5) — tropical sign boundaries (0°, 30°, 210°–240°, negative and multi-turn angles), IAU constellation of bright stars far from boundaries (Sco, Leo, Vir, Tau, UMi), Moon in Scorpius 2026 (12–14 visits, midpoint inside, one minute outside each boundary outside), tropical Scorpio 2026 (13–14 intervals of 48–66 h), window validation and clipping | Tropical and IAU systems; hourly scan with minute boundaries. Remaining: golden "known dates" from a published list (DT-013) |
| T-405 | Houses, lots, ascendant (A-14) | WIP | — | main@7c1946f | U: HousesTest (4) — ascendant on the eastern horizon and midheaven culminating (property, 1950–2050, latitudes ±60°, all longitudes), Placidus cusps 11/12 at ⅓ and ⅔ of the diurnal semi-arc with zodiacal order and oppositions, polar-circle null, lots day/night formulas and Tehran day/night chart | Meeus angles, NOAA true obliquity, Placidus by semi-arc trisection. Remaining: published-chart golden (DT-018) |
| T-406 | Tithi, planetary hours, Chinese/animal year, year names | WIP | — | main@a332cf4, main@88e16b2, main@9f23c0b | U: PlanetaryHoursTest (3) — Chaldean order and weekday rulers for every weekday, unequal day/night hours, polar Unavailable · U: TithiTest (3) — 30 tithis per synodic month, tithi 15/30 near full/new moon, boundaries · U: AnimalYearTest (2) — 12-year mapping table anchored on 2020 = Rat · G/P: ChineseNewYearTest (7) — 2020 anchor, every month start 1901–2100 vs ICU4J with the 16 near-midnight differences listed and checked against the USNO moon phases, 2033 leap month, 1929 time switch, New Year 21 Jan–20 Feb 1901–2100, rule properties for random years −3000…5000 | Tithi = (Moon − Sun) / 12° on a modern ephemeris (not the Surya Siddhanta computation); animal of the Chinese year beginning in a Gregorian year. Pending: panchang reference times (DT-014), Hijri-Persian animal year names (DT-015), none for lunar new year: computed from GB/T 33661 rules (main@88e16b2), `AnimalYear.forDate` switches on it. Planetary hours return Unavailable at polar-day edges instead of throwing (main@9f23c0b), found by SkyAndTimesCompletenessTest |
| T-407 | Photography panel calculations (F-10) | DONE | reference check pending | main@cdb2902 | U: PhotographyPanelTest (3) — morning/evening golden hour (apparent altitude −4°…6°) and blue hour (−6°…−4°) ordering and durations, moonrise/moonset, polar cases empty | Plan's ±3 min comparison with a published photographers' ephemeris pending (DT-017) |
| T-500 | NLP date parser (F-03) | DONE | LONG gaps | main@f0c0af1 | G: 500-phrase synthetic fa/en corpus (`golden/nlp/date-phrases.tsv`; expected days from `:core:calendar`, never from the parser) · P: NUMERIC and ISO format → parse round trip for 24 languages × 3 calendars (60 dates each) · S: LONG round-trip snapshot `long-round-trip.tsv` (asserted for fa, en) (24 tests; lines 98.97%, branches 92.5%) | Explicit Persian/Islamic/Gregorian dates in any digit script, relative (fa/en), anchored "N days before/after <event>", ranges, ranked readings with confidence and span. `AnchorLookup.find(query, reference: Jdn)`; ranges return `end: ParsedEnd?`. Not supported: two-digit years, Nepali, spelled numbers > 10, relative words in the other 22 languages. LONG parse-back gaps: kmr Islamic/Gregorian and tg Persian/Islamic 0/60, ja Persian 0/60, ta Islamic 39/60 (digits in month names), fr Islamic 54/60, bn Islamic 55/60 |
| T-501 | Text date detector (F-04) | DONE | real-world corpus pending | main@ecb16ea, main@5d4cf77 | G: TextSnippetCorpusTest — 200 synthetic everyday fa/en snippets (176 with dates: single, weekday-prefixed, yearless, numeric, Islamic/Gregorian month names, ranges; 24 number-heavy without dates: times, prices, phones, versions, IPs) yield exactly the expected spans and days · B: 100 KB of snippet text (date-dense worst case) scanned < 50 ms · U: TextDateDetectorTest (7) — range outranks inner dates, same-span alternatives, seed windows, relative/anchored only on request, confidence floor, ambiguous numeric order (34 nlp tests; lines 98.97%, branches 92.5%) | Seed-token scanner + T-500 grammar on ±24-token windows; overlaps resolved by score. Defaults: ABSOLUTE + RANGE, confidence ≥ 0.7. Parser change: the year plausibility check (≤ 150 years, distance penalty) now also applies to month-name dates. PLAN asks for 200 real-world snippets; synthetic snippets used until a reusable corpus exists (DT-022). Far years (main@5d4cf77): a year followed by its calendar marker is parsed at any distance; the ±150-year plausibility rule applies only to unmarked years |
| T-502 | ICS reader/writer (F-05) | DONE | — | main@31f5c52 | G: 30 synthetic fixture calendars (Google/Outlook-style exports: PRODID, VTIMEZONE, X- properties, Windows zone names, folding, escaping) · U: content lines (6), values (5), writer (2), robustness — malformed input yields errors with line numbers, never exceptions (4) · P: write→read round-trip · fuzz (49 tests) | RFC 5545 subset: VEVENT, DTSTART/DTEND (DATE, floating, UTC, TZID), RRULE (FREQ D/W/M/Y, INTERVAL, COUNT, UNTIL, BYDAY±n, BYMONTHDAY±), EXDATE, SUMMARY/DESCRIPTION, VALARM DISPLAY; 75-octet UTF-8-safe folding, CRLF. Unsupported (warnings): VTODO/VJOURNAL/VFREEBUSY, VTIMEZONE bodies (IANA ids used), sub-daily FREQ, BYMONTH/BYYEARDAY/BYWEEKNO/BYSETPOS/WKST, RDATE, DURATION, RECURRENCE-ID, attendees |
| T-503 | Recurrence engine | DONE | — | main@4464dbb, main@697cc1a, main@e7cb1da, main@7a90a76 | G: RecurrenceOracleTest — 5 RFC 5545 examples + 50 Gregorian cases against an independent day-by-day java.time oracle (52) · U: RecurrenceEngineTest — invalid-day policies SKIP / NEXT_DAY / LAST_DAY_OF_MONTH for 30 Esfand, 29 February, day 31 and Islamic 30 Dhu al-Hijjah (10) · P: instances strictly increasing in Gregorian, Persian and Islamic calendars · U/G (main@697cc1a): daily BYDAY/BYMONTHDAY limit candidates before COUNT (weekday-only, multi-weekday, −1 and positive month days, intersecting filters, INTERVAL=2, ignored ordinal), a daily rule counts month days in the event's own calendar; 6 new golden rules (56) and the day-by-day oracle limits the same way | ADR-0011; lazy sequence bounded by COUNT/UNTIL/window, stops after 1 000 empty periods. B02 fixed (main@697cc1a): the DAILY branch ignored BYDAY and BYMONTHDAY, so FREQ=DAILY;BYDAY=MO;COUNT=3 gave three consecutive days; per-frequency semantics documented in `RecurrenceRule`. I03 (main@e7cb1da): periods are computed from their index using the calendar's months per year and expansion starts at the requested window when there is no COUNT; first 2026 occurrence of a daily 1900 series 4.91 → 0.008 ms, monthly from year 1 13.11 → 0.004 ms; RecurrenceSeekTest (3 + timing). I01 (main@7a90a76): `OccurrenceSeries` (start, rule, length, exclusions, changes keyed by original day; `starts`, `isOccurrence`, `instances`) is the shared expansion; OccurrenceSeriesTest (3) |
| T-504 | Workday engine (F-07) | DONE | — | main@d19884b | U: WorkdayCalculatorTest — 100 cases (50 addWorkdays, 50 workdaysBetween) against an independent java.time loop, next/previous/zero semantics, profile options (sources, half-day policy, personal leave), no-workday profile → typed failure · P: addWorkdays lands on a workday (104 tests) · G: NowruzWorkdaysGoldenTest (`:data:events`) — first 14 days of Farvardin 1404/1405 and Farvardin workday counts with the generated official holidays (30) | Built on HolidayCalendar (T-303); weekend is a user parameter; workdaysBetween half-open, next/previous strict, addWorkdays(d, 0) → d or next workday; NoWorkday after 3 660 non-working days |
| T-600 | Proto DataStore | DONE | — | main@83f9182 | U: UserPreferencesMappingTest — defaults for all 24 languages, proto ↔ model mapping, table-driven migrations · R: UserPreferencesRepositoryTest — DataStore read/write (33 tests; line coverage 91/92) | `user_prefs.proto` + serializer + typed store. Build-logic: protobuf Gradle plugin on the convention classpath; generated `ir.taqvim.data.preferences.proto.*` excluded from Kover. Default Islamic variant per language is provisional (ADR-0012). First-run language from the device locale's launch language (fa-AF → prs, ku → kmr); proto field 16 `onboarding_completed` (main@c4273e2) |
| T-601 | Room schema & DAOs | DONE | — | main@c2e5b80, main@6928708 | R: PersonalDaoTest, WorkAndCacheDaoTest — in-memory DAO CRUD with Turbine flows · R: TaqvimDatabaseMigrationTest — MigrationTestHelper validates every exported schema up to the latest · U: DatabaseConvertersTest (14 tests; line coverage 147/151) | The 11 §4.3 tables, 7 DAOs, converters and mappers to core `RecurrenceRule`/`WorkdayProfile`, 2 000-row diagnostics ring, schema v1 JSON in `data/database/schemas`. Migration chain is 1→1 until v2 (ADR-0012). License gate: 506 modules, 0 violations. I02 (main@6928708): personal events and their children are read as Room relations in one transaction; 1 000 events: calendar range read 3 002 → 8 queries (556 → 77 ms), reminder setup 4 001 → 10 queries (243 → 16 ms); PersonalEventDetailsTest |
| T-602 | Device calendar adapter | DONE | — | main@ca9a397, main@5f1d9db | U: DeviceEventMappingTest (6) — all-day event keeps its date in UTC+3:30, UTC−8, Asia/Tehran, America/Los_Angeles; timed events in the device zone; property: every all-day date in every quarter-hour offset −12…+14 kept and inside its window; window widening/day overlap; invisible/deleted rows dropped, colors opaque · R: CalendarInstancesSourceTest (4) — provider fixtures (all-day, multi-day, deleted, invisible calendar), no permission → provider not queried, SecurityException/crash/null cursor → typed results, ContentObserver register/unregister · R: DeviceCalendarRepositoryTest (4) — provider → cache → events, refresh on change, permission loss → empty and cache cleared, unavailable provider keeps cache · R: WorkAndCacheDaoTest +1 (lines 141/142, branches 39/40) | Uses the T-601 `device_events_cache`; new `DeviceEventDao.replaceOverlapping`/`deleteOverlapping` so instances deleted in the provider leave the cache. Fixtures via a synthetic Robolectric-registered ContentProvider (`ShadowContentResolver` has no calendar data), so DELETED/VISIBLE filtering is in the adapter. Device zone read at collection start (zone change needs re-collection). Not yet wired into `:app`. I06 (main@5f1d9db): follows device time-zone changes through the shared `DeviceTimeZone.changes()` stream |
| T-603 | Location data | DONE | districts pending | main@c2e1528 | U: CityTableParserTest (3) — any column order, empty localized column = English spelling, prs→fa fallback, bad rows rejected with line number · U: CityCatalogTest (4) — bundled Natural Earth list (7342 places, unique ids, Tehran fields from the source), search ignoring case/Latin diacritics/Arabic letter variants, prefix-then-population ranking, nearest across the antimeridian · R: CityCollationTest (5) — ICU collation en, de, tr, fa, ar, each differing from code-point order · R: DeviceLocatorTest (5) — no permission, disabled providers, timeout removes the request, first fix wins, cancellation · R: PlatformGeocoderTest (5) (22 tests; lines 176/184, branches 97/110) | Natural Earth 5.2.0-pre populated places (public domain), names in 14 of 24 languages (DT-020); `LocationManager` only, no Play services; reverse geocoding only. Iran country divisions pending (DT-021). Not yet wired into `:app` |
| T-604 | Scheduler | DONE | — | main@4fbcf1e, main@762c432 | U: AlarmPoliciesTest (5) — skip if fired > 15 min late (exactly 15 min is on time), dedup, reconcile, restore drops late alarms and duplicates · U: ReschedulePolicyTest (3) — reschedule matrix for boot, app update, clock, time zone, exact-alarm permission and preference changes · U: AlarmSchedulerTest (6), RescheduleCoordinatorTest (7) — idempotent replace, moved alarms, inexact fallback with banner state, reboot restore · R: SystemAlarmClockTest (2) — ShadowAlarmManager set/replace/cancel, exact vs inexact · R: SchedulerIntegrationTest (2) — Koin + Room + AlarmManager + receivers end to end (25 tests; lines 199/201) | Dedup key (kind, sourceId, triggerAt), no schema change. Exact alarms when `canScheduleExactAlarms()`, else `setAndAllowWhileIdle` + `exactAlarmStatus`. Features supply alarms per kind via `AlarmSource`. Boot/update: restore + recompute; clock/zone: recompute; permission: restore; prayer prefs → PRAYER, Islamic variant/Hijri offset → REMINDER. Open: Hijri-offset 30-day expiry triggers no reschedule; `schedulerModule` not yet in `:app`. Wired in `:app` (main@189deba): `schedulerModule`, application starts `PreferenceChangeWatcher`. Remaining: no `AlarmSource`/`AlarmDelivery` bound yet (T-1001/T-1101). I07 (main@762c432, ADR-0033): fired alarms are leased (retry 1 min later) until the delivery outcome is recorded; failures retry up to 3 times within the 15-minute lateness window, then give up; reminder and prayer snoozes are scheduler kinds restored after reboot and pruned when their occurrence disappears; Room schema 6 (migration 5→6: attempts, retry and snoozed-from columns); AlarmDeliveryStateTest (10), AlarmPoliciesTest (+3), migration test |
| T-605 | Backup/restore (F-11) | DONE | — | main@d594bea, main@c6cdb01, main@d1f2863 | U: BackupCryptoTest (4) — PBKDF2-HMAC-SHA256 and SP 800-108 KDF against independently computed vectors (ASCII and UTF-8 passphrases), AES-GCM rejects wrong key/nonce/AAD/ciphertext, 51-byte header round trip · U: BackupCodecTest (9) — plaintext and encrypted round trips with preview of every table; fresh salt/nonce; wrong/missing/empty passphrase → typed errors; property: 40 random Unicode passphrases; tampered tag, ciphertext, nonce, salt, check value, algorithm or iterations → Corrupted/WrongPassphrase; truncated → Truncated/Corrupted; newer header/JSON version → UnsupportedFormat; garbage/foreign JSON → NotABackup; unknown fields ignored; dangling references, duplicate keys, bad enums → InvalidContent · R: BackupServiceTest (3) — export → change → restore brings back 7 tables and preferences (Room + DataStore); plaintext readable without passphrase; failing restore rolls back the Room transaction and leaves preferences untouched (31 database tests) | In `:data:database` (Konsist forbids feature→data; PLAN §3.1 has no backup module); depends on `:data:preferences`. JSON document (`format`, `formatVersion`, `appVersion`) or `TAQVIMBK` header + AES-256-GCM; PBKDF2-HMAC-SHA256 600 000 iterations, 128-bit salt, 96-bit nonce, header as AAD. Deviations: PBKDF2 only (no Argon2 in platform JCA); Room restore atomic, preferences written after it; caches, diagnostics and scheduled alarms not backed up (scheduler must reconcile after restore). Not wired into `:app`/`:feature:backup` (T-1503 needs a core port). Wired in `:app` (main@189deba, main@e39889d): `BackupService` bound; backups carry the event source link (schema 3) and the chosen place. Remaining: UI and core port (T-1503). Backup also carries athan settings (main@2599980; sound device-only). Backup also carries official reminder opt-ins and the all-day reminder time (main@56be204); backup/restore UI wired (main@cb576a0), restore recomputes all alarms. Backups carry optional `eventExceptions` and `eventOverrides` with key, parent and span checks; older backups restore unchanged (main@de60580); backup preview shows no counts for them. B09 fixed (main@c6cdb01, ADR-0032): a restore journal in noBackupFilesDir saves the backup and the data it replaces before writing; failures undo both stores, an undo failure is reported as RESTORE_INCOMPLETE and later restores refuse until recovery; recovery runs at app start before watchers and rescheduling; RestoreRecoveryTest (7). ADR-0032 open points closed (main@d1f2863): a per-process `RestoreGate` (held only when a restore journal exists) keeps the app shell on a "finishing restore" screen, and holds subscription refreshes, widget triggers, the persistent notification, icon and language sync until recovery ends; an incomplete restore keeps the hold until the next start; RestoreGateTest (4), RestoreHoldTest (3) |
| T-700 | Theme | DONE | — | main@99eac40 | U: SchemeContrastTest (2) — WCAG 2.2 contrast of 71 text/background role pairs ≥ 4.5:1 (standard) and ≥ 7:1 (high contrast) for random, default and extreme seeds in light/dark/black · U: ColorMathTest (5), SchemeGeneratorTest (4), ThemeSettingsTest (6), CustomFontsTest (2) · R: TaqvimThemeRobolectricTest (2), CustomFontsRobolectricTest (1) · S: ThemeScreenshotTest — 6 themes × LTR/RTL = 12 references (34 tests) | Own CIE LCh tonal palettes with fixed L* tones per role (not Material HCT), so contrast does not depend on the seed. Dynamic color + high contrast → scheme generated from the dynamic primary. Custom font from a file after an sfnt header check (file picker in T-1500); gradient and background-image veil (no contrast guarantee over images). No new dependencies |
| T-701 | Components | DONE | — | main@22d40c7 | U: ComponentGeometryTest (11) — wheel index/progress arithmetic, date-selection day clamping (29/30 Esfand), picker and month-grid model validation, gap-free column edges (property), row height, week-number slots, day-tone precedence, moon terminator, sun position and arc in LTR/RTL, ring sweep · R: ComponentSemanticsTest (8) — day cell spoken summary, selected state, click and labelled long-press; chips; tabs; top bar heading and action labels; empty state and tooltip; moon image description, sun arc summary, ring range info; screen surface padding · R: PickerAndGridTest (6) — wheel as one adjustable control, picker day clamping across years/months, bottom sheet, grid actions, RTL mirroring, grid measures each child once per pass · S: ComponentScreenshotTest — 13 galleries × 4 environments (light LTR 1.0, dark LTR 1.3, light RTL 2.0, dark RTL 1.0) = 52 references (111 core:ui tests; component package lines 237/237, branches 76/80) | TopBar, ScreenSurface, SegmentedTabs, NumberWheel, DatePickerSheet (calendar-agnostic), EventChip, DayCell, MonthGrid (custom single-pass Layout), MoonDisc, SunArc, ProgressRing, EmptyState, TooltipCard; all texts from callers. Deviations: 4 environments per component cover each variant value instead of the full product; black theme and large screens not captured; font scale 2.0 can clip secondary labels in fixed-size cells (T-1701); DayCell recomposition test left to T-1802. Day cells fit at font scale 2.0 (main@86b2592) |
| T-702 | Painters for widgets | DONE | — | main@909ed8b | P: PainterGeometryTest (7) — month cells tile every row without gaps and mirror exactly in RTL for any size/columns/rows (property), week column, event dots ≤ 3, text shrink-to-fit, map projection padding/clamping/NaN · R: PainterRenderingTest (7) — Farvardin 1405 selection on the mirrored half in RTL, holiday tone, opaque vs transparent background, week numbers; Moon new/full/first quarter/waning/rotated; Sun only while up, on its side in LTR/RTL; map water/land/marker; size validation · S: PainterSnapshotTest — month fa RTL light / en LTR dark with week numbers, sun LTR/RTL/night, Moon 4 phases, map = 10 references · B: month bitmap 720×640 best of 20 ≈ 1.3 ms (< 8 ms; Robolectric unit timing test) (130 core:ui tests; painter lines 279/281, branches 68/68) | `BitmapPainter<M>.paint(model, w, h)` / `draw(canvas, …)` with Paints cached per instance (not thread-safe); palette from any Material ColorScheme; environment carries density, font scale, RTL, typeface; reuses T-701 geometry. Map thumbnail draws caller-projected shapes + marker only (no map data until T-1301). Month bitmap shows one secondary date, no shift label. No microbenchmark (would need a new dependency during the Maven outage) |
| T-703 | Motion & shared elements | DONE | — | main@9b8d98b, main@a559999 | U: TaqvimMotionTest (4) — durations follow the animator scale (0/negative/NaN = no motion), reduced motion yields no transition, RTL mirroring and push/pop reversal (property), shared keys equal by value and never colliding across kinds · R: MotionRobolectricTest (2) — system animator scale read; shared bounds applied inside SharedTransitionLayout and plain outside · UI: NavigationMonkeyTest — 500 random events (tabs, back, 12 taqvim:// links, 20 destinations, router callbacks), seed 703 (override with -Dtaqvim.monkey.seed): no crash, calendar at the bottom, exactly the top screen composed after every event | `TaqvimMotion` (Material 3 durations/easings, system animator scale), `NavigationMotion` shared-X-axis push/pop/predictive-pop transitions (RTL-mirrored, none when animations are off), typed `SharedKey` registry with `WithSharedBounds` (no-op outside an animated container, screenshots unchanged); NavDisplay inside SharedTransitionLayout. Only personal event chip → editor uses shared bounds (Day/Month keys exist but day-cell → details is not wired: the pager is re-created). Animator scale read per composition; ViewModel cleanup checked indirectly |
| T-800 | CalendarViewModel & use cases | DONE | — | main@720443c | U: CalendarViewModelTest (12, Turbine) — load today in every calendar with month and events; select day → month and events; month paging keeps the selection; today resets selection and follows the day change; explicit selection survives midnight; preferences change primary calendar, week start and pager offset; day events re-emit, tabs switch; debounced search runs once for the latest query; blank/closed search never searches; result opens its next day; effects (editor, event, snackbar for undated result); paging ignored before today is known · U: CalendarCalendarsTest (6) — unavailable/duplicate calendars, Islamic variant arithmetic, month offsets across years, today ticks only on day change, search trims and limits · U (app): CalendarAdaptersTest (2), TimesAdaptersTest (4) · Koin verify (feature:calendar 18 tests, lines 197/203, branches 56/60; app 8 tests) | Immutable CalendarUiState, sealed CalendarAction, CalendarEffect channel. Feature depends only on core via ports (settings, day events, search, today); `:app` adapts T-600/T-305/T-304 and wires eventsDataModule, calendarFeatureModule, timesFeatureModule (TimesSettingsSource emits null until a chosen city exists, T-1502). Search covers the official dataset only (personal events in T-804); next date searched this year and next. `schedulerModule`, location, ICS and backup modules not yet wired. Nepali skipped until T-105 |
| T-801 | Month pager | DONE | benchmark not run | main@41c189d, main@5d4cf77 | UI: CalendarScreenTest (5, Robolectric) — swipe 12 months forward and 24 back shows each month's title; tap selects a day; long press opens the editor on that day; week number opens the timeline at the week's first day; loading indicator · S: MonthScreenshotTest — 12 months of 1405 in fa (light RTL) and en (light LTR), 12 Gregorian months from April 2026 in ne (dark LTR; Bikram Sambat unavailable until T-105) = 36 references · U: MonthPageBuilderTest (6) — Farvardin 1405 grid, selection, secondary days, heading; every month of 1405; holidays/weekends, ≤ 3 dots, spoken summaries; week numbers across the year boundary; English heading spanning two Persian months · U: MonthLayoutTest (2) — grids start on the week start for every week start and ±24 months · U: CalendarMonthsTest (4, Turbine) — shown month ±1 loaded as six weeks, window moves with paging, event changes re-emit, settings, week-number effect · B: MonthPagerScrollBenchmark (24 months, FrameTimingMetric) written, not run (needs a device) (71 feature:calendar tests; lines 392/400, branches 121/131; `:app` tests incl. Koin verify green in a clean worktree) | `HorizontalPager` over ±1 200 months, ±1 page composed ahead; page models built on `Dispatchers.Default`; events of the shown month ±1 via the new `CalendarMonthSource` port (adapted in `:app` over `EventsRepository.days`); selection cross-fade 150 ms. Week numbers count from the first day of the primary calendar's year (not ISO; PLAN undefined). Shift labels empty until shift data is wired; T-802 day details and T-803 toolbar not yet on the screen. No year limit (main@5d4cf77): `MonthPages` pages one month each way up to `CalendarLimits.MAX_MONTH_OFFSET` (Int.MAX_VALUE/2); go-to-date offers every paged year |
| T-802 | Day details tabs | DONE | — | main@55ea31e | U: DayDetailsCalculatorTest (8) — Nowruz 1405: 3 days from 27 Esfand, day 1 of week/year, spring day 1 of 93, Sun in Aries; negative distances; southern seasons swapped (Sydney); new moon at the 2026-08-12 solar eclipse, full moon at the 2026-08-28 lunar eclipse; Tehran 10:30 next time Dhuhr; polar day without times; string resource for every enum value · U: DayDetailsViewModelTest (4, Turbine) — tabs follow selection, place, clock and day; source tooltip open/close/clear; citation → OpenUrl; minute ticks · UI: DayDetailsScreenTest (9, Robolectric) — each tab; every chip described with its source; official event shows its citation and opens the source; empty day offers "Add event"; next time announced as selected; no place; loading · S: DayDetailsScreenshotTest — 3 tabs × light/dark × en LTR/fa RTL = 12 references (104 feature:calendar tests; lines 744/765, branches 190/213; `:app` 10 tests green) | Calendars: day in every calendar, distance (days + y/m/d when ≥ 1 month), week and season rings (Persian-month seasons), Sun/Moon tropical signs and Moon phase at the place's local noon. Events: chips; official → inline source tooltip with first citation; others → OpenEvent. Times: place and method, SunArc, 8 times, next primary time within the selected day. New ports `CalendarPlaceSource` (null until T-1502) and `NowSource`. Opening device events needs the app navigation handler (not built yet) |
| T-803 | Toolbar & menu | DONE | — | main@a84ee78 | U: CalendarMenuTest (5, Turbine) — menu and dialogs; go to date selects the clamped primary-calendar day; week numbers and secondary calendar stored and followed; failed store → "not saved"; search, shift work, planetary hours and print effects · U: MonthPrintDocumentTest (3) — Persian month prints RTL with 7 headers, every cell, only the month's events with holiday style and escaping; empty English month · UI: CalendarToolbarTest (6, Robolectric) — today, search, shift work, planetary hours, print month, week-number toggle, secondary calendar dialog, go-to-date picker (131 feature:calendar tests; lines 930/958, branches 272/305) | Title = shown month with its span in the other calendars; today/search/more. Menu: go to date (±100 years), shift work (effect only — no shift feature yet), print month (HTML → PrintManager), planetary hours (effect to astronomy), week numbers and secondary calendar via the new `CalendarDisplayStore` port (secondary = reorder of `calendars`; week-number preference field pending in T-1500). `:app` must bind `CalendarDisplayStore` (AppModuleTest fails until bound; requested). `CalendarDisplayStore` bound (main@2eeb12a), week numbers stored (main@bf95948); planetary hours opens astronomy without the dialog (AstronomyRoute has no initial dialog yet). Planetary hours now opens the Astronomy dialog for the selected day (main@609b23f) |
| T-806 | Adaptive layout (F-13) | DONE | tablet emulator run pending | main@a84ee78 | U: CalendarLayoutTest (4) — stacked below 600 dp, two panes from 600 dp, tabletop at any width; picker range and month names · UI: CalendarAdaptiveTest (3, Robolectric) — side by side on a 1280×800 tablet configuration, stacked on a phone, selection kept while resizing 400 → 900 → 400 dp · S: CalendarAdaptiveScreenshotTest — phone/fold/tablet × en light LTR / fa dark RTL = 6 references | Material 600 dp breakpoint; two independently scrolling panes; tabletop posture (month above the fold) from material3-adaptive 1.3.0. PLAN "UI on tablet emulator" covered by a Robolectric tablet configuration; emulator run pending (instrumented CI) |
| T-804 | Search screen (F-12) | DONE | — | main@de0aacf, main@2ecb37a, main@1413bc2, main@6be22a5, main@a4243f3 | UI: SearchScreenTest (4, Robolectric) — typing "نور" through the route shows Nowruz and opens it (1 Farvardin 1405); grouped headings, go-to-date, filters, clear; recent/idle/searching/no-results states · U/P: SearchMatcherTest (6) — ي/ی, ك/ک, ZWNJ, spaces, case and Persian/Arabic digits match; typos only for queries ≥ 4 characters; highlight ranges; property: any substring matches, variant spellings share a key · U/P: SearchRankerTest (5) — "نور" ranks Nowruz first; group order; synonyms find settings/tools; duplicates keep the best match; property: results independent of input order, unique keys, ≤ limit · U: SearchDatesTest (5) — fa/en written dates offered as a day, words/ranges/long text not offered, recent store · U: SearchViewModelTest (5, Turbine) — debounced typing gives one search, filters, failing event source still finds tools/settings, open result records the query, language change · S: SearchScreenshotTest — results light/dark × en LTR/fa RTL, fa recent, en no results (31 tests; lines 368/376, branches 148/160) | Events, tools and settings in one grouped list; own `SearchMatcher` on T-203 keys (ranking exact > prefix > word prefix > substring > typo ≤ 1–2 edits; holidays then title as tie-breaks) because core `EventSearchIndex` ranks events only; filters All/Events/Tools/Settings (PLAN names no other filters); "go to date" when the query is mostly one date (T-500, confidence ≥ 0.7); 250 ms debounce; recent queries in a session store until a preference exists (T-1500). Ports SearchSettingsSource/SearchEventSource/RecentQueriesStore; wiring requested from the navigation-shell work. Wired in `:app` (main@62f5a25: search adapters over official index, personal events, device and subscription caches) and reachable (main@358eabb); search history from UserPrefs (main@bf95948). Day results and official events open their day; `SearchRoute(initialQuery)`; BACKUP/PRIVACY entries open their screens (main@a2c4179, main@609b23f). B12 fixed (main@2ecb37a, main@1413bc2, ADR-0031): search dates all-day external events by their own dates and timed rows by the same rule as the calendar; an event still running opens on today. I08 search bugs fixed (main@6be22a5): personal results are dated like the calendar (`PersonalEventDays`, ADR-0031) and a started recurring series returns its next occurrence, honouring overrides and cancellations; both pinned contract tests enabled (app: 204 tests, none skipped). I06 (main@a4243f3): search settings carry the device zone, so a zone change re-runs and re-dates results |
| T-805 | Year view | DONE | macrobenchmark not run | main@6a47034, main@803842f, main@5d4cf77 | U: YearViewModelTest (7, Turbine) — today's year in the first calendar with day flags; next/previous/chosen/today years (limit 3000); calendar switch keeps the same days; zoom 1–4 per row; holiday changes re-emit; settings changes re-resolve calendars; month opens through an effect · U: YearCalendarsTest (4) — available calendars (Nepali skipped), 1405 = Nowruz 2026 to the day before Nowruz 1406, month starts in all three calendars, year limits and Islamic variant · U: YearPageBuilderTest (6) — every month of 1405 starts on its weekday, today/holiday/weekend tones and summaries, headings with the other calendars' years, numbered months where names are missing, year page built under one 60 Hz frame (best of 10) · UI: YearScreenTest (6, Robolectric) — tap month → calendar at its first day; next/previous/today; swipe and tabs switch calendar; year selection; pinch zoom and accessibility zoom actions; loading · S: YearScreenshotTest — 1405 fa RTL light/dark, 2026 en LTR light/dark, en year selection = 5 references (28 tests; lines 331/337, branches 112/121) | One page per available calendar showing its year of the same anchor day; year controls + 1–3000 selection grid; discrete pinch zoom 1–4 mini months per row (also accessibility actions); canvas mini months with cached text layouts, one button each; page models on `Dispatchers.Default`. Own ports (Konsist forbids feature→feature, so calendar selection duplicates `CalendarCalendars`). Neighbouring calendar pages show only the language weekend until selected. PLAN "B" covered only by a unit timing test. Wired in `:app` (main@189deba). Remaining: navigation (T-803). Reachable in the app via the navigation host (main@358eabb, ADR-0015). Year month opens the calendar on that month (main@609b23f). B (main@803842f): YearViewBenchmark (open from the More tab — no `taqvim://` link —, 5 years next and back by the tagged year controls, calendar swipes with grid flings; FrameTimingMetric) — compiles, not run (no device); YearScreenTest drives the new YEAR_NEXT_TAG/YEAR_PREVIOUS_TAG. No year limit (main@5d4cf77): every year of each calendar (`CalendarLimits.years`), selection lists ±1 000 years around the shown year |
| — | Navigation host (PLAN §3.1 `:app`, no task ID) | DONE | main@358eabb | U/P: AppBackStackTest — push/pop/replace top-level, deduplication, saveable JSON round trip (property) · U: AppRouterTest — every feature callback → destination or intent · R: AppShellTest — tabs, More → Year, back, link and device-event intents · S: AppNavigationFrameScreenshotTest — 4 references (83 `:app` tests; assembleDebug, licenseCheck 551 modules 0 violations) | ADR-0015: serializable `AppDestination`, pure `AppBackStack` saved with `rememberSaveable`; Navigation 3 `NavDisplay`; `NavigationSuiteScaffold` (bar on phones, rail on tablets) with Calendar/Times/Tools/More; `AppRouter` maps feature effects (personal events → editor, device events → calendar app, citations → Custom Tab http(s) only). Fallbacks: CalendarRoute has no initial day, EventEditorRoute no initial date/time, AstronomyRoute no initial dialog. Deep links (ACTION_VIEW `taqvim://…`) not handled yet (T-1103) |
| T-900 | Timeline layout | DONE | — | main@912a2b6, main@a4243f3 | U/P: IntervalColoringTest (5) — 33-case table (disjoint, touching, overlapping, nested, chained, day edges) + overlaps never share a column, cluster width = deepest overlap, input-order independence · U/P: TimelineGeometryTest (7) — zoom 0.5–2×, 15-min snapping, px↔minute round trip, drag box whole steps and direction-independent, move keeps length, resize keeps start · U: TimelineContentBuilderTest (6) — weeks from the week start (property), all-day/timed split, Tehran prayer lines in order, polar day none · U: TimelineViewModelTest (7, Turbine) — week of today with placed events, navigation, zoom limits, draft move/resize/confirm/cancel → CreateEvent, place and event changes, OpenEvent effect · UI: TimelineScreenTest (6, Robolectric) — long-press drag creates 09:00–10:30, resize handle and accessibility actions, arrow/Shift/Enter/Escape keys, open event, modes and navigation, zoom actions and pinch, loading · S: TimelineScreenshotTest — week and day × fa RTL light / en LTR dark = 4 references (35 tests; lines 434/453, branches 157/183) | Greedy interval coloring by start in a custom single-measure Layout; all-day row; now line; dashed prayer lines at the chosen place; pinch and accessibility zoom; 15-min drag box with move/resize handles and keyboard/a11y; draft bar replaces the day controls. Bug found by tests and fixed: drag distances measured in root coordinates so snapping no longer lags the finger. Events crossing midnight must be split per day by the provider; week view is exactly seven days. Ports TimelineSettingsSource/TimelineDaysSource/TimelinePlaceSource/TimelineClockSource; wiring requested from the navigation-shell work. Wired in `:app` (main@62f5a25: settings, place, minute clock, events split at midnight in the device zone) and reachable (main@358eabb). I06 (main@a4243f3): spans and the clock follow a device zone change at once |
| T-901 | Month list & agenda | DONE | — | main@551aa61 | UI: AgendaScreenTest (6, Robolectric) — scroll 100 rows forward and back, both list ends request more months; mode, today, print, share, event and day actions; today selected, holiday and empty-month rows; loading; route + ViewModel navigation; share intent · P: AgendaWindowTest (4) — any load sequence keeps the window contiguous, ≤ 24 months, within ±1 200 and anchored at the end it grew · U/P: AgendaListBuilderTest (5) — Mordad–Aban 1405 month list (Persian names, digits, fa LONG date, 122 days); English agenda (event days + today, empty months, headers spanning Persian months); holiday/weekend; property: unique keys, day order, one header per month, today present iff its month is loaded (fa/en/ar) · U: AgendaExportTest (3) — shared text, printable HTML with direction/escaping · U: AgendaViewModelTest (6, Turbine) · S: AgendaScreenshotTest — agenda light/dark × en LTR/fa RTL, fa month list, loading (30 tests; lines 342/364, branches 125/142) | Lazy list anchored at today with month headers in the primary calendar and spans in the others; agenda (event days + today) or all days; months load 3 at a time at either end, ≤ 24 loaded, a century either way; rows built on `Dispatchers.Default`; print (HTML → PrintManager) and share (plain text). Month headers scroll (not sticky). Nepali falls back to Gregorian until T-105. Ports `AgendaSettingsSource`/`AgendaDaySource`; app wiring pending. Wired in `:app` (main@189deba). Remaining: navigation (T-803). Reachable in the app via the navigation host (main@358eabb, ADR-0015). Agenda day opens the calendar on that day (main@609b23f) |
| T-1000 | Events editor | DONE | — | main@34cc9a9, main@5d4cf77, main@a72f00c, main@e9b99ec, main@a9d2d73, main@f31a07c, main@60cca26, main@01ea742, main@a4243f3 | U: EventValidatorTest (8) — blank titles (property), title/notes/link limits, end before start (property), interval/count in Latin/Persian/Eastern Arabic digits (property), until before start, reminder count and range · U: EditorFormTest (4) — new event is all-day today in the first calendar; stored events round-trip through the form (property); rule parts the editor can't change are kept · U: EditorReducerTest (7) — calendar change keeps the same days incl. repeat-until (property); start move keeps length; repeat on/edit/off; reminders sorted and distinct; typed dates and ranges (T-500) · U: EditorPresenterTest (6) — errors only after save, change tracking, fa dates/times/digits, weekdays from the week start, reminder texts, picker month names · U: EventEditorViewModelTest (8, Turbine) — open new/stored/missing/unsupported/failing, validation then save, failed save retried, delete and discard, typed date, settings change relocalizes · R: EventEditorScreenTest (11) — create/edit/delete through the route, validation errors, discard confirmation, typed date, schedule, repeat, invalid-day policy, pickers, reminders, colors, loading/not-found/failure states · S: EventEditorScreenshotTest — new and detailed × light/dark × en LTR/fa RTL = 8 references (52 tests; lines 524/539, branches 261/293) · U/R/S (main@60cca26): OccurrenceEditTest (5), RoomPersonalEventStoreTest (+2), PersonalExpansionTest (+1), AppRouterTest (+1), EventEditorScreenTest scope dialog and occurrence form, 4 occurrence screenshots | Title, calendar (Persian/Islamic/Gregorian; switching keeps the same days), date by picker or typed phrase, all-day/timed, RRULE-lite repeat in the event's calendar with the ADR-0011 invalid-day policy (ordinal BYDAY, BYMONTHDAY and week start kept but not editable), ≤ 5 reminders, 8-color palette, notes, source link. Product limits: title 200, notes 4 000, interval ≤ 999, count ≤ 9 999, reminders ≤ 4 weeks. 24-hour time wheels. Ports `PersonalEventStore`/`EditorSettingsSource`; app wiring pending — `personal_events` has no source-link column (needs schema 3). Nepali events open as not found until T-105. Wired in `:app` (main@14c322a schema 3 `source_link`, main@189deba `RoomPersonalEventStore` + editor settings + official anchor lookup). Remaining: navigation (T-803). Reachable in the app via the navigation host (main@358eabb, ADR-0015); "new event on a day" and timeline drag-create open an empty editor (EventEditorRoute has no initial date/time yet). New event on a day and timeline drag-create open the editor on that day and times (main@a2c4179, main@609b23f). No year limit (main@5d4cf77): the editor picker offers every year of the calendar; `NumberWheel` shows a 3 000-value window of any Int range. B10 and B11 fixed (main@a72f00c, main@e9b99ec): system Back follows the same close rule as Cancel (discard dialog when there are changes, wait while saving; predictive back kept when unchanged); unsaved drafts survive process death in the entry's SavedStateHandle (EditorDraftCodec) and are cleared on save, delete or discard; EditorBackTest (5), EditorDraftCodecTest (3), 3 view-model restore tests. I04 (main@a9d2d73): editor save/delete use `attempt` (rethrows cancellation); a cancelled operation clears the busy state without an error; EditorCancellationTest (2). F02 (main@f31a07c, owner-approved): the editor previews the next ten occurrences from the calendar's own occurrence pipeline; RecurrencePreviewTest. F01 done (main@60cca26, ADR-0034 accepted in main@01ea742): opening a repeating occurrence asks "this occurrence" or "all occurrences"; one-occurrence saves and cancellations use the existing override tables (read by calendar, reminders and export); whole-series saves drop overrides the new rule no longer produces; scope and draft survive process death; calendar occurrences carry the id `eventId@day`. I06 (main@a4243f3): editor settings follow the device zone for today, the preview and new forms; an open form keeps the zone it was created with |
| T-1001 | Reminder notifications | DONE | — | main@a6b2edf, main@762c432, main@37ac1b2, main@a4243f3, main@7a90a76, main@9a152e5 | P: ReminderPlannerTest — random events in Persian, Gregorian and Islamic calendars (all-day/timed; Tehran, Berlin, Los Angeles; DAILY/WEEKLY/MONTHLY/YEARLY with every invalid-day policy; 0 min–4 weeks lead): reminders strictly after now, within 400 days, ordered, one per occurrence and reminder, ≤ 50, each lands on its occurrence's start · U: 30 Esfand yearly in a common year (SKIP none, NEXT_DAY 1 Farvardin, LAST_DAY 29 Esfand); monthly Islamic event from 1 Ramadan 1447; 09:30 Los Angeles daily keeps local time across DST and a skipped 02:30 moves to 03:00; all-day reminders at the chosen time; Nepali/past events give none · U: ReminderAlarmsTest (4) — personal ids positive, official negated; a fired alarm shows once; bounded history · R: ReminderNotificationsTest (5) — personal and official channels, texts ("In 3 days", "Today"), Done/Snooze actions, tap opens ACTION_VIEW taqvim://event/42; Done cancels; Snooze re-shows after 10 min; nothing without POST_NOTIFICATIONS (42 notification tests; lines 573/590, branches 245/314) | Pure ReminderPlanner (ADR-0011 expansion, 400-day horizon, nearest 50 alarms, replanned on every alarm); timed events in their zone, all-day at a chosen time (default 09:00, device zone). `ReminderAlarms` adapts to T-604 REMINDER source/delivery with a once-per-occurrence log. One channel per kind; Done only dismisses (no completed state); Snooze 10 min; `taqvim://` tap links are placeholders until T-1103. Pending: `ReminderSetupSource` adapter, all-day time preference, reschedule trigger on event/reminder changes, runtime POST_NOTIFICATIONS request. Personal reminders wired in `:app` (main@d6aaa4c). Still pending: all-day time preference, reschedule trigger on event/reminder changes, deep links. All-day reminder time stored in UserPrefs (main@56be204); reminder alarms recomputed when events, rules, reminders or opt-ins change (main@2a5b73a). Pending: settings row for the all-day time, runtime POST_NOTIFICATIONS request. All-day time setting (main@b516a9e); notification permission requested when a reminder is added (main@44911f6); reminder tap links handled by T-1103. Reminders skip exception days and cancelled occurrences and fire at an override's own day, time and title; replanned when either table changes (main@478c750). I07 (main@762c432): delivery is recorded only after the notification is posted (pending/delivered/failed in a shared DeliveryLog, old key-only entries read as delivered); snoozes go through the persistent `SnoozeScheduler`. Moved-occurrence reminder bug fixed (main@37ac1b2, ADR-0033 addendum): reminders are keyed `KIND:source#<original day>` so a moved occurrence and the regular one on its new day are both planned; old `@` keys still count as delivered for the occurrence the old planner kept; one alarm delivers every reminder due at the same instant and retries only failures; ReminderOccurrenceKeyTest (6). I06 (main@a4243f3): test that the reminder setup reads the zone at each recompute (the scheduler already recomputes on the time-zone broadcast). I01 (main@7a90a76): the reminder planner uses `OccurrenceSeries` and jumps to the planning window; 200 daily series from 1990 recompute in 91 ms (was 293 ms). A personal reminder fires as a notification through the device AlarmManager (main@9a152e5, DeviceReminderTest) |
| T-1002 | Official-event reminders | DONE | — | main@a6b2edf, main@d604af6 | U: OfficialRemindersTest (3) — next Nowruz across the Persian year boundary (late Esfand 1404 → 1 Farvardin 1405 = 2026-03-21; after it → 1406); titles in the language with Persian fallback; 3 days before Nowruz 1405 fires 2026-03-18 09:00 Tehran then moves to 1406; validity from 1406 skips 1405; Nepali none; Islamic New Year across 1447/1448 | Opt-ins attach to an EventId with 0–30 days lead through `CalculatorOfficialEventSchedule` over the T-300 OccurrenceCalculator (validity applied; astronomical rules without a source skipped). Stored ids negated to share the REMINDER alarm kind without a schema change. Pending: opt-in storage (Room table or UserPrefs field, plus backup), app wiring. Still pending: official opt-in storage (Room schema 4 + backup), planning wiring, deep links. Opt-in storage: Room schema 4 `official_reminders` + backup (main@56be204); planned with the user's Islamic variant (main@2a5b73a); `OfficialReminderStore` port bound. Pending: day-details opt-in UI in the Events tab. "Remind me" chips (same day, 1, 3, 7 days before) on official events in day details (main@20f4a79); backup preview row (main@b1aed91). B04 fixed (main@d604af6): official reminders compute each event with its source's own calendars, as the calendar does; OfficialReminderParityTest checks reminder days equal displayed days for every official event and Islamic variant, Nov 2025–Apr 2027 |
| T-1003 | ICS import/export & subscriptions | DONE | — | main@2a4a56b, main@7d38d7a, main@2ecb37a, main@c9b0223, main@9a3ff36 | R: IcsRoomTest (4) — fixture import into Room (3 created, repeated UID skipped, reader problem reported), duplicates skipped or replaced by UID, unreadable text stores nothing, export → import replaces without duplicates · R: SubscriptionRefresherTest (5) — changed feed replaces cache and stores ETag/Last-Modified, 304 keeps cache, validators dropped after 7 days, failures keep cache, bad URLs not fetched, only due enabled feeds refreshed · R: SubscriptionRefreshWorkTest (2) — periodic work needs a connected network, cancelled when network not allowed, retry only after transient failures · R: IcsDocumentsTest (2) · U: HttpIcsFetcherTest (2, local JDK HTTP server) — 200 with validators, conditional 304, 404, size limit (declared and chunked), read timeout, refused connection, invalid URLs · U: SubscriptionPolicyTest (5), IcsEventMappingTest (6), IcsExportMappingTest (5, incl. round trip and Persian NEXT_DAY rule as RDATE + X-TAQVIM-RECURRENCE), IcsOccurrenceExpanderTest (5, DST, EXDATE/RDATE, UNTIL, window clipping), TaqvimRecurrenceTest (3) · U: IcsRecurrenceDatesAndExtensionsTest (4) · migration test 1→2 (267 tests across core:ics, data:database, data:events, Konsist; licenseCheck green) · U (main@7d38d7a): imported UTC values keep UTC (2026-03-23T09:00Z stays 09:00Z after the European change), zoned values keep their zone, floating values stay device-zone; UNTIL keeps its time — cutoff before, exactly at and after the last start, a named-zone equality case, and an unchanged DATE cutoff · U (main@2ecb37a, main@c9b0223): IcsOccurrenceExpanderTest — old daily/weekly/monthly series, COUNT from DTSTART, EXDATE, an old instance overlapping the window, moved override of an old series, cancelled standalone and master; SubscriptionRefresherTest — pause and rename during a fetch survive 304 and 200, delete during a fetch writes nothing, overlapping refreshes run in order, a feed cancelling its events empties the cache, a 2020 daily series keeps all 432 rows across two full downloads | ADR-0013: RRULE only for Gregorian SKIP rules with Monday week start, otherwise RDATE list + `X-TAQVIM-RECURRENCE` (3 653 days / 1 000 dates). Room schema 2 (`personal_events.ics_uid`, subscription `last_modified`/`last_checked_at_epoch_millis`). HTTPS only (webcal → https); 5 MiB / 15 s / 30 s limits; WorkManager periodic refresh (Apache-2.0, already in catalog). Pending: subscription UI + its test and the network-allowed preference (T-1500); `icsDataModule` and worker factory not in `:app`; EXDATE/RECURRENCE-ID not stored for personal events, overrides ignored in subscriptions. Wired in `:app` (main@189deba): `icsDataModule`, WorkManager configured with the worker factory. Remaining: nothing calls `SubscriptionRefreshScheduler.update()` yet; network-allowed preference and subscription UI (T-1500). Subscription UI, its tests and the network-allowed preference delivered by T-1500 (main@3fc4d09, main@bf95948). Remaining: EXDATE/RECURRENCE-ID storage for personal events and override handling in subscriptions. Exception dates and overrides done (main@f4abfef, main@de60580, main@e3a58b8, main@478c750): schema 5 adds `event_exceptions` and `event_overrides` (cascade on event delete, migration 4→5 matches Room's 5.json); import stores EXDATE and RECURRENCE-ID/STATUS:CANCELLED components grouped by UID; export writes EXDATE values and override VEVENTs, ADR-0013 RDATE lists skip excluded days (addendum); subscriptions apply overrides; tests IcsOverridesTest, SeriesInstancesTest and PersonalExpansionTest (properties incl. Persian/Islamic rules), IcsExceptionsRoomTest round trip, IcsExportExceptionsTest. Cancelled overrides export as EXDATE. Still missing: per-occurrence editing UI (not in PLAN T-1003) and RANGE=THISANDFUTURE. B05 and B07 fixed (main@7d38d7a): import stored UTC starts as device-local wall clock (disagreeing with subscription expansion) and truncated a date-time UNTIL to its day, adding an occurrence past the cutoff. B01, B06 and B08 fixed (main@2ecb37a, tests main@c9b0223): expansion skips rows before the window instead of counting them (seek budget 100 000 candidates, output cap 1 000 in the window), refreshes write only fetch metadata and store rows with validators in one transaction (nothing written if the subscription was deleted; same-subscription refreshes serialised), cancelled standalone events and masters produce no rows. F03 (main@9a3ff36): refreshes record the failure kind and time and the feed reader's problem count; Room schema 7 (migration 6→7 on ics_subscriptions); SubscriptionHealthRecordTest (3) and migration test |
| T-1100 | Times tab & report | DONE | — | main@ea91bd3 | U: MonthlyReportTest (5) — one row per day for every month of Persian 1400–1410, Gregorian 2024–2026 and Umm al-Qura 1445–1447; HTML with `dir`/`lang`, Persian digits, escaping, polar rows · U: PrayerScheduleTest (7) — zone offset per day (Tehran, Oslo DST), display order, next time and progress (960 sampled instants, across midnight), polar day, localized HH:mm · U: TimesStateMapperTest (7) — no place, countdown and sun progress, expanded list, other days, after the last time, polar day, countdown without duration patterns · U: TimesViewModelTest (3, Turbine) — minute ticks, expand, day navigation, settings changes, report · R: TimesScreenTest (7) — next row announced as selected, countdown and sun path descriptions, actions, polar/missing-place/loading states · S: TimesScreenshotTest — today light/dark × en LTR/fa RTL, expanded, polar, no place (36 tests; lines 255/261, branches 75/87) | Expand/collapse, next-time countdown refreshed each minute, sun arc, monthly report as self-contained HTML printed through an off-screen WebView + `PrintManager` ("Save as PDF" from the system dialog; no PDF generator). Reads a `TimesSettingsSource` port. Countdown falls back to `H:MM` in local digits where duration patterns are missing (DT-009). fa LONG title fixed by ADR-0014 (main@51648bb, 3 screenshots re-recorded). Wired in `:app` (main@e39889d): settings from the stored place (T-1502). Remaining: navigation (T-803). Reachable in the app via the navigation host (main@358eabb, ADR-0015) |
| T-1101 | Athan settings | DONE | — | main@df5f30f, main@39f5e17 | U: AthanPreferencesMappingTest (5) — defaults identical for all 24 languages (every athan off); random settings round-trip through proto and serializer (property); stores before T-1101 read as defaults; stored oddities repaired (unknown/duplicate prayers, gaps clamped to ±60, volume 0–100, blank sound = default) · R: UserPreferencesRepositoryTest +1 — athan settings persist across DataStore instances · U: ReschedulePolicyTest — athan switch, vibration, sound, Iran time and chosen place changes recompute PRAYER alarms · U: AthanSettingsViewModelTest (5, Turbine) — Persian digits; exact-alarm warning only while an athan is on and exact alarms are denied; switches and gaps stored within ±60; picked sound adopted, unusable file flagged; volume stored once (debounced); preview play/stop/end/failure · UI: AthanSettingsScreenTest (6, Robolectric) — each control persists through the route; sound pick via document picker; exact-alarm banner opens ACTION_REQUEST_SCHEDULE_EXACT_ALARM; bypass disabled without Fajr · S: AthanSettingsScreenshotTest — light/dark × en LTR/fa RTL, fa with exact-alarm banner (45 data:preferences, 25 data:scheduler, 37 feature:settings tests; `:app` 30 tests green in a clean worktree) | Per-prayer switch and gap (−60…60 min); sound via SAF OpenDocument with persisted read access and preview (full playback T-1102); volume 0–100 %; vibration; DND bypass for Fajr only (stored; enforced in T-1102); "Iran time" UTC+03:30 toggle (stored; used by T-1102). UserPrefs field 14 (optional, no schema bump). Ports AthanSettingsStore/ExactAlarmAccess/AthanSoundLibrary/AthanPreview. Method/Asr/high-latitude pickers left to T-1500. T-604 reschedule matrix extended (place and athan changes → PRAYER). No bundled athan recording (DT-028). App wiring and backup field requested from the navigation-shell work. Wired in `:app` (main@62f5a25: store, exact-alarm access, sound library, 10 s preview) and reachable (main@358eabb); backup carries athan settings except the device-only sound (main@2599980). Pending: runtime POST_NOTIFICATIONS request and notification-policy access prompt. POST_NOTIFICATIONS requested when an athan is enabled; DND access banner for the Fajr bypass, re-checked on resume (main@44911f6) |
| T-1102 | Athan playback | DONE | OEM checklist not written | main@c5c1492, main@762c432 | P: AthanPlannerTest (5) — for random instants in 2026, gaps −60…60 and any set of enabled prayers, athans = A-10 times + gaps, strictly after now, ordered, one per prayer and day; after Isha the next is tomorrow's Fajr; a −60 min Fajr gap before midnight is planned; polar day (Tromsø) none; Iran time = UTC+03:30 · U/P: AthanPoliciesTest (3) — any claim sequence sounds each prayer at most once per day; bounded history; silent/vibrate/DND matrix with Fajr bypass only with policy access · U: AthanAlarmsTest (2) — alarm instants from the plan; a fired alarm plays once and calls the T-1103 hook; nothing without a place or on off-plan instants · R: AthanServiceTest (5) — foreground notification (silent channel, Stop and Snooze), MediaPlayer started and stopped; stops after 5 minutes and when the sound ends; silent mode mutes except allowed Fajr bypass; snooze re-arms 10 minutes later · R: AthanSessionTest (4) — picked sound at its volume with vibration, unreadable URI → default alarm sound, audio focus loss and end of sound interrupt · R: AthanPlatformTest (4) (23 tests; lines 311/326, branches 152/191) | Foreground service (mediaPlayback) with MediaPlayer on USAGE_ALARM; audio focus loss (e.g. a call) stops it; vibration; stop/snooze (10 min); silent mode and DND respected unless the Fajr bypass is on with notification policy access; ≤ 5 minutes; persistent once-per-prayer-per-day log; 2-day planning window replanned at each alarm. Heads-up (not full-screen) notification; no screen-off stop (PLAN does not ask). If Android refuses a background foreground start, the notification is posted without playback. No bundled recording (DT-028). Ports AthanSetupSource/AthanPlaybackStarter/AthanEventHook (T-1103 no-op); PRAYER AlarmSource/AlarmDelivery wiring and runtime POST_NOTIFICATIONS / policy-access prompts pending. Wired in `:app` (main@3947f77): AthanSetupSource from preferences, PRAYER AlarmSource/AlarmDelivery (qualified bindings). I07 (main@762c432): athan delivery recorded only after playback starts; athan snoozes are persistent scheduler alarms |
| T-1103 | Automation broadcasts (F-14) | DONE | — | main@a2c4179, main@609b23f | U/P: DeepLinksTest (5) — every documented link → destination, unreadable links → calendar, fuzz 1 000 arbitrary strings never throw, every Persian y-m-d link → exactly that JDN (property), selected text → first date / converter · R: AppIntentsTest (2) — VIEW taqvim:// and PROCESS_TEXT intents, parse context in device language · UI: DeepLinkShellTest (2) — day link opens the calendar on that day; unreadable link shows the calendar · R: AutomationBroadcastsTest (3) — ATHAN_STARTED extras, DAY_CHANGED at midnight with jdn/date and the next alarm, boot/zone change re-arm · U/P: DayChangeTimeTest (2) — next midnight in Tehran and São Paulo's skipped 2018 midnight; property over 5 zones · U: AutomationModuleTest (2) (104 `:app` tests; feature entry-parameter tests in calendar, events, astronomy, tools, search) | ADR-0016, docs/AUTOMATION.md. `taqvim://` links only navigate (calendar, day, event, occasion, convert, times, astronomy, search, settings incl. backup/privacy); limits 500 characters, Gregorian years 1–9999; MainActivity singleTop handles onCreate/onNewIntent. PROCESS_TEXT "Open in Taqvim" via T-501. Implicit broadcasts ATHAN_STARTED (T-1102 hook) and DAY_CHANGED (inexact midnight alarm, re-armed on start/boot/update/time/zone; may be minutes late); only runtime-registered receivers get them (Android 8+); always on (no PLAN switch). Entry parameters added: CalendarRoute initial day, EventEditorRoute new-event draft, AstronomyRoute planetary-hours entry, converter text, SearchRoute query, ToolsBoardStore. `automationModule` not yet included in appModule. `automationModule` included in appModule (main@d922348): athan broadcast hook active; `settings/about` link (main@a1ad8b6); day-change property test fixed for skipped days such as Samoa 2011-12-30 (main@845b46a). `taqvim://map` opens the world map (main@7af6cf6; ADR-0016 addendum) |
| T-1200 | Widget framework | DONE | — | main@163eed2, main@9a152e5 | U: WidgetUpdatePolicyTest (6) — each trigger reaches only dependent installed widgets (property); next day at local midnight in Tehran, São Paulo 2018 skipped midnight, Berlin 23-hour day; property: next day ≤ 25 h on the following date (5 zones, 1990–2040); wake-up is the earliest of midnight/prayer/minute, none without widgets · U/P: WidgetKindAndSizeTest (4) — 70n−30 dp buckets, largest fitting bucket (property) · U/P: WidgetConfigTest (4) — defaults per kind, normalization idempotent and in range (property) · U: WidgetRefresherTest (5), WidgetLinksTest (2), WidgetConfigViewModelTest (3, Turbine) · R: WidgetConfigScreenTest (4), WidgetGlanceTest (4, glance-appwidget-testing), WidgetPaintersTest (3), WidgetPlatformTest (4) — AlarmManager wake-up set/replace/cancel, update receiver with Koin, placing a widget re-plans, config activity cancels unknown ids (39 tests; lines 446/481, branches 163/188; licenseCheck 634 modules, 0 violations) | Glance base widget loading stored config + shared content per session off the main thread (process-death safe); responsive size buckets; themed frame; per-widget config screen via APPWIDGET_CONFIGURE; targeted updates of installed widgets only with one inexact RTC wake-up (refreshes when the device wakes); taps via ADR-0016 links; bitmaps via T-702 painters. Glance 1.2.0 (Apache-2.0). Config activity and GlanceRemoteViewsService allowlisted (ADR-0017). Config screen uses default theme until settings are wired. Parked during the 09:15–~17:00 Google Maven outage (branch removed after commit). Wired in `:app` (main@1515e02); widget configs in a separate Proto DataStore `widget_configs.pb` keyed by appWidgetId, removed on delete, excluded from backup (launcher-specific ids) (main@58373cd); update watcher redraws only affected widgets on preference, prayer, day and event changes. Flaky WidgetConfigTest property generator fixed (main@b0355f7); WidgetUpdateReceiver now resolves its refresher per broadcast and ignores broadcasts when Koin isn't running, so errors no longer leak into later tests (main@711e0bc, WidgetUpdateReceiverTest). All 12 widget providers bind, update and render on the managed device (main@9a152e5, DeviceSurfacesTest) |
| T-1201 | Widget: 1×1 date | DONE | screenshots, device tap tests and < 30 ms render benchmark pending | main@b976f33, main@1515e02, main@0439373 | G: DayWidgetsGlanceTest — day number and weekday, tap opens today · U: WidgetContentBuilderTest — fa/en titles, weekdays and digits · U: WidgetCatalogTest — registration receiver/widget match, exported receivers with provider metadata, Koin registration | Weekday, day number, secondary date, holiday tone; receiver exported with APPWIDGET_UPDATE and allowlisted (ADR-0017). Nepali falls back to Persian until T-105. Glance composition benchmark with a 30 ms budget, required in `benchmark/required.json` (main@0439373; not run on a device) |
| T-1202 | Widget: 4×1 date + clock | DONE | screenshots, device tap tests and < 30 ms render benchmark pending | main@b976f33, main@1515e02, main@0439373 | G: full date and secondary date, no events | Clock is a launcher-ticked platform TextClock inside Glance (no per-minute wake-ups); the TextClock itself is not asserted (Glance unit tests do not render it). Glance composition benchmark with a 30 ms budget, required in `benchmark/required.json` (main@0439373; not run on a device) |
| T-1203 | Widget: 2×2 date + events + next prayer | DONE | screenshots, device tap tests and < 30 ms render benchmark pending | main@b976f33, main@1515e02, main@0439373 | G: 2 events at MEDIUM and 4 at LARGE; holiday opens its day, personal event opens `taqvim://event/<id>`; next prayer opens times; no-events and no-place texts; content toggles; fa RTL text and digits · U: next prayer Asr at 13:30 Tehran, tomorrow's Fajr after Isha, none without a place · P: next prayer always later than now, at most one line marked (2026) | Events from EventsRepository, next prayer from A-10 at the stored place. Glance composition benchmark with a 30 ms budget, required in `benchmark/required.json` (main@0439373; not run on a device) |
| T-1204 | Widget: 4×2 with prayer strip | DONE | screenshots, device tap tests and < 30 ms render benchmark pending | main@b976f33, main@1515e02, main@0439373 | G: six times with the next one bold; one-row widget keeps only the strip; no-place text opens times | Fajr, Sunrise, Dhuhr, Asr, Maghrib, Isha at the stored place. Glance composition benchmark with a 30 ms budget, required in `benchmark/required.json` (main@0439373; not run on a device) |
| T-1205 | Widget: Month interactive | DONE | screenshots and device tap tests pending | main@b0355f7, main@a438f8c, main@5d4cf77 | G: previous/next/today steps kept per widget id; days open `taqvim://day`; holiday and weekend tones; secondary day at 4×4; RTL-mirrored controls; + opens `taqvim://event/new/<date>` · P: every month page starts on the week start and holds the whole month (74 widget tests) | Month state per widget in a private SharedPreferences file (cleared on removal; Glance state library not on the classpath). New link `taqvim://event/new[/<y-m-d>]` (ADR-0016 addendum). No year limit (main@5d4cf77): month widget offset up to `CalendarLimits.MAX_MONTH_OFFSET` |
| T-1206 | Widget: Month bitmap | DONE | screenshots and < 30 ms device render benchmark pending | main@b0355f7, main@a438f8c | G: T-702 month painter at the widget's pixel size; holidays, events and secondary days follow the widget's settings · B: best-of-5 render time under a loose 250 ms Robolectric budget | Device benchmark still required for the PLAN 30 ms budget |
| T-1207 | Widget: Week strip | DONE | screenshots and device tap tests pending | main@b0355f7, main@a438f8c | G: seven days from the user's week start with weekday initial, tones and event dot; each day opens its day | — |
| T-1208 | Widget: Schedule list | DONE | screenshots and device scroll tests pending | main@b0355f7, main@a438f8c | G: today and event days of the next 14 days as a LazyColumn; headers open the day, personal events the editor; empty message | — |
| T-1209 | Widget: Sun arc | DONE | screenshots pending | main@b0355f7, main@a438f8c | G: T-702 sun-arc painter with daylight progress and the next prayer; without a place asks for one and opens times · U: Tehran sunrise/sunset, polar night | Polar day/night at a chosen place now shows a no-sunrise/sunset message (main@711e0bc) |
| T-1210 | Widget: Moon | DONE | screenshots pending | main@08fa677, main@2aaa59b, main@711e0bc, main@5128b71 | G: SkyWidgetsGlanceTest — sizes, Persian digits, links · U: WidgetSkyBuilderTest (100 widget tests) | Phase disc flipped for the southern hemisphere; lit share; next full/new moon dated in the primary calendar; taps to `taqvim://astronomy`. Depends on LOCATION (hemisphere) |
| T-1211 | Widget: Map | DONE | screenshots pending | main@08fa677, main@2aaa59b, main@711e0bc, main@5128b71 | G: SkyWidgetsGlanceTest · U: WidgetSkyAdaptersTest (6) — outline loaded once and retried, 72×36 day/night grid, sun position, projection · U: ShadeAndRingPainterTest (core/ui) | Day/night world map with the chosen place via the map thumbnail painter (night shading added), refreshed every minute; taps to `taqvim://map`. Geometry from `:feature:map` passed through `:app` as plain points/darkness (no feature→feature dependency) |
| T-1212 | Widget: Countdown/age (F-09) | DONE | screenshots pending | main@08fa677, main@2aaa59b, main@711e0bc, main@5128b71, main@5d4cf77 | P: WidgetCountdownMathTest — Persian, Gregorian, tabular Islamic; 30 Esfand / 29 February move to the month's last day in common years · U: WidgetCountdownBuilderTest | Days/weeks until a date or years/months/days since it, in the date's own calendar, optionally yearly; settings offer date, calendar, title and upcoming official events (12 within 120 days); stored in `WidgetCountdownProto` (widget_configs field 6); progress ring painter added to core/ui. Countdown always opens its settings when placed. No year limit (main@5d4cf77): countdown years cover every year of the calendar |
| T-1213 | Persistent notification | DONE | device checks pending | main@bf20f3c, main@f080650, main@be04534, main@9a152e5 | notification module 71 tests (29 new): content (date, weekday, next prayer, other calendars, holidays, prayer strip), setting on/off, re-post only on change or when dismissed, daily refresh coordinator (earlier of next prayer and day change; reboot/update/clock/zone/locale receivers) | Low-importance silent ongoing notification, lock-screen visible, status-bar icon = today's number (cached bitmap), optional large-number badge (off by default); opens `taqvim://calendar`. No foreground service (not justified for a status line): one inexact non-waking alarm shared with the launcher icon. Personal-event changes refresh at the next wake-up. Main briefly failed the app audit/AppModuleTest between be04534 and a4e7d4b. Persistent notification posted and removed on the managed device (main@9a152e5) |
| T-1214 | Dynamic launcher icon | DONE | device checks pending | main@bf20f3c, main@f080650, main@a4e7d4b | R: LauncherIconTest — exactly one entry enabled, new enabled before old disabled, nothing written while off, DONT_KILL_APP · allowlist + release audit (35 new lines) | ADR-0022: 32 activity aliases (LauncherDefault + LauncherDay01…31; Latin and Persian digits generated by `tools/icons/launcher_day_icons.py`); off by default, turning on shows a warning; launcher limits documented (icon may move or drop, pinned shortcuts may be disabled, switch lands shortly after midnight, digits follow the system locale) |
| T-1215 | QS tile, shortcuts, live wallpaper, daydream | DONE | device checks pending | main@be04534, main@4db72a8, main@a4e7d4b, main@9a152e5 | R: TodayTileTest (onDestroy skipped: Robolectric shadow bug); shortcut intents parse through DeepLinkParser; wallpaper redraw scheduling (14 wallpaper tests); daydream attach/detach · app 157 tests incl. MemoryLeakTest, release audit, Konsist | QS tile (BIND_QUICK_SETTINGS_TILE) with today's date; dynamic shortcuts (today, new event, prayer times, converter) via `taqvim://` links, republished after icon switches; live wallpaper (BIND_WALLPAPER) month + Moon + day/night sky, drawing only while visible and redrawing on content change (1 min–24 h); daydream (BIND_DREAM_SERVICE) dim clock with dates, no burn-in shifting. Without a place the wallpaper uses 06:00–18:00 and an equatorial Moon. QS tile service and shortcuts resolve on the managed device (main@9a152e5) |
| T-1300 | Astronomy screen | DONE | — | main@e4c1222, main@5d4cf77, main@76c6ddd | U: AstronomyHeaderCacheTest (4) — Moon and signs once per hour, seasons and eclipses once per UTC day, separate places, LRU eviction; next season, total solar eclipse 2026-08-12 and partial lunar eclipse 2026-08-28 (NASA GSFC catalogs) · U: AstronomyStateMapperTest (6) — summer noon in Tehran (daylight, subsolar latitude, next season on both sides of the 2026 June solstice, Sun progress, picker date 1405-03-31), full moon 2026-08-28, Persian digits, polar day, phase-name sectors, sign positions · U: AstronomyDialogsTest (4) — horoscope with 12 cusps, angles and lots, polar null; year horoscope at the March equinox; Sunday planetary hours start with the Sun; tropical Moon in Scorpio 13–14 periods in 1405 · U: AstronomyViewModelTest (3, Turbine) · R: AstronomyScreenTest (10) — modes, header eclipses, actions, slider, dialogs, polar houses, loading and missing-place states · S: AstronomyScreenshotTest — Moon light/dark × en LTR/fa RTL, Earth light LTR, Sun dark RTL, no place (34 tests; lines 518/548, branches 99/129) | Earth/Moon/Sun modes; instant follows the clock until moved (±day, ±year in the settings' calendar, minute slider, date picker); animated Moon phase and Sun progress; dialogs: horoscope (Placidus cusps, angles, lots; no planets beyond Sun/Moon in `:core:astronomy`), year horoscope at the March equinox, planetary hours, Moon in Scorpio. Moon constellation shown as IAU abbreviation (DT-026). Reads an `AstronomySettingsSource` port; `astronomyFeatureModule` not yet in `:app`. Wired in `:app` (main@189deba, main@e39889d): settings from the stored place. Remaining: navigation (T-803). Reachable in the app via the navigation host (main@358eabb, ADR-0015). `AstronomyRoute(entry)` opens planetary hours for a day (main@a2c4179). Range (main@5d4cf77): Gregorian −9999…9999 — the limit is the astronomy engine (cosinekitty 2.1.19 season/eclipse searches fail from about year 12 147 and −10 722, and libration can stall far out), not data. Moon-in-Scorpio dialog states it is computed (main@76c6ddd, DT-013) |
| T-1301 | Map | DONE | DT-034 crescent-map comparison; globe frame time on a device | main@ce9356a, main@074ab4a, main@50b537b, main@e216e41, main@6abd6b9, main@ece6731, main@c8ee198, main@4a5c127, main@a8041a3, main@cbd40b6, main@76c6ddd, main@5ccbc69 | U/P: MapGeometryTest (6) — projection round trip (property), longitude wrap, screen↔map round trip with zoom 1–8 and pan inside the map (property), zoom keeps the point under the fingers, great circle to the Kaaba lies on the circle and ends there (property), antimeridian split · U/P: MapLayersTest (9) — subsolar latitude at the 2026 equinoxes (0°) and solstices (±23.44°) within 0.03°; Sun/Moon altitudes from sub-points match the ephemeris (property 2020–2030); March-equinox terminator through the poles; twilight classes; Moon up over half the world; declination and Yallop crescent grids; 1° day/night mask < 150 ms · U: WorldOutlineTest (3) — asset body matches its generator's SHA-256, 128 land rings / 333 boundary lines · U: MapViewModelTest (6, Turbine) · R: MapScreenTest (5) — layer chips, zoom/pick accessibility actions, tap to pick, slider and day buttons, states · S: MapScreenshotTest — day/night, Moon, crescent (Yallop, 2026-08-13), magnetic declination, Qibla + direct path, grid at 2× × light/dark × en LTR/fa RTL (35 tests; lines 493/504, branches 197/227) · U/P (main@ece6731): MapLineLayersTest — every time-zone and plate point on the map, globe hides exactly the far-side points (property), Iran band edge and Zagros boundary present; WorldOutlineTest hashes and counts (922/1 246); MapScreenTest chips, legend, 2012 note and CC BY attribution; 9 new and 14 re-recorded screenshots · G (main@c8ee198): OdehTable6Test (5) and YallopTest (8) — 578 Odeh and 295 Yallop observation records, computed zone/class vs what observers saw (Odeh zone A seen by eye 136/156, zone D seen by any aid 1/47; Yallop class A seen 154/166), fact-integrity checks, formula and threshold checks from the published constants · U (main@4a5c127): TimeZoneLayerTest (7) — band zones vs tz rules incl. July DST, boundary rule with unknown or absent zones, label formatting in Persian digits, overlay order, plate zoom filter; WorldOutlineTest asset formats and counts (128 bands, 60 with a zone, 986 boundaries, 1 247 plate lines, areas summing to Earth); tools/geodata/test_line_layers.py (8) · G (main@cbd40b6): OdehGeometryCrossCheckTest (2) — an independent Meeus implementation (test code only) agrees with the app on all 578 Odeh records: ARCV/ARCL within 0.015°, width within 0.001′, zone 576/578 | Plate carrée on Canvas from bundled Natural Earth 1:110m (no online tiles); layers: day/night with twilight bands, Moon visibility, crescent visibility (Yallop A–E), magnetic declination (platform WMM), grid, Qibla, direct path; pinch/pan and accessibility zoom; time slider; tap-to-pick effect. Masks are 2°/5°/10° grids off the main thread. Pending: magnetic inclination/field strength, 3D globe, Odeh criterion, crescent classifier vs 5 published crescent maps (DT-034), city markers, T-1502 map pick. Ports MapSettingsSource/WorldOutlineSource/MagneticModel. Wired in `:app` (main@7af6cf6): MapSettingsSource from preferences (language, stored place and its zone or device zone, primary calendar); World map in More and `taqvim://map`; a picked point is saved as a coordinates place after a confirmation dialog. Not in search (no MAP tool entry in `:feature:search`). World map findable in search via a MAP tool entry (main@77cf910); covered by the layout audit. Added (main@074ab4a, main@50b537b, main@e216e41): magnetic inclination and field-strength layers (platform WMM; MagneticLayersTest property: grids equal GeomagneticField per cell), city markers from the T-603 catalog via `MapCitySource` (12→120 with zoom, spaced, population order; tap saves the city after confirmation), orthographic globe on Canvas with drag rotation, zoom, all layers and markers (ADR-0024 deviation from PLAN's OpenGL ES/AGSL; MapGlobeTest), Odeh 2004 crescent criterion A–D alongside Yallop (OdehTest against Table V and eq. 2), map clipping and seamless shading fixes (78 map tests, 28 screenshots). Remaining: classifier vs published crescent maps (DT-034), globe frame time unmeasured. Added (main@6abd6b9): one "Pick <city>" TalkBack custom action per shown city marker on the flat map and globe (population order, following zoom, pan and globe side), emitting the same LocationPicked effect as a tap (MapScreenTest +2, MapViewModelTest +1). Fixed (main@73ed518): `wrapLongitude` could round to −180.00000000000003 (MapGlobeTest seed 5493680962570084356); values rounded past ±180° are folded back, tested on 40 neighbouring doubles each side of every half turn in ±20 turns. Time-zone (Natural Earth 10m, public domain) and tectonic-plate (Matthews 2016, CC BY 4.0) layers added (main@ece6731, DT-035); release APK 5 385 225 bytes. DT-036 closed (main@c8ee198): both crescent goldens hold observation facts only and the app computes ARCV/W/q; morning crescents added (Tb = Tr − 4/9 lag). Offsets computed from the device tz rules at the shown moment (main@4a5c127): stale 2012 offsets never displayed, boundaries hidden where neighbours now share an offset (253 of 986 in January, 279 in July), smaller plates revealed by zoom (< 90 000 km² ÷ zoom²), CC BY attribution drawn on the map; release APK 5 396 841 bytes. I09 (main@a8041a3): bands whose places now follow other offsets are listed at generation time and marked "*" at run time (23 of 128); offset labels keep their order in RTL; release APK 5 456 489 bytes. Crescent legend notes the values are computed (main@76c6ddd, DT-034) Licence conflict resolved (main@5ccbc69, ADR-0039): CC BY 4.0 is admitted for bundled **data** through a new `dataLicenses` section in `config/license/allowed-licenses.json` (attribution required), while a CC BY *dependency* still fails `licenseCheck`; no CC0 plate source exists (Bird 2003 and UTIG state no licence, fraxen/tectonicplates is ODC-BY, GEM is CC BY-SA, global_tectonics is GPL-3.0). The full Matthews et al. (2016) credit — authors, year, title, Zenodo link, licence URI, "as is" disclaimer and the modification made — is shown in About › Data sources next to a "Read the license" button, with the short credit drawn on the map itself; CLDR gained the Unicode notice. `DataSourceLicenseTest` (4) checks every shown licence against the allow-list and rejects any `-SA`/`-NC` id; `AllowListParserTest` +3. licenseCheck 658 modules, 0 violations; SBOM regenerated (244 library components, data deliberately excluded) |
| T-1302 | Compass | DONE | — | main@8a40fcf | U: AnglesTest (5) — wrap and signed-turn properties, filter across north, circular mean, 15° announcements with hysteresis (property) · U: RotationMathTest (7) — identity, azimuth about z (property), pitch/roll, headings from gravity + field, rotation-vector and gravity+field paths agree for tilted devices (property), degenerate readings · U: CompassStateMapperTest (6) — Tehran noon (Qibla, Sun, Moon, 24 h path), true north with declination, Qibla turn and alignment, magnetic without a place, Persian digits · U: CompassViewModelTest (4, Turbine) · R: PlatformSensorsTest (6) — ShadowSensorManager injection (rotation vector preferred, fallback, accuracy, unregister, gravity, missing sensors), WMM declination sign/size · R: CompassScreenTest (5) — TalkBack heading description, Qibla/Sun/Moon texts, calibration hint, orientation lock set and restored · S: CompassScreenshotTest — dial light/dark × en LTR/fa RTL, no place (51 compass tests; lines 500/576, branches 197/258) | Rotation vector with accelerometer + magnetometer fallback; circular low-pass; true north via platform `GeomagneticField` (WMM, no bundled coefficients); Qibla arrow with the turn to take; Sun/Moon azimuth ring (no planets: `:core:astronomy` has Sun/Moon only); animated 24 h Sun path; TalkBack live region every 15° with 3° hysteresis; natural-orientation lock while shown. App wiring pending. Wired in `:app` (main@189deba, main@e39889d): settings from the stored place. Remaining: navigation (T-803). Reachable in the app via the navigation host (main@358eabb, ADR-0015) |
| T-1303 | Bubble level | DONE | — | main@8a40fcf | U: LevelMathTest (5) — classification (scale-invariant property), flat/portrait/landscape tilt (property, face-down/upside-down), calibration per orientation, tolerance, ruler ticks (cm/in), localized decimals · U: LevelViewModelTest (3) — calibration persists across ViewModels, reset, free fall keeps last orientation · R: LevelScreenTest (2) — calibration persists across screens, portrait reading, ruler tab · S: flat light LTR, upright dark RTL, ruler | Orientation from the dominant gravity axis (PLAN "landing" read as lying flat); two-axis bubble flat, one-axis upright; per-orientation offsets through a `LevelCalibrationStore` port (needs a UserPrefs field); cm/in ruler from `DisplayMetrics.xdpi`; scrolls on small phones. Wired in `:app` (main@189deba): calibration in memory only (`SessionLevelCalibrationStore`). Remaining: persist in UserPrefs (T-1500), navigation (T-803). Reachable in the app via the navigation host (main@358eabb, ADR-0015); level offsets stored in UserPrefs (main@bf95948) |
| T-1400 | Converter, distance, duration, time zones, QR | DONE | — | main@6179966, main@9945456, main@5f1d9db | U: DurationExpressionTest (3) — 106-expression table: 78 values incl. Persian units/digits, decimals, nesting, ±100 000 days; 28 errors with positions · P: DurationPropertiesTest (3) — fuzz of grammar-alphabet text never throws and stays within limits; generated sums evaluate exactly; presenter parts/totals/digits · U/P: DateToolsTest (5) — written/relative dates in every calendar; distance days/weeks/Persian period/workdays (151 in 1405/1/1→6/22 with a Friday weekend); numeric dates convert back (fa, en) · U: TimeZoneBoardTest (3) — Tehran/Kabul/Los Angeles/Kiritimati offsets, day shifts, Persian digits, search · U: QrEncoderTest (3) — ASCII/Persian/link codes decode back with ZXing's reader, empty, too long · U: ToolsViewModelTest (3, Turbine) · R: ToolsScreenTest (11) — each tool, errors, loading, FileProvider share intent, PNG quiet zone · S: ToolsScreenshotTest — converter light/dark × en LTR/fa RTL, distance, duration, zones, QR (39 tests; lines 535/551, branches 265/289; licenseCheck 539 modules, 0 violations) | Own duration grammar (w d h m s in English and Persian). Converter and distance on the T-500 parser; workdays via a WorkdayCalculator port (≤ 3 660 days). Time-zone board with platform zone names, session-only zone list (persistence in T-1500). QR via ZXing core 3.5.4 (Apache-2.0), shared as PNG through FileProvider. Not done: Nepali (T-105), animal-year compatibility (DT-027). Known cosmetic issues: English tab labels ellipsized at phone width; RTL shows "25 + 1" as "۱ + ۲۵" (needs bidi isolation, T-1701). `toolsFeatureModule` not yet in `:app`. Wired in `:app` (main@189deba): workdays from the default profile. Remaining: stored board zones (T-1500), navigation (T-803). Reachable in the app via the navigation host (main@358eabb, ADR-0015); board zones read from UserPrefs (main@bf95948) but the tools screen has no port to save edits yet. Board edits persisted through `ToolsBoardStore` once `automationModule` is included; converter accepts initial text (main@a2c4179). Board zone edits persisted via `ToolsBoardStore` (main@d922348). Duration expression laid out LTR and tool tabs scroll (main@0949d6c). I05 (main@9945456): only the selected tool is computed, on an injected computation dispatcher with `mapLatest`; a 102 610-character paste (548 ms) and a 300-character QR code (61 ms) no longer run on the main thread (longest main-thread task 1 ms, ToolsMainThreadTimingTest). I06 (main@5f1d9db): follows device time-zone changes through the shared `DeviceTimeZone.changes()` stream |
| T-1500 | Settings screens | DONE | — | main@33060a6, main@3fc4d09, main@a9d2d73, main@9a3ff36 | U: AppSettingsMappingTest (5) — defaults identical for all 24 languages (week numbers off, ancient Iranian festivals off); random settings round-trip through proto and serializer (property); stores before T-1500 read as defaults; stored oddities repaired · R: UserPreferencesRepositoryTest +1 — app settings persist across DataStore instances · U: BackupCodecTest +1 — settings round-trip; recent searches and level offsets never written and kept on restore; older documents read as defaults · U: SettingsCatalogTest (29 dynamic + 4) — every item reads back what it writes, links open distinct pages, labels cover every value, main/secondary calendar reordering, settings search across word starts, ZWNJ and letter variants · U: SettingsHomeViewModelTest (5, Turbine) — rows, switches, choice and multi-choice dialogs (weekend never empty), links → effects, clear recent searches, deep link opens its tab highlighted · UI: SettingsHomeScreenTest (3, Robolectric) — table-driven: each of the 25 items changes its stored value or opens its page; search reaches other tabs · U/UI: SubscriptionsViewModelTest (4), SubscriptionsScreenTest (3) — address validation, add/duplicate/failure, refresh, pause, remove, network switch · S: SettingsScreenshotTest — 4 references (85 feature:settings, 51 data:preferences, 33 data:database tests) · U/R/S (main@9a3ff36): SubscriptionHealthTest (4), SubscriptionHealthScreenTest (3), SubscriptionHealthAdapterTest (3), 2 new and 3 re-recorded screenshots | Three tabs + settings search + deep link with highlight; 25 table-driven items over a `GeneralSettingsStore` port (language, theme extras, digits, main/secondary calendar, week start, weekend, Hijri method, week numbers, event sources, subscriptions + network, recent searches, persistent notification, prayer method, Asr, high latitude; links to Location, Athan, Subscriptions, Widgets). One optional `AppSettingsProto` (UserPrefs field 15) also holds T-803 week numbers, T-1400 board zones and T-1303 level offsets; backup carries all but recent searches and level calibration (device-only). Language choice stores the code only (defaults in T-1501); English tab labels ellipsize at phone width. App wiring pending. Wired in `:app` (main@bf95948): GeneralSettingsStore, SubscriptionsStore, stored values consumed by calendar, events, times/timeline/astronomy/athan, theme, level, tools board, search; settings home and subscriptions routes reachable. All-day reminder time stored (main@56be204) but no settings row yet (needs a time-choice control). All-day reminder time row with a time-of-day control, half-hour steps (main@b516a9e). Subscriptions network default changed to off for new installs by T-1804 (main@f75f1f5). Event sources follow the language until the user changes them (`event_sources_chosen` in app settings, carried in backups; main@19deed9). Tabs scroll instead of ellipsizing (main@0949d6c). Language item now applies the new language's defaults to values not explicitly chosen (main@f749b4a). I04 (main@a9d2d73): subscription add/refresh/pause/remove failures show retryable messages and always clear busy state; SubscriptionsFailureTest (3). F03 subscription health page (main@9a3ff36, owner-approved): per-feed status (paused, failed, never downloaded, stale, downloaded) and details — last success, last check, next check, cached event count and span, unreadable items, last failure and its reason — in the language's calendar and digits |
| T-1501 | Language & first-run | DONE | — | main@c4273e2, main@fe9096e, main@f749b4a | U: LanguageSwitchTest (5) — 24×24 defaults; changed values kept (property); chosen values survive round trips; unknown → en; first run per device language fa/en/ne/ckb/ar · U: DeviceLanguagesTest (2) · U: BackupCodecTest — onboarding flag device-only · U: OnboardingViewModelTest (4, Turbine) · R: OnboardingScreenTest (3) · S: OnboardingScreenshotTest (9, light/dark × LTR/RTL + large text) · U: OnboardingAdaptersTest (5) · R: AppLocalesTest (3) — LocaleManager round trip, pre-13 path (stored tag, locale context, RTL), locales_config = language table · R: OnboardingShellTest (5) — fresh install → onboarding → pick language → exact defaults → skip → calendar; recreate goes straight to the calendar (63 preferences, 42 database, 125 settings, 170 app, 43 Konsist tests) | ADR-0023: explicit choice = value differing from the current language's default (kept on language switch; values equal to the default follow the new language; event sources follow `eventSourcesChosen`); device language matched by language+region, language, primary (fa-AF → prs, ku → kmr, else en); stored language is the source of truth, LocaleManager on 13+, stored tag + wrapped context before; app bundle language splits disabled. Onboarding is an app-shell gate (not a destination) with three skippable pages (language, location, event sources — no notifications step), device-only completion flag, not reopenable from settings. StartupBenchmark's first fresh-install cold start measures the onboarding screen |
| T-1502 | Location settings | DONE | — | main@4d7cae6, main@5980f0f, main@a9d2d73 | U: ChosenPlaceMappingTest (5) — city/device/coordinates places round-trip through proto and serializer (property); unusable stored values (unknown source, out-of-range or NaN coordinates, blank/unknown zone) read as no place; validation · R: UserPreferencesRepositoryTest +1 — place persists across DataStore instances · U/P: CoordinateInputTest (4) — displayed coordinates parse back in every digit script (property); ranges and malformed text; Arabic decimal separator; zone ids · U: LocationSettingsViewModelTest (7, Turbine) — stored place in the language's digits; debounced search keeps port ranking; choosing a city stores it; device fix stored with described name and zone; denied/disabled/timed-out/unavailable store nothing; typed coordinates validated and stored · R: LocationSettingsScreenTest (7) — city, device and coordinates paths persist through the route; permission-denied explanation; errors; loading · S: LocationSettingsScreenshotTest — 4 references (39 data:preferences tests, lines 91/92, branches 34/36; 22 feature:settings tests; `:app` 10/10 in a clean worktree) | Place stored in UserPrefs field 13 (optional message, no schema bump). Feature depends only on core via ports LocationSettingsStore, CitySearch, DeviceLocation, PlaceDescriber, TimeZoneIds; location permission requested only on "Use my location". Coordinates and zone ids forced LTR; Arabic comma with Arabic-script digits. Not done: districts (DT-021), map pick done (main@fb41c74, main@e216e41); backup does not yet carry the place. App wiring pending (replaces `ChosenCitySource` for Times/Astronomy/Compass). Wired in `:app` (main@e39889d): store, city search, device fix, place describer; the stored place feeds Times, Astronomy, Compass and Calendar (replaces `ChosenCitySource`). Remaining: navigation (T-803). Reachable in the app via the navigation host (main@358eabb, ADR-0015). Map pick: the Coordinates page's "Pick on the map" opens `AppDestination.MapPick`; a confirmed point is saved and the map closes (LocationSettingsScreenTest +2, MapPickFlowTest). I04 (main@a9d2d73): city search, place save, device lookup and description failures show retryable states, cancellation returns to idle; LocationFailureTest (4) |
| T-1503 | Backup/restore UI & privacy dashboard | DONE | — | main@50d2730, main@e6451ed | U: BackupViewModelTest (7, Turbine) — encrypted export carries the passphrase once and wipes it; plain export sends none; unwritable destination; short passphrase never leaves; busy and cancelled exports still wipe every array; plain backup → preview (fa creation day, language, rows per table) → restore only after confirmation; encrypted backup asks for the passphrase, retries after a wrong one, every attempt wiped; every read/restore failure shown · U/P: PassphrasesTest (4) — strength hint (length and character kinds incl. Persian letters/digits), ≥ 8 characters typed twice, copies wiped (property), a label for every failure/table/strength/data/permission · U: PrivacyViewModelTest (3) — data kinds and permissions listed, clearing only clearable kinds after confirmation, failure shown, permissions re-checked · UI: BackupScreenTest (6, Robolectric) — encrypted export through CreateDocument clears both inputs; plain export and restore round trip through OpenDocument with confirmation; encrypted import prompt and retry; all 10 failure messages · UI: PrivacyScreenTest (4) — clear after confirmation, failed clear explained, each permission row opens its system settings intent · S: BackupScreenshotTest — export, preview, privacy × light/dark × en LTR/fa RTL = 6 references (30 tests; lines 497/505, branches 212/235) | Export via SAF CreateDocument (plain JSON or passphrase); import via OpenDocument with passphrase prompt, preview and overwrite confirmation; typed failures. Passphrases in memory only (never rememberSaveable; cleared on handover and dispose) and passed as CharArrays wiped when the operation ends or is cancelled. Strength hint is length/character kinds, not an entropy estimate. Privacy dashboard: stored data and where; clear location, device calendar copy, diagnostics, recent searches (personal events/reminders/subscriptions managed in their own screens); permission status with system settings links. Ports BackupOperations, BackupLanguageSource, PrivacyDataSource, PermissionStatusSource; DI and destinations requested. Wired in `:app` (main@cb576a0: BackupOperations over T-605 + SAF with 64 MB read cap, language source, PrivacyDataSource via Room PrivacyDao, PermissionStatusSource) and reachable from More, search and `taqvim://settings/backup\|privacy` (main@609b23f). Backup preview does not count official reminders (BackupTableKind lacks them). Backup preview lists official reminders (main@b1aed91). B13 fixed (main@e6451ed): the privacy dashboard requires notifications to be enabled on every Android version and POST_NOTIFICATIONS from API 33 |
| T-1504 | About, licenses, diagnostics & report | DONE | — | main@803d63a, main@d9fb503, main@b70b58f | P: DiagnosticsRedactorTest (5) — random Persian titles in quoted and key=value forms never survive; coordinates in Latin/Persian digits keep ≤ 2 decimals; tokens in https/webcal/content links and e-mail addresses removed; passphrase/password values removed; long digit runs masked · U: ProblemReportTest (3) — app/device/Android/language facts plus redacted diagnostics, newest 200 entries, no address → share · U: LicenseCatalogTest (4) — asset sorted, unique, every license text bundled; asset equals the RUNTIME set of the license gate report; inconsistent catalogs rejected · U: AboutViewModelTest (4, Turbine) · UI: AboutScreenTest (9, Robolectric) — facts/links/pages, license texts, data sources, diagnostics filter/copy/share; report intent built only after confirmation and without planted personal data; clipboard redacted · S: AboutScreenshotTest — 7 references (32 tests) | License list from a checked-in asset generated from the T-001 report by `tools/licenses/about_licenses.py` (`--check` for staleness; 267 runtime components; Apache-2.0/BSD-3-Clause/MIT/Unicode-3.0 texts copied verbatim). Data sources mirror PROVENANCE. Pattern-based redaction applies to shown, copied, shared and reported diagnostics (unquoted free-form titles would pass — diagnostics writers must use quoted/`title=` forms). No upload; report via the user's e-mail/share app. Cosmetic: Latin build type reorders in the fa version line. Ports AboutInfoSource/DiagnosticsSource; app wiring pending. Wired in `:app` (main@a1ad8b6): BuildConfig facts + app language, diagnostics from Room, About in More/search/`taqvim://settings/about`, CI `about_licenses.py --check`; links hidden until the project publishes URLs. In-app license list regenerated after the Glance/Wear dependency updates (main@4903c6e); LicenseCatalogTest green. fa version line checked correct by T-1701. Debug-only libraries kept out of the in-app licence list (main@d9fb503) The report now addresses `support@taqvim.app` (main@b70b58f): `AboutIntents.report` builds an `ACTION_SENDTO mailto:` intent with the redacted body when an installed app handles `mailto:` (a `<queries>` entry in `feature/about`), otherwise `ACTION_SEND`; both go through `Intent.createChooser`, so nothing is sent by the app itself. AboutScreenTest 11 (fake e-mail app: recipient set, no PII; share-sheet fallback; unhandled intent returns false) |
| T-1600 | Wear app | DONE | device run of the watch suite: the Wear system image cannot be downloaded here | main@4d9bcf4, main@5d4cf77, main@5f1d9db, main@9a152e5, main@cf87a75 | U: WearTodayTest (5) — Nowruz 1405 fa date, holiday, other calendars, next prayer Dhuhr in Persian digits; after Isha → tomorrow's Fajr; property 2026: next prayer after now and ≤ 26 h; no place / Tromsø polar day → none; en Gregorian in the place's zone · U/P: WearMonthConverterTest (5) — Farvardin 1405 grid, offsets, every week start; converter wrap/clamp, steps stay valid, calendar switch keeps the JDN, 1405-01-01 = 2026-03-21 · U: WearSettingsModelTest (4) · R: WearTilesAndComplicationsTest (6) — month and next tiles (texts, no-city prompt), date / month progress / next prayer complications and previews · R: WearScreensTest (4) — round watch · S: wear_today, wear_month (fa round) · L: WearManifestTest (3) — exported components = allowlist with reasons, no implicit exports, no network/calendar/location permission (27 tests; lines 383/555) | ADR-0019: standalone watch — the Wearable Data Layer is Play services under the Android SDK License, outside ADR-0003, so no phone sync; own preferences (language, main calendar, prayer method, one of the 80 most populous catalog cities); today / month / converter / settings screens; month and next prayer+occasion tiles; date, month progress, next prayer complications; library permissions removed with `tools:node="remove"`; minSdk 30; encrypted-only Auto Backup. Compose for Wear OS 1.6.2, Tiles 1.6.2, ProtoLayout 1.4.2, complications 1.3.0 (Apache-2.0; licenseCheck 624 modules, 0 violations). Not done: UI smoke on a Wear emulator, tile screenshots (tile testing client times out under Robolectric; texts checked), release export audit for `:wear`; unused WorkManager components merged via `:data:events` (guarded, allowlisted). No year limit (main@5d4cf77): the converter steps through every whole year of the calendar; release APK 4 388 512 bytes (budget 8 MB, no Wear size task exists). I06 (main@5f1d9db): follows device time-zone changes through the shared `DeviceTimeZone.changes()` stream. Managed-device runs (main@9a152e5): phone smoke only — AGP managed devices offer no Wear image, so Wear stays manual Wear managed device added (main@cf87a75): `wearApi34` (Wear OS Small Round, API 34, `android-wear` x86_64) with 7 device test methods in 11 runs — `WearDeviceSmokeTest` (4 × fa/en, UiAutomator against resource-id test tags on every nav destination), `WearTileDeviceTest` (2; tiles bound through `DefaultTileClient`, answering a real tile request and inflated by the ProtoLayout renderer) and `WearComplicationDeviceTest` (1). One command: `./gradlew :wear:wearApi34DebugAndroidTest` (needs /dev/kvm). **Not yet run**: every `dl.google.com` path 404s in this sandbox, so `wearApi34Setup` cannot fetch `system-images;android-34;android-wear;x86_64`; the suite compiles and is wired, nothing more. Tile screenshots do not need the emulator — Roborazzi records `wear_tile_month/round_fa.png` and `wear_tile_next/round_fa.png` (454×454) from the real renderer, gated by `:wear:verifyRoborazziDebug`; the device test also writes both PNGs to the additional-test-output dir. Wear JVM tests 29 → 31. `instrumented.yml` excludes `:wear:connectedDebugAndroidTest` from the phone sweep (wear minSdk 30 would fail the API 26 leg) and the tests `assumeWatch()` themselves off non-watch devices. Still manual (checklist §5): adding a tile or complication to a watch face, tapping a tile, and time-zone/midnight updates |
| T-1700 | TalkBack pass | WIP | manual TalkBack device pass and sign-off pending | main@9511cd2, main@f02a7ac, main@7eaf900, main@abdf955, main@131eb76, main@9a152e5 | A11y: `AccessibilityAudit` (core/ui-testing; 11 tests with planted violations) runs on every screenshot state of core/ui, calendar, events, times, astronomy, compass, tools, year, agenda, timeline, search, settings, backup, about — 0 violations after fixes | Checks unlabeled actions/images, blank labels, unlabeled long-press, duplicate sibling labels, and < 48 dp targets whose widened touch area overlaps another target. Fixed: SegmentedTabs ≥ 48 dp (all tab rows), event color swatches 48 dp, invalid-day policy and search result rows ≥ 48 dp, distinct About source-button labels, polite live region on the Times countdown. Accepted exception: narrow overlapping week-view timeline blocks (each fully labelled; day view full width). Not covered: map, app shell, widgets, wear, notification content; plain (non-screenshot) screen tests don't audit automatically (`assertAccessible()` available). PLAN's manual 20-step TalkBack script per release still required. The stale about LicenseCatalogTest it found was fixed by regenerating the license list (main@4903c6e). Automated Accessibility Test Framework checks on the calendar, times, tools, More and all 11 More screens pass on the managed device (main@9a152e5, DeviceAccessibilityTest); the TalkBack-by-ear pass stays manual |
| T-1701 | RTL & font scale | DONE | manual pass pending | main@e55c0c0, main@8aa8a63, main@86b2592, main@74a3b42, main@0949d6c | L: `LayoutAudit` (core/ui-testing) runs in every captured screenshot — flags ellipsized, cut-off, over-limit or off-window text — enabled for core/ui and every feature module except widgets and wear · U: `BidiText` isolate helpers (core/i18n) checked with `java.text.Bidi` · S: large-text states (phone fa RTL and en LTR at font scale 2.0) for each captured screen (reduced matrix) | Fixed: day cells and top bar fit at 2.0 (font scale capped at 1.3 + auto-size; T-701, T-803); segmented tabs switch to a scrollable row when labels don't fit; number wheel, timeline, year and athan labels auto-size; timeline day header split into two single-line texts; tools duration expression laid out LTR so "25 + 1" keeps its order (T-1400); tool and settings tabs scroll instead of ellipsizing (T-1500); astronomy step buttons wrap. Checked: fa version line already correct (T-1504). Audit exceptions: chip and timeline event titles (intentionally truncated); auto-sized text still overflowing at its minimum size is not reported |
| T-1702 | Translations | DONE | optional human review of the 22 machine-translated languages; Weblate instance URL and per-language reviewers confirmed 2026-09-18 (main@b70b58f) | main@1e3c51b, main@b579b37, main@6e3a842, main@bc2e422, main@577c1f3 | L: TranslationKonsistTest (7) — placeholders match the source (`%%` literal), CLDR plural categories per language, no copies of English outside `config/i18n/same-as-source.txt`, no ASCII , ; ? in Arabic-script text, no bidi marks/embeddings/overrides (isolates allowed), pseudo-locales on in debug, planted violations detected · L: TranslationReportKonsistTest (2) — completeness per language and module, fa and en asserted 100 % · L: FarsiTranslationKonsistTest (existing) · L (main@577c1f3): konsist checks placeholders, CLDR plural categories, same-as-source entries, Arabic-script punctuation and bidi, and fa/en completeness; lintDebug with warningsAsErrors for app, wear and 17 feature modules | Report: `./gradlew :konsist:translationReport` (CI artifact). en and fa 1256/1256; the other 22 languages 0 %, to be translated by humans through Weblate per PLAN (docs/i18n/TRANSLATING.md) — no machine translation. 389 translator comments added (438 of 1225 English strings commented; app strings not yet). Owner: Weblate instance URL, reviewer per language, fa native sign-off. All 24 launch languages ship (owner directive 2026-09-17 allows machine translation): 1 352 entries × 22 languages, each file marked `<!-- MT: needs review -->`; `prs` derived from the reviewed Persian with Dari vocabulary; `.weblate` component config and the MT-review workflow in TRANSLATING.md; release APK 5 708 188 → 7 245 152 bytes (budget 8 388 608) |
| T-1800 | Baseline/startup profiles, R8, shrinking | DONE | device cold-start measurement (T-1801) | main@14d7fd0, main@3718b74, main@f7b87f2, main@1a79898, main@d9dd4da | build: `:app:verifyReleaseShrinking` — R8 mapping keeps back-stack and backup serializers, WorkManager worker name, proto field names (`shrinking-requirements.txt`) · build: `:app:checkReleaseApkSize` — release APK 4 246 558 bytes ≤ 8 MiB · B: StartupBenchmark cold/warm/cold-without-AOT (not run; needs a device) | ADR-0018. Found and fixed: R8 renamed Proto DataStore fields (protobuf-javalite ships no keep rules), which would have broken stored preferences in every release build — one keep rule added; none needed for kotlinx.serialization, Koin, Room, Compose, Navigation 3, WorkManager. Size/shrinking checks run in the PR build job. BaselineProfileGenerator written (cold start → calendar → month swipes → day details → times → search, with startup profile); profile not generated (needs an API 33+ device; on-demand benchmark.yml job); no hand-written profile. Dex layout optimisation waits for the Baseline Profile Gradle plugin (not cached during the Maven outage). Checked 2026-09-17: Google Maven returned 404 for every artifact (Maven Central fine) and the `androidx.baselineprofile` plugin is not in the local cache, so applying it (to :app and :benchmark, with dex layout optimisation and startup profile packaging) waits for Google Maven. Baseline and startup profiles generated on the Gradle Managed Device pixel6Api34 (main@f7b87f2, `./gradlew :app:generateBaselineProfile`, ADR-0018 addendum; baseline 33 287 rules, startup 28 493); release APK 5 673 847 bytes. Generating them exposed and fixed a launch crash on real Android (LanguageSpec regex rejected by ICU; `AndroidRegexKonsistTest`). Release-build crash fixed (main@1a79898): R8 full mode repackages classes, so the relative `Class.getResourceAsStream` calls in core/i18n, data/location and core/calendar read nothing — the minified app died at start with "languages.properties is missing". All three now read from the classpath root, and `ClasspathResourceKonsistTest` fails the build on any relative lookup in production code. Found by the macrobenchmark logcat on CI; no debug test could see it Size cut (main@d9dd4da): release APK 7 265 104 → 6 429 065 bytes (−836 039, 76.6 % of the 8 MiB budget) — `localeFilters` keeps the 24 launch languages plus the two debug pseudo-locales (libraries shipped 88; −462 884), `cities.tsv` stored column by column (−176 999), build-time metadata dropped (−130 901), map line layers delta-encoded (−42 323), fewer zip entries (−22 932); every value and geometry unchanged. `checkReleaseApkSize` warns from 90 % and fails above 100 % (`ApkSizeBudget`, 18 tests). The owner's 1.5 MiB target is not met: the remaining 1 564 338 bytes are the other languages' resources, removable only by AAB language splits, rejected because Play then installs only the device's system languages and in-app language choice (ADR-0023) would fall back to English — ADR-0018 addendum; the owner can flip it. Checked and not worth applying: string-pool dedupe already done (28 020 bytes of identical-to-default entries left, removing them breaks MissingTranslation and Weblate), shrinkResources already strips 1 239 entries, translator comments never reach `resources.arsc` |
| T-1801 | Macrobenchmarks | WIP | device baselines: jank, month-screen memory and Glance render budgets need a physical device | main@3718b74, main@e2d6c5c, main@51fab98, main@76d786f, main@0439373, main@2c8940d, main@5db8ef3 | B: StartupBenchmark (3), MonthPagerScrollBenchmark, ScreenScrollBenchmarks (timeline scroll, search typing), YearViewBenchmark (3, T-805, main@803842f) — compile only · U: test_compare_benchmarks (4) — > 10 % growth fails, ≤ 10 % passes, missing baseline recorded, missing results fail | Journeys via test tags as resource ids and `taqvim://` links (new `taqvim://timeline[/y-m-d]`, ADR-0016 addendum). Nightly benchmark.yml runs them on an API 36 emulator and fails on > 10 % regression against `benchmark/baselines` (none committed yet). Widget render (T-1200 blocked) and map mask (T-1301) benchmarks not written. Added (main@e2d6c5c): MapScreenBenchmark (taqvim://map pinch/pan, FrameTimingMetric) and `:benchmark:micro` widget render timings (month 4×4, sun arc, moon, progress ring, map thumbnail with outline and day/night grid; 30 ms budgets) and map mask grids (day/night, moon visibility; 150 ms budgets; crescent grid regression-only) in budgets.json, wired into nightly benchmark.yml. Widget timings use an interim in-house harness (10 warm-up + 30 timed, median, AndroidX JSON format) because Jetpack Microbenchmark could not be downloaded (Google Maven 404 again on 2026-09-15; ADR-0018 addendum). Glance composition measured by `GlanceWidgetBenchmark` (main@0439373; compile-only, not run on a device). Added (main@51fab98): the 8 wall-clock budget tests are tagged `TimingTest` (JUnit 5 tag / JUnit 4 category), excluded from every default Test task and run alone through `timingTests --max-workers=1` (one fork, no Kover), also in pr.yml's unit job — TextSnippetCorpusTest had failed the parallel `test` gate on load alone (108 ms vs 50 ms); ADR-0018 addendum. B14 fixed (main@76d786f): `benchmark/required.json` lists the 20 required benchmarks; `compare_benchmarks.py --required` (used by benchmark.yml) fails on a missing required benchmark or metric and on unknown results; without it, results missing from the baseline and duplicates fail; 16 Python tests. Generator split into startup and journeys (main@f7b87f2); CI timing budgets scale on runners (main@2c8940d). Benchmark run green on CI (main@df2558f, main@5db8ef3): `:benchmark:micro` applies the Compose compiler (the Glance timings had failed with NoSuchMethodError), the map, timeline and search journeys measure with a warm start (a cold start kills the process between setup and measurement, so they had measured an empty screen), `required.json` uses the metric names the library reports, frame journeys require `frameCount` (hosted emulators have no frame timeline), and budgets may be `physicalDeviceOnly` — the month-screen memory budget and the four Glance render budgets are skipped unless `compare_benchmarks.py --physical-device` is used (ADR-0018 addendum) |
| T-1802 | Compose stability | DONE | — | main@c5e943b | build: `composeStabilityCheck` per Compose module (reports via `-Ptaqvim.composeMetrics=true`, CI static job) fails on unstable composable parameters and *UiState/*Content/*Model classes · U: ComposeStabilityTest (4) — report parsing, exceptions with reasons · U: RecompositionTest (2) — DayCell recomposes once per change, skips unrelated state and equal models; moving the month-grid selection recomposes only 2 cells · U: RecompositionCounterTest | ADR-0021. First run: 15 unstable composable parameters (LanguageSpec ×9, IntRange ×3, CalendarDate, Coordinates, Glance Intent) and unstable UI models in ~12 features, all from immutable :core types compiled without the Compose compiler; fixed via documented `config/compose/stability.conf` entries. One exception with reason: Glance WidgetFrame click Intent. RecompositionCounter uses Compose trace hooks (no production hooks) |
| T-1803 | Memory & leaks | DONE | month heap budget not run: needs device | main@1418c78 | R: MemoryLeakTest (2) — MainActivity collected after onDestroy (verified failing without the fix), process-lifetime objects use the Application context · B: MonthScreenMemoryBenchmark (MemoryUsageMetric Max RSS), budget < 80 MB enforced by `compare_benchmarks.py --budgets` on the nightly emulator run (not run) · U: budget tests (2) | Found and fixed: MainActivity leaked through the day-change alarm PendingIntent built with the Activity context (diagnosed with a Shark heap analysis; `DayChangeAlarm` now uses the application context). Watchers take flows and stores, not Contexts; caches bounded. LeakCanary 2.14 debug-only (Apache-2.0; 2 debug activities allowlisted for debug builds; legacy storage permissions removed in the debug manifest). licenseCheck 640 modules, 0 violations |
| T-1804 | Security | DONE | — | main@5d933cf, main@f75f1f5 | L: ExportedComponentsTest (4) — merged debug unit-test manifest exports exactly the allowlist (`security/exported-components.txt`, reason per entry), no implicit exports, only documented permissions · build: `:app:auditReleaseExportedComponents` (in `check`) — release manifest vs allowlist · R: SecurityConfigurationTest (4) — cleartext refused and system CAs only; Auto Backup only end-to-end encrypted (API 26–27 none, 28–30 clientSideEncryption, 31+ disableIfNoEncryptionCapabilities); StrictMode only in debug; day-change receiver ignores spoofed/empty intents · Konsist: SecurityKonsistTest (5) — components declare exported, INTERNET only in data/events, immutable PendingIntents, dedicated FileProvider paths, planted violations detected · deep-link fuzz from T-1103 (120 `:app` tests; Konsist 43) | ADR-0017, docs/SECURITY.md. Found and fixed: no INTERNET permission was merged (subscriptions could not have worked on a device) and Auto Backup copied data unencrypted. Network only for T-1003 subscriptions, now off by default for new installs (stored choices kept). Exported: MainActivity, ProcessTextActivity, WorkManager/profileinstaller components guarded by system permissions, debug/test-only hosts. No FLAG_SECURE (not in PLAN; passphrases never saved). Not run: assembleRelease (needs CI signing secrets; release manifest covered by the audit task). Owner placeholders: security contact, report encryption key or channel, response time |
| T-1900 | Release engineering | DONE | signed build waits for the keystore secrets | main@63c2a68, main@f8759ce, main@c5bacfb, main@bcd23b4, main@dd8cd65 | L: actionlint (release.yml, release-dry-run.yml) · changelog parser simulation over git log (101 features, 5 fixes, 4 tests, 8 documentation, 3 build & CI, 68 bookkeeping commits skipped) | docs/RELEASE.md: semver + versionCode formula, tags/branches, pipeline, signing secret names only, changelog (cliff.toml), 7 release blockers, manual sign-offs (OEM/athan, widgets, TalkBack, RTL/font scale, translations, instrumented UI, backup, data), Play internal→closed→open→staged production, store metadata layout (described only), rollback. release-dry-run.yml: unsigned release build, exported-components audit, SBOM, changelog preview, clean-rebuild reproducibility check. release.yml: tag must equal versionName, audit gate, SHA256SUMS, draft/pre-release. Pending: first CI run (push not approved), Play account and store listing, data-only release path (T-1901), owner placeholders. Nothing is published without the owner's push approval. Version derived from the tag (main@f8759ce): `TaqvimVersion` in build-logic parses `vX.Y.Z[-beta.N]` and computes versionCode per RELEASE.md (30 build-logic tests incl. ordering property); `:app` uses `-Ptaqvim.version` (CI passes the tag) else `version.properties` (0.1.0 → 10099); `verify<Variant>Version` checks outputs; verified v1.2.3-beta.4 → APK 1.2.3-beta.4 / 1020304, rc tags rejected. Data-only releases: not applicable, dataset updates ship with app releases (ADR-0020) CI green (2026-09-18): PR, Benchmarks, Instrumented tests (API 26/30/33/36) and Release dry run all succeeded on main@dd8cd65 — runs 35327888666, 35327888908, 35327888637, 35327892318 (URLs in STATUS_REPORT §6) — tagged `v1.0.0-rc2`. Fixes: rc tags build unsigned instead of failing without secrets (main@c5bacfb); emulators cold-boot on every level because a resumed AVD snapshot came up without system services (API 26 hung to the 90-min timeout, API 30 failed in 2 min; main@bcd23b4, main@dd8cd65); the test step has a 60-min guard |
| T-1901 | Support system | WIP | — | main@c3416e4, main@30706f2, main@2e3eb8a | L: issue forms parsed and structure-checked (types, unique ids, labels, dropdown options) | Covered: `SUPPORT.md` (channels, what to include, P0 crash fix ≤ 48 h and holiday data error ≤ 72 h from PLAN, triage steps, privacy of reports matching the T-1504 redaction and 200-entry cap, fa/en); bug report form gains severity (P0/P1/P2) and the real in-app report path; data correction form states 72 h. Already done elsewhere: in-app problem report (T-1504), dataset correction form (T-003/D-09). In-app FAQ done (main@30706f2): 15 questions in 5 topics describing current behaviour (Nepali calendar and widgets answered as not yet available), search across letter/digit forms, answers link to their screen via documented `taqvim://` links, report-a-problem action; FaqCatalogTest 5, FaqViewModelTest 3, FaqScreenTest 5, 4 screenshots. Owner placeholders in SUPPORT.md (repository URL, support e-mail, extra answered languages, P1/P2 targets, first-response time, severity label names, stale-issue days) confirmed on 2026-09-18 (main@b70b58f). Data-only release path not built: owner decided dataset updates ship with app releases (ADR-0020, 2026-09-14). Data audit docs/DATA_AUDIT.md (main@6372553); support placeholders filled with defaults flagged TODO(owner) (main@2e3eb8a), then all 29 markers removed as confirmed and `support@taqvim.app` wired into the in-app report as an `ACTION_SENDTO mailto:` chooser with a share-sheet fallback (main@b70b58f, T-1504) |
| T-1902 | Beta program | WIP | docs only | main@63c2a68, main@407520d | — | docs/BETA.md: 2-week Play closed beta with -beta.N builds, opt-in steps, feedback via in-app report/SUPPORT.md, checklist from PLAN §8.1/§8.3/§9, exit criteria (14 days, crash-free sessions ≥ 99.9 % over the last 7 days from Android vitals — no crash reporting in the app, no open P0 or overdue data error, budgets met, sign-offs, fa/en 100 %). Pending: Play account and track, testers, devices, running the beta; nothing distributed without owner approval. docs/MANUAL_TEST_CHECKLIST.md with every device-only check (main@407520d) |

## Epic summaries

### Epic 0 — Foundation & Delivery Pipeline (completed 2026-09-13)

**Shipped:** ~45-module Gradle skeleton with build-logic conventions and latest-stable catalog (T-000);
dependency license gate with scope-aware allow-list, cache-first POM resolution and LGPL canary negative test
(T-001); Konsist architecture suite (T-002); PR / instrumented / benchmark / release workflows with signed
builds, changelog and CycloneDX SBOM (T-003); 8 custom lint rules active in every module (T-004); test
infrastructure — cited golden fixtures with repo audit, snapshots, time fakes, 54-environment screenshot
matrix (T-005). ADR-0001…0005.

**Tests by type**

| Type | Count |
|---|---|
| Unit (JVM / host, JUnit 6 + Kotest) | 101 |
| Robolectric | 3 |
| Lint detector (`LintDetectorTest`) | 17 |
| Konsist architecture | 33 |
| Screenshot references (Roborazzi) | 1 |
| Static gates | spotless, detekt, Android Lint + custom rules, Konsist, license gate, Kover |

**Coverage:** every module with code ≥ 99.4 % line (Kover); root merged gate (≥ 85 %) and `:core:*` gates
(≥ 95 % line / ≥ 90 % branch) pass. **Benchmarks:** none yet (startup Macrobenchmark skeleton; T-1801).
**License gate:** 437 external modules, 0 violations.

**Open risks**
1. ~~EPL-1.0 test-only exception for `junit:junit` (ADR-0005) awaits owner confirmation.~~ Confirmed 2026-09-13.
2. T-003 acceptance ("workflows green on main") requires pushing to GitHub; not yet pushed (push not approved as of 2026-09-13).
3. ~~Google Maven (dl.google.com) is unreachable from the development network without a proxy/VPN; new
   AndroidX/Google dependencies cannot be fetched locally until it is reachable.~~ Reachable again on 2026-09-13
   (HTTP 200); the license gate still reads POMs cache-first in case it becomes unreachable.
4. The development machine (14 GB) crashed from OOM during unbounded builds; builds are now budgeted and run
   inside a capped systemd scope (ADR-0004 §9). Full gates take ~2–3 min warm.
5. detekt 1.23 and Robolectric 4.17 need a JDK 21 toolchain (ADR-0004 §6, §8); Robolectric emulates API 36.
