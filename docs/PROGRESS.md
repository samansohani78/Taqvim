# Taqvim — Progress Tracker

Source of truth: [`docs/PLAN.md`](PLAN.md). Deviations: [`docs/adr/`](adr/).
Status legend: `TODO` · `WIP` · `DONE` · `BLOCKED` (see Notes).

| Task | Title | Status | Branch/Commit | Tests added | Notes |
|---|---|---|---|---|---|
| T-000 | Repository bootstrap | DONE | main@f34ecdb | R: MainActivityTest · U: AppModuleTest (Koin graph verify), TaqvimIssueRegistryTest · L: ProjectStructureKonsistTest; spotless/detekt/lint/Kover gates green | ADR-0001–0004. Tests & detekt on JDK 21 toolchain; build resource budget after local OOM crashes (ADR-0004 §9) |
| T-001 | License gate | DONE | main@d6c7ac3 | U: AllowListParserTest, SpdxNormalizerTest, PomLicensesTest (XXE, parent inheritance, cache lookup), LicensePolicyTest, ClassificationAndCodecTest · Negative: LGPL canary (`-Ptaqvim.licenseGate.canary=true`) rejected | 427 modules, 0 violations. ADR-0005: jakarta.annotation-api substitution, trove4j excluded, EPL-1.0 test-only exception confirmed by owner 2026-09-13; kxml2 MIT override. Google Maven unreachable from dev network → POMs read cache-first |
| T-002 | Architecture rules (Konsist) | DONE | main@3ea66ba | Konsist: ArchitectureKonsistTest (10 rules on real code), ArchitectureRulesTest (17 unit), KonsistFixturesTest (8 end-to-end, planted violations) | package↔module, layering (imports + build scripts), Android-free pure core, files ≤400 / functions ≤50 lines, ViewModel/UiState contract, no global mutable state. `!!`/`try`/`as` → detekt + T-004 lint; complexity ≤12 → detekt. Rule 10 (main@0b13c78): expression-bodied `@Test` must declare `: Unit` |
| T-003 | CI workflows | DONE (local) | main@8587d6b | L: actionlint (4 workflows + composite action) · local: CycloneDX SBOM (192 runtime components, no test deps), signed release APK verified by apksigner (v2), full gate green | pr.yml (static+SARIF, unit+Kover, Roborazzi verify, license gate + LGPL canary negative test, dataset, assemble, actionlint), instrumented.yml (API 26/30/33/36, KVM), benchmark.yml (nightly), release.yml (signed AAB/APK, git-cliff changelog, SBOM). **"Green on main" pending first push to GitHub** |
| T-004 | Custom lint module | DONE | main@c3fc147 | L: SourceRuleDetectorsTest (6), StateTextAndBackDetectorsTest (8), TaqvimIssueRegistryTest (3) — LintDetectorTest positive/negative per rule; Konsist: string-literal unit test | NoDoubleBang, NoTryCatch, UseRunCatching (quick fix), NoUnsafeCast, NoHardcodedNonLatinText, HardcodedComposeText (added: quality bar forbids hard-coded UI text), NoGlobalMutableState, PreferPredictiveBack (Kotlin + manifest); active in every module, full gate green |
| T-005 | Test infrastructure (`:core:testing`) | DONE | main@e10a0a2 | U: GoldenFilesTest, SnapshotAndTimeFakesTest, ScreenshotMatrixTest · R: ScreenshotEnvironmentRobolectricTest · S: ui_testing_self_test (1 reference) · Konsist: FixtureProvenanceKonsistTest | Golden fixtures with provenance header + repo audit, snapshots, FakeClock/FakeTimeZone, 54-environment screenshot matrix (phone/tablet/fold at mdpi), `:core:ui-testing` (ADR-0004 §2). Composables excluded from Kover (ADR-0004 §10) |
| T-100 | Value types | DONE | main@e94b47d | U: JdnTest (8), CalendarTypesTest (7) · P: jdn+n−n==jdn, weekday(jdn+7)==weekday(jdn), weekday+7, MinuteOfDay wrap | Overflow-checked Jdn + JdnRange, CalendarDate, ISO Weekday, validated Coordinates, integer MinuteOfDay; explicit API + KDoc 100%. **Correction (main@0b13c78):** its property tests were silently skipped by JUnit (expression-bodied `@Test` returning a value) until that fix; they now run and a Konsist rule prevents recurrence |
| T-101 | Gregorian ↔ JDN (A-01) | DONE | main@c266f45 | G: GregorianIcuOracleTest (1 000 000 random JDNs in [−1e6, 5e6] vs ICU4J proleptic) · U/P: GregorianCalendarSystemTest (1582-10-15, year 0, negative years, round-trip, successor) · U: PropertyTestingTest | Fliegel & Van Flandern + 400-year cycle shifting (exact for all Int years); CalendarArithmetic contract. **Correction (main@0b13c78):** its property tests were silently skipped by JUnit (expression-bodied `@Test` returning a value) until that fix; they now run and a Konsist rule prevents recurrence |
| T-102 | Persian calendar (A-02) | DONE (data partial) | main@9b1b7bf | G: PersianCalendarOfficialTest — official leap years 1206–1498 (293), every day of the official 1404/1405 calendars (730, incl. weekday), official Nowruz instants 1404/1405 → 1 Farvardin, Calendar Center century note · G: PersianCalendarAstronomyTest — table recomputed from equinoxes for SH −3000…3000, official table dates = equinox dates in Iran time, equinoxes within 1 min of official instants · G: PersianIcuOracleTest — ICU4J first day of every year 1200–1500 + 100 000 random days, no exceptions · U/P: PersianCalendarSystemTest (month lengths, invalid dates, boundaries, seam continuity, year-length and round-trip properties, 100 000 random days over JDN −1e6…5e6, B: 10⁶ conversions < 300 ms), BirashkArithmeticTest (4) | ADR-0008: generated leap table from the A-02 rule with the Birashk 2820-year fallback anchored at the table edges; astronomy library test-only. Sources: owner-supplied Calendar Center PDFs (docs/sources, main@d048e48). Official data covers 1206–1498 and the 1404/1405 instants; 1499–1500 and Nowruz instants 1390–1403/1406–1420 still pending (DT-001) |
| T-103 | Islamic tabular (A-03) & UAQ (A-04) | DONE | main@97c9da4 | G: TabularIslamicCalendarTest — type II vs ICU4J civil on 100 000 random days; UmmAlQuraCalendarTest — every month AH 1300–1600 and 100 000 random days (AH 1200–1700) vs ICU4J, table boundaries · P: round-trip for both tabular variants and UAQ across/beyond the table · U: epoch, leap cycles, month alternation, 10 631-day cycle, type I/II one-day difference, invalid input (15 tests) | ADR-0006: UAQ month masks embedded (obtained via ICU4J public API, Unicode-3.0 notice in `licenses/`); ICU4J remains test-only; civil tabular fallback outside AH 1300–1600 |
| T-104 | Islamic Iran official (A-05) + observational (A-06) | DONE (data partial) | main@28a2d16, main@ac90cf8 | G: IranIslamicCalendarTest — every lunar date of the official 1404/1405 calendars (730), 26 published month starts, table edges, 29/30-day months 1440–1455, 100 000 round-trips · U: HijriDateResolverTest — official > offset > estimate, 30-day expiry (FakeClock), ±2 bound · G: YallopTest — NAO TN 69 Table 4 (295 observations: q, classes, crescent width), real evenings around a new moon · G: ObservationalMonthStartsTest — Iran calibration 23/25 official month starts (≥ 90 %), Tehran single site never early, Makkah within ±1 day of Umm al-Qura (≥ 95 %) | ADR-0009 + addendum: aligned tabular estimate, user offset, A-06 calibration (any of 5 cities, class D) fitted to the 25 published months — re-validate with more official years (DT-002) |
| T-105 | Nepali (A-07) | TODO | | | |
| T-106 | Calendar utilities | WIP | main@5cbcf02, main@44f4346 | U: CalendarMathTest — 135 rows over Gregorian, tabular Islamic (incl. Dhul-Hijjah 29/30) and Umm al-Qura; PersianCalendarMathTest — 109 rows (29/30 Esfand from the official leap table, nth/last weekday and Saturday-start weeks on the official 1404/1405 calendars, clamping, seasons) · P: `addMonths(k)` then `−k` returns the month start (Gregorian, Islamic, Persian); y/m/d period components rebuild the target date | Calendar-agnostic over `CalendarArithmetic`; 244 table rows (≥ 200 met). Remaining for acceptance: Nepali 32-day months after T-105 (DT-003) |
| T-107 | `kotlinx.datetime` bridge | DONE | main@ba40f51 | U: DateTimeBridgeTest — LocalDate↔Jdn, out-of-range rejection, Asia/Tehran day boundaries before/after DST abolition (2022), Asia/Kabul fixed offset, Europe/Berlin & America/Los_Angeles DST, TodayProvider follows clock and zone · P: agrees with A-01 and round-trips; a zone shifts the civil day by ≤ 1 (8 tests) | `api(kotlinx-datetime 0.8.0)`; uses `kotlin.time.Clock`/`Instant` |
| T-200 | Language table | DONE | main@cdf1a66 (model: main@7e4e798) | U: LanguageTableTest (29: approved list, every language has every field ×24, exact data-gap set, numeral/direction rules, lookup & and-joining, snapshot `snapshots/language-table.json`), LanguageTableParserTest (4: missing/malformed entries, CLDR date-pattern parsing) · G: LanguageTableCldrOracleTest (24: native name, script, direction, week start, weekend, AM/PM, "and", date order/separator/padding, Gregorian/Persian/Islamic month names vs ICU4J 78.3) · U: PrayerConventionsTest (2) | ADR-0007 (languages + product defaults). CLDR 48 data in generated `languages.properties` (no non-Latin text in Kotlin). Gaps not guessed → DATA_TODO DT-004…DT-007 (ckb "and"; Persian months ckb/kmr/ne/id/ms/zh; Islamic months ckb/ne/zh; Bikram Sambat months) |
| T-201 | Numerals | DONE | main@b05c636 | U: NumeralsTest — `parse("۱۲٫۵") == 12.5`, separators and grouping per system (incl. Indian grouping), lenient mixed-digit parsing, traditional Tamil (18 rows + malformed input) · P: Long and BigDecimal round-trip through every system × grouping, traditional Tamil round-trip (35 tests) | Latin, Persian, Eastern Arabic, Devanagari, Tamil; CLDR separators; Tamil ௰/௱/௲ signs |
| T-202 | Date & duration formatting | DONE | main@05bf3e6 | U + oracle: PluralRulesTest (27), FormatTableTest (4), DateFormatterTest (47), RelativeTimeFormatterTest (48) — against ICU4J PluralRules (n 0–1000), full date formats (40 random dates per language × calendar), relative phrases (0–1000 past/future, day/week/month/year), units and lists · S: DateFormatSnapshotTest — 24 languages × 4 calendars × {long, numeric, iso} (288 rows) | CLDR 48 via ICU4J 78.3 → `formats.properties` (generator in scratchpad, re-checked by oracle tests); integer CLDR plural evaluator (TR35); ICU4J test-only. Gaps degrade instead of guessing: numeric month, no era, `null` relative/duration text (DT-008…DT-010); NEPALI uses the Gregorian pattern until T-105. fa/prs Persian LONG pattern overridden to `EEEE d MMMM y` (ADR-0014, main@51648bb); ps/ckb/ne root-fallback patterns in DT-025 |
| T-203 | Persian text normalization & fuzzy match | DONE | main@94d2947 | U: PersianTextTest — 50 normalization + 50 fuzzy pairs, search keys, OSA distance with early exit · P: normalize/searchKey idempotent, distance symmetric and zero iff equal (104 tests) | Restricted Damerau–Levenshtein (OSA) ≤ 2 on search keys; hamza carriers folded in search keys only |
| T-204 | String resources plan | DONE | main@1a4d047 | L: MissingFarsiTranslationDetectorTest (4: untranslated string/plurals/array reported, missing values-fa folder, complete fa clean, other locales may be incomplete), TaqvimIssueRegistryTest updated · Konsist: FarsiTranslationKonsistTest (repo fa completeness = 100 %, negative temp-tree case) | docs/i18n/STRINGS.md: en source, fa 100 % at every commit (stricter than "before v1"), naming/placeholder/plural conventions, Weblate component config (project to be created by owner), en-XA/ar-XB pseudo-locales in debug |
| T-300 | Rule engine | DONE | main@d7cf58e | U: RuleEvaluationTest — 108 cases, ≥ 10 per rule (Fixed incl. 29 Feb and 30 Esfand, NthWeekday, LastWeekday ± offset, LastDayOfMonth, Single, NthDayOfYear, RelativeToEvent across calendars, Astronomical with time zones) against java.time/ICU4J · G: OccurrenceGoldenTest — last Friday of Ramadan 1440–1450 (ICU4J civil), 3rd Thursday of November 2020–2035, day 256 2015–2030, last Tuesday of the Persian year 1395–1410 (ICU4J), official 1447 last Friday of Ramadan (60) · U: cycle/self/unknown/duplicate rejection, unavailable calendar, missing astronomy source, model invariants · P: occurrences sorted & unique and dated in the rule's calendar | `CalendarProvider.DEFAULT` = Persian, Iranian official Islamic, Gregorian (Nepali after T-105); `AstronomicalEventSource` implemented by T-403; validity/visibility left to T-302 |
| T-301 | Year cache & day lookup | DONE | main@6db021b | U: EventLookupTest (6) — Persian/Islamic/Gregorian merge and order, disabled source vs ALWAYS_DISPLAYED, offset crossing into the next year, cache hits/misses/eviction, unavailable calendar · B: 10 000 warm `eventsOn` < 50 ms (best of 5, 60 definitions) | Thread-safe LRU of 64 per (calendar, year, enabled sources); years y−1…y+1 consulted; one calculator per source so each source can use its own Islamic calendar |
| T-302 | Policies | DONE | main@5c701af | U: EventVisibilityPolicyTest — 192-row truth table (source × holiday-only × hide-abroad × abroad × validity none/in/out × holiday × ALWAYS_DISPLAYED) + 4 cases (non-religious abroad, validity calendar unavailable, variant per source, lookup integration) | Order: validity → ALWAYS_DISPLAYED → source enabled → holiday-only → religious-abroad; validity evaluated in its own calendar; ADR-0010 Islamic variant per source |
| T-303 | Holiday determination & workday basics | WIP | main@12b54fa | U: HolidayCalendarTest (3) — reasons, enabled sources only, weekend parameter and per-language CLDR weekends · G: OfficialHolidaysGoldenTest (`:data:events`) — generated official holidays equal the official 1404 and 1405 calendars in both directions | `holidayReasons`/`isHoliday`/`isWeekend`/`isWorkday`; weekend is a parameter (e.g. `forLanguage` from T-200 CLDR data), never guessed. Pending: official calendars 1403 and 1406 |
| T-304 | Search index | DONE | main@c635f90 | U: EventSearchIndexTest (9) — نوروز / نوريز / nowruz, ranking exact > prefix > substring > fuzzy, two-edit fuzzy, short queries, ties, filters, limits, best matching text per event · B: 10 000 mixed queries over 1 000 events ≈ 40 ms per run (< 200 ms) | Suffix table plus single-deletion fuzzy index over PersianText search keys (T-203); fuzzy only for queries ≥ 4 characters and while fewer than `limit` stronger hits exist |
| T-305 | Repository impl (`:data:events`) | DONE | main@c8b422e | U: EventsRepositoryTest (6) — Nowruz 1405 holiday, weekend and official Hijri date; Turbine re-emission on preference change, none for identical results; Hijri precedence official > offset > estimate, none for Umm al-Qura; personal events spanning days and RRULE-lite expansion in their own calendar; device/iCalendar day assignment in the collection zone; empty range rejected · U: EventsSettingsTest (2) · R: RoomEventInputsTest (3) — Room personal records with recurrence, iCalendar window, Koin module end to end (44 data:events tests; lines 173/174, branches 30/34) | `EventsRepository.days(range)`/`day(jdn)` combine the generated dataset (visibility + holidays per preferences) with personal, device and iCalendar events; dataset lookups rebuilt only on settings change. Enabled sources default to all except ANCIENT_IRAN (PLAN §5.1) until a preference exists (T-1500). No override table yet (D-07). Zone read at collection start; Hijri-offset expiry applies at the next emission. `eventsDataModule` not yet in `:app` |
| D-01 | Dataset JSON Schema + validator CLI | DONE | main@ad81460 | U: DatasetValidatorTest — valid sample covering all 8 rule types, 30 invalid fixtures each reporting exactly one issue (all 6 issue kinds: malformed JSON, schema, duplicate id, unknown/self reference, day out of range, invalid validity), cross-file ids/references, semantic checks skipped on schema errors, fuzz 10 000 random/mutated documents without a crash (35 tests) · DatasetCliTest — exit codes 0/1/2 | `dataset/events.v1.json` (draft 2020-12, rule `type` discriminator via if/then so one defect = one error) + `dataset/README.md`; `./gradlew :tools:dataset:validate`. Dependency: networknt json-schema-validator 3.0.7 (Apache-2.0, Jackson 3); license gate 452 modules, 0 violations. Day ranges use each calendar's longest month (exact per-year checks come with D-08 generation) |
| D-02 | Iran official holidays 1403–1406 | WIP | main@945d0c5 | G: IranOfficialHolidaysTest — the dataset's rules reproduce exactly the 26 official holiday dates of 1404 and of 1405 (golden date sets and the official_holiday column of the daily fixtures); validator clean | `dataset/iran/iran-official-holidays.json`: 27 records (10 Persian Fixed; 16 Islamic Fixed + Imam Reza martyrdom as LastDayOfMonth of Safar), official Persian titles with PDF text-layer artefacts corrected, citations with pages for both years, no English titles (no primary source). Pending: official calendars 1403 and 1406, reviewer sign-off (open title questions in the D-02 report) |
| D-03 | Afghanistan set | TODO | | | |
| D-04 | Nepal set | TODO | | | |
| D-05 | UN international days | WIP (93 of ≥ 150) | main@2648a53, main@5c281e0 | G: UnInternationalDaysTest (4) — validator clean, golden count 93 (89 Fixed, 4 NthWeekdayOfMonth), 14 rules spot-checked against the cited pages (golden CSV), every record cites the UN list and a Persian-title source · data/events goldens (NowruzWorkdays 30, OfficialHolidays 2, OfficialEvents 1) green on the regenerated sources | 93 of 237 UN days; 10 weeks excluded (schema has no duration). fa titles: UNIC Tehran list via the Internet Archive copy of 2012-10-05 in docs/sources (86), United Nations in Iran pages (7); nothing translated. Renamed/moved days keep UNIC's Persian name with the current UN date. 144 days without a primary Persian title (DT-019, DT-023, DT-024). Generated sources exempt from ktlint max line length (.editorconfig) |
| D-06 | Ancient Iranian festivals | TODO | | | |
| D-07 | Islamic Iran override table 1390–1410 | TODO | | | |
| D-08 | Dataset code generator (KotlinPoet) | DONE | main@49d1ead, main@fe060cc | S: EventsCodeGeneratorTest — snapshot of the synthetic sample (all 8 rule types, validity, flags, aliases, links), determinism/id order/file splitting, empty dataset · U: GenerateEventsCliTest — refuses invalid data and writes nothing, removes stale files, usage errors, committed output up to date · U: OfficialEventsTest — :data:events compiles, ids sorted and unique, citations and Persian titles, all 27 events evaluate in OccurrenceCalculator | `./gradlew :tools:dataset:generateEvents` → `data/events/.../generated/OfficialEvents*.kt` (8 events per file); file-level NoHardcodedNonLatinText/MaxLineLength suppression for dataset text; dataset-only fields (retrieved, updated, reviewedBy) not emitted |
| D-09 | Dataset CI + CONTRIBUTING-DATA.md | DONE (local) | main@5ff3a12 | L: actionlint (5 workflows) · local: `:tools:dataset:validate` (1 file, 0 issues); the existing D-01 fixture 13-missing-citation proves a record without a citation is rejected | pr.yml "Dataset validation" runs validate, then `:tools:dataset:test :data:events:test` (golden sets + stale-generated-code check); fixed the job's wrong task name (`validateDataset` → `validate`). Weekly `dataset-links.yml` resolves every citation/link URL (D-05). `CONTRIBUTING-DATA.md` (rules, workflow, reviewer sign-off checklist), PR-template dataset checklist. **"Green on GitHub" pending first push** |
| T-400 | Solar position (A-09) | DONE | main@e408019 | G: NoaaSolarCalculatorTest — all 240 rows of NOAA's Solar Calculations spreadsheet (declination, equation of time, solar noon, sunrise, sunset, zenith, refraction, apparent elevation, azimuth) to 1e-6 (plan ±0.01) · U: polar day/night, Sun overhead and poles stay finite · P: declination/equation-of-time continuity 1901–2099, morning azimuth increases (5 tests) | Transcribed from NOAA's public-domain spreadsheet (Meeus-based); branch-free floor modulo; `null` rise/set on polar days |
| T-401 | Prayer times (A-10) | DONE | main@e6b65cb | G: PrayerTimesOfficialTest — Institute of Geophysics official 1405 timetables, 31 Iranian cities × 365 days: Fajr, sunrise, Dhuhr, sunset, Maghrib within 1 min, midnight within 2 min (TEHRAN method) · U: PrayerTimesCalculatorTest — polar day/night → Unavailable, high-latitude rules (none, ½, ⅐, angle-based, Geophysics white nights), Makkah Isha +90, Hanafi Asr, four midnight modes · P: ordered times for every method at non-polar latitudes (37 tests) | Plan's six-city golden: Tehran and Mashhad covered (plus 29 more Iranian cities); Kabul, Istanbul, Berlin, Sydney need published timetables (DT-011) |
| T-402 | Qibla & great-circle (A-11) | DONE | main@ce4d495 | U/oracle: GreatCircleTest (13) — 10 sample cities, bearing ±1e-6° and distance ±1 m against an independent vector formulation; antipode, coincident points and poles → null; random-pairs property | Spherical model with IUGG mean radius; Kaaba 21.4225°N 39.8262°E (PLAN §6). Deviation: no primary Qibla table was available, so G became an oracle comparison; computed Tehran bearing is 218.4° (the plan's “≈217.6°” was not reproduced) |
| T-403 | Astronomy façade (A-13) | WIP | main@2facefb | G: EclipsesTest — NASA GSFC catalogs 2024–2030, all 16 solar + 16 lunar eclipses (type, peak ±5 min), local total eclipse 2024-04-08 and partial 2029-01-14 at catalog greatest-eclipse points · G: official Iranian equinox instants 2025/2026 ±5 min · U: SkyTest — season order and year length, Moon quarter cycle, southern-hemisphere phase mirroring, rise/transit/set incl. polar night, position and libration bounds | cosinekitty/astronomy 2.1.19 internal (no library types in the API). Remaining: lunar tilt; equinox/solstice golden 2020–2040 (USNO unreachable, DT-012) |
| T-404 | Zodiac & moon-in-Scorpio | WIP | main@5ccdd5f | U: ZodiacTest (5) — tropical sign boundaries (0°, 30°, 210°–240°, negative and multi-turn angles), IAU constellation of bright stars far from boundaries (Sco, Leo, Vir, Tau, UMi), Moon in Scorpius 2026 (12–14 visits, midpoint inside, one minute outside each boundary outside), tropical Scorpio 2026 (13–14 intervals of 48–66 h), window validation and clipping | Tropical and IAU systems; hourly scan with minute boundaries. Remaining: golden "known dates" from a published list (DT-013) |
| T-405 | Houses, lots, ascendant (A-14) | WIP | main@7c1946f | U: HousesTest (4) — ascendant on the eastern horizon and midheaven culminating (property, 1950–2050, latitudes ±60°, all longitudes), Placidus cusps 11/12 at ⅓ and ⅔ of the diurnal semi-arc with zodiacal order and oppositions, polar-circle null, lots day/night formulas and Tehran day/night chart | Meeus angles, NOAA true obliquity, Placidus by semi-arc trisection. Remaining: published-chart golden (DT-018) |
| T-406 | Tithi, planetary hours, Chinese/animal year, year names | WIP | main@a332cf4 | U: PlanetaryHoursTest (3) — Chaldean order and weekday rulers for every weekday, unequal day/night hours, polar Unavailable · U: TithiTest (3) — 30 tithis per synodic month, tithi 15/30 near full/new moon, boundaries · U: AnimalYearTest (2) — 12-year mapping table anchored on 2020 = Rat | Tithi = (Moon − Sun) / 12° on a modern ephemeris (not the Surya Siddhanta computation); animal of the Chinese year beginning in a Gregorian year. Pending: panchang reference times (DT-014), Hijri-Persian animal year names (DT-015), lunar new-year dates (DT-016) |
| T-407 | Photography panel calculations (F-10) | DONE (reference check pending) | main@cdb2902 | U: PhotographyPanelTest (3) — morning/evening golden hour (apparent altitude −4°…6°) and blue hour (−6°…−4°) ordering and durations, moonrise/moonset, polar cases empty | Plan's ±3 min comparison with a published photographers' ephemeris pending (DT-017) |
| T-500 | NLP date parser (F-03) | DONE (LONG gaps) | main@f0c0af1 | G: 500-phrase synthetic fa/en corpus (`golden/nlp/date-phrases.tsv`; expected days from `:core:calendar`, never from the parser) · P: NUMERIC and ISO format → parse round trip for 24 languages × 3 calendars (60 dates each) · S: LONG round-trip snapshot `long-round-trip.tsv` (asserted for fa, en) (24 tests; lines 98.97%, branches 92.5%) | Explicit Persian/Islamic/Gregorian dates in any digit script, relative (fa/en), anchored "N days before/after <event>", ranges, ranked readings with confidence and span. `AnchorLookup.find(query, reference: Jdn)`; ranges return `end: ParsedEnd?`. Not supported: two-digit years, Nepali, spelled numbers > 10, relative words in the other 22 languages. LONG parse-back gaps: kmr Islamic/Gregorian and tg Persian/Islamic 0/60, ja Persian 0/60, ta Islamic 39/60 (digits in month names), fr Islamic 54/60, bn Islamic 55/60 |
| T-501 | Text date detector (F-04) | DONE (real-world corpus pending) | main@ecb16ea | G: TextSnippetCorpusTest — 200 synthetic everyday fa/en snippets (176 with dates: single, weekday-prefixed, yearless, numeric, Islamic/Gregorian month names, ranges; 24 number-heavy without dates: times, prices, phones, versions, IPs) yield exactly the expected spans and days · B: 100 KB of snippet text (date-dense worst case) scanned < 50 ms · U: TextDateDetectorTest (7) — range outranks inner dates, same-span alternatives, seed windows, relative/anchored only on request, confidence floor, ambiguous numeric order (34 nlp tests; lines 98.97%, branches 92.5%) | Seed-token scanner + T-500 grammar on ±24-token windows; overlaps resolved by score. Defaults: ABSOLUTE + RANGE, confidence ≥ 0.7. Parser change: the year plausibility check (≤ 150 years, distance penalty) now also applies to month-name dates. PLAN asks for 200 real-world snippets; synthetic snippets used until a reusable corpus exists (DT-022) |
| T-502 | ICS reader/writer (F-05) | DONE | main@31f5c52 | G: 30 synthetic fixture calendars (Google/Outlook-style exports: PRODID, VTIMEZONE, X- properties, Windows zone names, folding, escaping) · U: content lines (6), values (5), writer (2), robustness — malformed input yields errors with line numbers, never exceptions (4) · P: write→read round-trip · fuzz (49 tests) | RFC 5545 subset: VEVENT, DTSTART/DTEND (DATE, floating, UTC, TZID), RRULE (FREQ D/W/M/Y, INTERVAL, COUNT, UNTIL, BYDAY±n, BYMONTHDAY±), EXDATE, SUMMARY/DESCRIPTION, VALARM DISPLAY; 75-octet UTF-8-safe folding, CRLF. Unsupported (warnings): VTODO/VJOURNAL/VFREEBUSY, VTIMEZONE bodies (IANA ids used), sub-daily FREQ, BYMONTH/BYYEARDAY/BYWEEKNO/BYSETPOS/WKST, RDATE, DURATION, RECURRENCE-ID, attendees |
| T-503 | Recurrence engine | DONE | main@4464dbb | G: RecurrenceOracleTest — 5 RFC 5545 examples + 50 Gregorian cases against an independent day-by-day java.time oracle (52) · U: RecurrenceEngineTest — invalid-day policies SKIP / NEXT_DAY / LAST_DAY_OF_MONTH for 30 Esfand, 29 February, day 31 and Islamic 30 Dhu al-Hijjah (10) · P: instances strictly increasing in Gregorian, Persian and Islamic calendars | ADR-0011; lazy sequence bounded by COUNT/UNTIL/window, stops after 1 000 empty periods |
| T-504 | Workday engine (F-07) | DONE | main@d19884b | U: WorkdayCalculatorTest — 100 cases (50 addWorkdays, 50 workdaysBetween) against an independent java.time loop, next/previous/zero semantics, profile options (sources, half-day policy, personal leave), no-workday profile → typed failure · P: addWorkdays lands on a workday (104 tests) · G: NowruzWorkdaysGoldenTest (`:data:events`) — first 14 days of Farvardin 1404/1405 and Farvardin workday counts with the generated official holidays (30) | Built on HolidayCalendar (T-303); weekend is a user parameter; workdaysBetween half-open, next/previous strict, addWorkdays(d, 0) → d or next workday; NoWorkday after 3 660 non-working days |
| T-600 | Proto DataStore | DONE | main@83f9182 | U: UserPreferencesMappingTest — defaults for all 24 languages, proto ↔ model mapping, table-driven migrations · R: UserPreferencesRepositoryTest — DataStore read/write (33 tests; line coverage 91/92) | `user_prefs.proto` + serializer + typed store. Build-logic: protobuf Gradle plugin on the convention classpath; generated `ir.taqvim.data.preferences.proto.*` excluded from Kover. Default Islamic variant per language is provisional (ADR-0012) |
| T-601 | Room schema & DAOs | DONE | main@c2e5b80 | R: PersonalDaoTest, WorkAndCacheDaoTest — in-memory DAO CRUD with Turbine flows · R: TaqvimDatabaseMigrationTest — MigrationTestHelper validates every exported schema up to the latest · U: DatabaseConvertersTest (14 tests; line coverage 147/151) | The 11 §4.3 tables, 7 DAOs, converters and mappers to core `RecurrenceRule`/`WorkdayProfile`, 2 000-row diagnostics ring, schema v1 JSON in `data/database/schemas`. Migration chain is 1→1 until v2 (ADR-0012). License gate: 506 modules, 0 violations |
| T-602 | Device calendar adapter | DONE | main@ca9a397 | U: DeviceEventMappingTest (6) — all-day event keeps its date in UTC+3:30, UTC−8, Asia/Tehran, America/Los_Angeles; timed events in the device zone; property: every all-day date in every quarter-hour offset −12…+14 kept and inside its window; window widening/day overlap; invisible/deleted rows dropped, colors opaque · R: CalendarInstancesSourceTest (4) — provider fixtures (all-day, multi-day, deleted, invisible calendar), no permission → provider not queried, SecurityException/crash/null cursor → typed results, ContentObserver register/unregister · R: DeviceCalendarRepositoryTest (4) — provider → cache → events, refresh on change, permission loss → empty and cache cleared, unavailable provider keeps cache · R: WorkAndCacheDaoTest +1 (lines 141/142, branches 39/40) | Uses the T-601 `device_events_cache`; new `DeviceEventDao.replaceOverlapping`/`deleteOverlapping` so instances deleted in the provider leave the cache. Fixtures via a synthetic Robolectric-registered ContentProvider (`ShadowContentResolver` has no calendar data), so DELETED/VISIBLE filtering is in the adapter. Device zone read at collection start (zone change needs re-collection). Not yet wired into `:app` |
| T-603 | Location data | DONE (districts pending) | main@c2e1528 | U: CityTableParserTest (3) — any column order, empty localized column = English spelling, prs→fa fallback, bad rows rejected with line number · U: CityCatalogTest (4) — bundled Natural Earth list (7342 places, unique ids, Tehran fields from the source), search ignoring case/Latin diacritics/Arabic letter variants, prefix-then-population ranking, nearest across the antimeridian · R: CityCollationTest (5) — ICU collation en, de, tr, fa, ar, each differing from code-point order · R: DeviceLocatorTest (5) — no permission, disabled providers, timeout removes the request, first fix wins, cancellation · R: PlatformGeocoderTest (5) (22 tests; lines 176/184, branches 97/110) | Natural Earth 5.2.0-pre populated places (public domain), names in 14 of 24 languages (DT-020); `LocationManager` only, no Play services; reverse geocoding only. Iran country divisions pending (DT-021). Not yet wired into `:app` |
| T-604 | Scheduler | DONE | main@4fbcf1e | U: AlarmPoliciesTest (5) — skip if fired > 15 min late (exactly 15 min is on time), dedup, reconcile, restore drops late alarms and duplicates · U: ReschedulePolicyTest (3) — reschedule matrix for boot, app update, clock, time zone, exact-alarm permission and preference changes · U: AlarmSchedulerTest (6), RescheduleCoordinatorTest (7) — idempotent replace, moved alarms, inexact fallback with banner state, reboot restore · R: SystemAlarmClockTest (2) — ShadowAlarmManager set/replace/cancel, exact vs inexact · R: SchedulerIntegrationTest (2) — Koin + Room + AlarmManager + receivers end to end (25 tests; lines 199/201) | Dedup key (kind, sourceId, triggerAt), no schema change. Exact alarms when `canScheduleExactAlarms()`, else `setAndAllowWhileIdle` + `exactAlarmStatus`. Features supply alarms per kind via `AlarmSource`. Boot/update: restore + recompute; clock/zone: recompute; permission: restore; prayer prefs → PRAYER, Islamic variant/Hijri offset → REMINDER. Open: Hijri-offset 30-day expiry triggers no reschedule; `schedulerModule` not yet in `:app`. Wired in `:app` (main@189deba): `schedulerModule`, application starts `PreferenceChangeWatcher`. Remaining: no `AlarmSource`/`AlarmDelivery` bound yet (T-1001/T-1101) |
| T-605 | Backup/restore (F-11) | DONE | main@d594bea | U: BackupCryptoTest (4) — PBKDF2-HMAC-SHA256 and SP 800-108 KDF against independently computed vectors (ASCII and UTF-8 passphrases), AES-GCM rejects wrong key/nonce/AAD/ciphertext, 51-byte header round trip · U: BackupCodecTest (9) — plaintext and encrypted round trips with preview of every table; fresh salt/nonce; wrong/missing/empty passphrase → typed errors; property: 40 random Unicode passphrases; tampered tag, ciphertext, nonce, salt, check value, algorithm or iterations → Corrupted/WrongPassphrase; truncated → Truncated/Corrupted; newer header/JSON version → UnsupportedFormat; garbage/foreign JSON → NotABackup; unknown fields ignored; dangling references, duplicate keys, bad enums → InvalidContent · R: BackupServiceTest (3) — export → change → restore brings back 7 tables and preferences (Room + DataStore); plaintext readable without passphrase; failing restore rolls back the Room transaction and leaves preferences untouched (31 database tests) | In `:data:database` (Konsist forbids feature→data; PLAN §3.1 has no backup module); depends on `:data:preferences`. JSON document (`format`, `formatVersion`, `appVersion`) or `TAQVIMBK` header + AES-256-GCM; PBKDF2-HMAC-SHA256 600 000 iterations, 128-bit salt, 96-bit nonce, header as AAD. Deviations: PBKDF2 only (no Argon2 in platform JCA); Room restore atomic, preferences written after it; caches, diagnostics and scheduled alarms not backed up (scheduler must reconcile after restore). Not wired into `:app`/`:feature:backup` (T-1503 needs a core port). Wired in `:app` (main@189deba, main@e39889d): `BackupService` bound; backups carry the event source link (schema 3) and the chosen place. Remaining: UI and core port (T-1503) |
| T-700 | Theme | DONE | main@99eac40 | U: SchemeContrastTest (2) — WCAG 2.2 contrast of 71 text/background role pairs ≥ 4.5:1 (standard) and ≥ 7:1 (high contrast) for random, default and extreme seeds in light/dark/black · U: ColorMathTest (5), SchemeGeneratorTest (4), ThemeSettingsTest (6), CustomFontsTest (2) · R: TaqvimThemeRobolectricTest (2), CustomFontsRobolectricTest (1) · S: ThemeScreenshotTest — 6 themes × LTR/RTL = 12 references (34 tests) | Own CIE LCh tonal palettes with fixed L* tones per role (not Material HCT), so contrast does not depend on the seed. Dynamic color + high contrast → scheme generated from the dynamic primary. Custom font from a file after an sfnt header check (file picker in T-1500); gradient and background-image veil (no contrast guarantee over images). No new dependencies |
| T-701 | Components | DONE | main@22d40c7 | U: ComponentGeometryTest (11) — wheel index/progress arithmetic, date-selection day clamping (29/30 Esfand), picker and month-grid model validation, gap-free column edges (property), row height, week-number slots, day-tone precedence, moon terminator, sun position and arc in LTR/RTL, ring sweep · R: ComponentSemanticsTest (8) — day cell spoken summary, selected state, click and labelled long-press; chips; tabs; top bar heading and action labels; empty state and tooltip; moon image description, sun arc summary, ring range info; screen surface padding · R: PickerAndGridTest (6) — wheel as one adjustable control, picker day clamping across years/months, bottom sheet, grid actions, RTL mirroring, grid measures each child once per pass · S: ComponentScreenshotTest — 13 galleries × 4 environments (light LTR 1.0, dark LTR 1.3, light RTL 2.0, dark RTL 1.0) = 52 references (111 core:ui tests; component package lines 237/237, branches 76/80) | TopBar, ScreenSurface, SegmentedTabs, NumberWheel, DatePickerSheet (calendar-agnostic), EventChip, DayCell, MonthGrid (custom single-pass Layout), MoonDisc, SunArc, ProgressRing, EmptyState, TooltipCard; all texts from callers. Deviations: 4 environments per component cover each variant value instead of the full product; black theme and large screens not captured; font scale 2.0 can clip secondary labels in fixed-size cells (T-1701); DayCell recomposition test left to T-1802 |
| T-702 | Painters for widgets | TODO | | | |
| T-703 | Motion & shared elements | TODO | | | |
| T-800 | CalendarViewModel & use cases | DONE | main@720443c | U: CalendarViewModelTest (12, Turbine) — load today in every calendar with month and events; select day → month and events; month paging keeps the selection; today resets selection and follows the day change; explicit selection survives midnight; preferences change primary calendar, week start and pager offset; day events re-emit, tabs switch; debounced search runs once for the latest query; blank/closed search never searches; result opens its next day; effects (editor, event, snackbar for undated result); paging ignored before today is known · U: CalendarCalendarsTest (6) — unavailable/duplicate calendars, Islamic variant arithmetic, month offsets across years, today ticks only on day change, search trims and limits · U (app): CalendarAdaptersTest (2), TimesAdaptersTest (4) · Koin verify (feature:calendar 18 tests, lines 197/203, branches 56/60; app 8 tests) | Immutable CalendarUiState, sealed CalendarAction, CalendarEffect channel. Feature depends only on core via ports (settings, day events, search, today); `:app` adapts T-600/T-305/T-304 and wires eventsDataModule, calendarFeatureModule, timesFeatureModule (TimesSettingsSource emits null until a chosen city exists, T-1502). Search covers the official dataset only (personal events in T-804); next date searched this year and next. `schedulerModule`, location, ICS and backup modules not yet wired. Nepali skipped until T-105 |
| T-801 | Month pager | DONE (benchmark not run) | main@41c189d | UI: CalendarScreenTest (5, Robolectric) — swipe 12 months forward and 24 back shows each month's title; tap selects a day; long press opens the editor on that day; week number opens the timeline at the week's first day; loading indicator · S: MonthScreenshotTest — 12 months of 1405 in fa (light RTL) and en (light LTR), 12 Gregorian months from April 2026 in ne (dark LTR; Bikram Sambat unavailable until T-105) = 36 references · U: MonthPageBuilderTest (6) — Farvardin 1405 grid, selection, secondary days, heading; every month of 1405; holidays/weekends, ≤ 3 dots, spoken summaries; week numbers across the year boundary; English heading spanning two Persian months · U: MonthLayoutTest (2) — grids start on the week start for every week start and ±24 months · U: CalendarMonthsTest (4, Turbine) — shown month ±1 loaded as six weeks, window moves with paging, event changes re-emit, settings, week-number effect · B: MonthPagerScrollBenchmark (24 months, FrameTimingMetric) written, not run (needs a device) (71 feature:calendar tests; lines 392/400, branches 121/131; `:app` tests incl. Koin verify green in a clean worktree) | `HorizontalPager` over ±1 200 months, ±1 page composed ahead; page models built on `Dispatchers.Default`; events of the shown month ±1 via the new `CalendarMonthSource` port (adapted in `:app` over `EventsRepository.days`); selection cross-fade 150 ms. Week numbers count from the first day of the primary calendar's year (not ISO; PLAN undefined). Shift labels empty until shift data is wired; T-802 day details and T-803 toolbar not yet on the screen |
| T-802 | Day details tabs | DONE | main@55ea31e | U: DayDetailsCalculatorTest (8) — Nowruz 1405: 3 days from 27 Esfand, day 1 of week/year, spring day 1 of 93, Sun in Aries; negative distances; southern seasons swapped (Sydney); new moon at the 2026-08-12 solar eclipse, full moon at the 2026-08-28 lunar eclipse; Tehran 10:30 next time Dhuhr; polar day without times; string resource for every enum value · U: DayDetailsViewModelTest (4, Turbine) — tabs follow selection, place, clock and day; source tooltip open/close/clear; citation → OpenUrl; minute ticks · UI: DayDetailsScreenTest (9, Robolectric) — each tab; every chip described with its source; official event shows its citation and opens the source; empty day offers "Add event"; next time announced as selected; no place; loading · S: DayDetailsScreenshotTest — 3 tabs × light/dark × en LTR/fa RTL = 12 references (104 feature:calendar tests; lines 744/765, branches 190/213; `:app` 10 tests green) | Calendars: day in every calendar, distance (days + y/m/d when ≥ 1 month), week and season rings (Persian-month seasons), Sun/Moon tropical signs and Moon phase at the place's local noon. Events: chips; official → inline source tooltip with first citation; others → OpenEvent. Times: place and method, SunArc, 8 times, next primary time within the selected day. New ports `CalendarPlaceSource` (null until T-1502) and `NowSource`. Opening device events needs the app navigation handler (not built yet) |
| T-803 | Toolbar & menu | DONE (app wiring pending) | main@a84ee78 | U: CalendarMenuTest (5, Turbine) — menu and dialogs; go to date selects the clamped primary-calendar day; week numbers and secondary calendar stored and followed; failed store → "not saved"; search, shift work, planetary hours and print effects · U: MonthPrintDocumentTest (3) — Persian month prints RTL with 7 headers, every cell, only the month's events with holiday style and escaping; empty English month · UI: CalendarToolbarTest (6, Robolectric) — today, search, shift work, planetary hours, print month, week-number toggle, secondary calendar dialog, go-to-date picker (131 feature:calendar tests; lines 930/958, branches 272/305) | Title = shown month with its span in the other calendars; today/search/more. Menu: go to date (±100 years), shift work (effect only — no shift feature yet), print month (HTML → PrintManager), planetary hours (effect to astronomy), week numbers and secondary calendar via the new `CalendarDisplayStore` port (secondary = reorder of `calendars`; week-number preference field pending in T-1500). `:app` must bind `CalendarDisplayStore` (AppModuleTest fails until bound; requested) |
| T-806 | Adaptive layout (F-13) | DONE (tablet emulator run pending) | main@a84ee78 | U: CalendarLayoutTest (4) — stacked below 600 dp, two panes from 600 dp, tabletop at any width; picker range and month names · UI: CalendarAdaptiveTest (3, Robolectric) — side by side on a 1280×800 tablet configuration, stacked on a phone, selection kept while resizing 400 → 900 → 400 dp · S: CalendarAdaptiveScreenshotTest — phone/fold/tablet × en light LTR / fa dark RTL = 6 references | Material 600 dp breakpoint; two independently scrolling panes; tabletop posture (month above the fold) from material3-adaptive 1.3.0. PLAN "UI on tablet emulator" covered by a Robolectric tablet configuration; emulator run pending (instrumented CI) |
| T-804 | Search screen (F-12) | DONE (app wiring pending) | main@de0aacf | UI: SearchScreenTest (4, Robolectric) — typing "نور" through the route shows Nowruz and opens it (1 Farvardin 1405); grouped headings, go-to-date, filters, clear; recent/idle/searching/no-results states · U/P: SearchMatcherTest (6) — ي/ی, ك/ک, ZWNJ, spaces, case and Persian/Arabic digits match; typos only for queries ≥ 4 characters; highlight ranges; property: any substring matches, variant spellings share a key · U/P: SearchRankerTest (5) — "نور" ranks Nowruz first; group order; synonyms find settings/tools; duplicates keep the best match; property: results independent of input order, unique keys, ≤ limit · U: SearchDatesTest (5) — fa/en written dates offered as a day, words/ranges/long text not offered, recent store · U: SearchViewModelTest (5, Turbine) — debounced typing gives one search, filters, failing event source still finds tools/settings, open result records the query, language change · S: SearchScreenshotTest — results light/dark × en LTR/fa RTL, fa recent, en no results (31 tests; lines 368/376, branches 148/160) | Events, tools and settings in one grouped list; own `SearchMatcher` on T-203 keys (ranking exact > prefix > word prefix > substring > typo ≤ 1–2 edits; holidays then title as tie-breaks) because core `EventSearchIndex` ranks events only; filters All/Events/Tools/Settings (PLAN names no other filters); "go to date" when the query is mostly one date (T-500, confidence ≥ 0.7); 250 ms debounce; recent queries in a session store until a preference exists (T-1500). Ports SearchSettingsSource/SearchEventSource/RecentQueriesStore; wiring requested from the navigation-shell work |
| T-805 | Year view | DONE (navigation pending; macrobenchmark not written) | main@6a47034 | U: YearViewModelTest (7, Turbine) — today's year in the first calendar with day flags; next/previous/chosen/today years (limit 3000); calendar switch keeps the same days; zoom 1–4 per row; holiday changes re-emit; settings changes re-resolve calendars; month opens through an effect · U: YearCalendarsTest (4) — available calendars (Nepali skipped), 1405 = Nowruz 2026 to the day before Nowruz 1406, month starts in all three calendars, year limits and Islamic variant · U: YearPageBuilderTest (6) — every month of 1405 starts on its weekday, today/holiday/weekend tones and summaries, headings with the other calendars' years, numbered months where names are missing, year page built under one 60 Hz frame (best of 10) · UI: YearScreenTest (6, Robolectric) — tap month → calendar at its first day; next/previous/today; swipe and tabs switch calendar; year selection; pinch zoom and accessibility zoom actions; loading · S: YearScreenshotTest — 1405 fa RTL light/dark, 2026 en LTR light/dark, en year selection = 5 references (28 tests; lines 331/337, branches 112/121) | One page per available calendar showing its year of the same anchor day; year controls + 1–3000 selection grid; discrete pinch zoom 1–4 mini months per row (also accessibility actions); canvas mini months with cached text layouts, one button each; page models on `Dispatchers.Default`. Own ports (Konsist forbids feature→feature, so calendar selection duplicates `CalendarCalendars`). Neighbouring calendar pages show only the language weekend until selected. PLAN "B" covered only by a unit timing test. Wired in `:app` (main@189deba). Remaining: navigation (T-803) |
| T-806 | Adaptive layout (F-13) | TODO | | | |
| T-900 | Timeline layout | DONE (app wiring pending) | main@912a2b6 | U/P: IntervalColoringTest (5) — 33-case table (disjoint, touching, overlapping, nested, chained, day edges) + overlaps never share a column, cluster width = deepest overlap, input-order independence · U/P: TimelineGeometryTest (7) — zoom 0.5–2×, 15-min snapping, px↔minute round trip, drag box whole steps and direction-independent, move keeps length, resize keeps start · U: TimelineContentBuilderTest (6) — weeks from the week start (property), all-day/timed split, Tehran prayer lines in order, polar day none · U: TimelineViewModelTest (7, Turbine) — week of today with placed events, navigation, zoom limits, draft move/resize/confirm/cancel → CreateEvent, place and event changes, OpenEvent effect · UI: TimelineScreenTest (6, Robolectric) — long-press drag creates 09:00–10:30, resize handle and accessibility actions, arrow/Shift/Enter/Escape keys, open event, modes and navigation, zoom actions and pinch, loading · S: TimelineScreenshotTest — week and day × fa RTL light / en LTR dark = 4 references (35 tests; lines 434/453, branches 157/183) | Greedy interval coloring by start in a custom single-measure Layout; all-day row; now line; dashed prayer lines at the chosen place; pinch and accessibility zoom; 15-min drag box with move/resize handles and keyboard/a11y; draft bar replaces the day controls. Bug found by tests and fixed: drag distances measured in root coordinates so snapping no longer lags the finger. Events crossing midnight must be split per day by the provider; week view is exactly seven days. Ports TimelineSettingsSource/TimelineDaysSource/TimelinePlaceSource/TimelineClockSource; wiring requested from the navigation-shell work |
| T-901 | Month list & agenda | DONE (navigation pending) | main@551aa61 | UI: AgendaScreenTest (6, Robolectric) — scroll 100 rows forward and back, both list ends request more months; mode, today, print, share, event and day actions; today selected, holiday and empty-month rows; loading; route + ViewModel navigation; share intent · P: AgendaWindowTest (4) — any load sequence keeps the window contiguous, ≤ 24 months, within ±1 200 and anchored at the end it grew · U/P: AgendaListBuilderTest (5) — Mordad–Aban 1405 month list (Persian names, digits, fa LONG date, 122 days); English agenda (event days + today, empty months, headers spanning Persian months); holiday/weekend; property: unique keys, day order, one header per month, today present iff its month is loaded (fa/en/ar) · U: AgendaExportTest (3) — shared text, printable HTML with direction/escaping · U: AgendaViewModelTest (6, Turbine) · S: AgendaScreenshotTest — agenda light/dark × en LTR/fa RTL, fa month list, loading (30 tests; lines 342/364, branches 125/142) | Lazy list anchored at today with month headers in the primary calendar and spans in the others; agenda (event days + today) or all days; months load 3 at a time at either end, ≤ 24 loaded, a century either way; rows built on `Dispatchers.Default`; print (HTML → PrintManager) and share (plain text). Month headers scroll (not sticky). Nepali falls back to Gregorian until T-105. Ports `AgendaSettingsSource`/`AgendaDaySource`; app wiring pending. Wired in `:app` (main@189deba). Remaining: navigation (T-803) |
| T-1000 | Events editor | DONE (navigation pending) | main@34cc9a9 | U: EventValidatorTest (8) — blank titles (property), title/notes/link limits, end before start (property), interval/count in Latin/Persian/Eastern Arabic digits (property), until before start, reminder count and range · U: EditorFormTest (4) — new event is all-day today in the first calendar; stored events round-trip through the form (property); rule parts the editor can't change are kept · U: EditorReducerTest (7) — calendar change keeps the same days incl. repeat-until (property); start move keeps length; repeat on/edit/off; reminders sorted and distinct; typed dates and ranges (T-500) · U: EditorPresenterTest (6) — errors only after save, change tracking, fa dates/times/digits, weekdays from the week start, reminder texts, picker month names · U: EventEditorViewModelTest (8, Turbine) — open new/stored/missing/unsupported/failing, validation then save, failed save retried, delete and discard, typed date, settings change relocalizes · R: EventEditorScreenTest (11) — create/edit/delete through the route, validation errors, discard confirmation, typed date, schedule, repeat, invalid-day policy, pickers, reminders, colors, loading/not-found/failure states · S: EventEditorScreenshotTest — new and detailed × light/dark × en LTR/fa RTL = 8 references (52 tests; lines 524/539, branches 261/293) | Title, calendar (Persian/Islamic/Gregorian; switching keeps the same days), date by picker or typed phrase, all-day/timed, RRULE-lite repeat in the event's calendar with the ADR-0011 invalid-day policy (ordinal BYDAY, BYMONTHDAY and week start kept but not editable), ≤ 5 reminders, 8-color palette, notes, source link. Product limits: title 200, notes 4 000, interval ≤ 999, count ≤ 9 999, reminders ≤ 4 weeks. 24-hour time wheels. Ports `PersonalEventStore`/`EditorSettingsSource`; app wiring pending — `personal_events` has no source-link column (needs schema 3). Nepali events open as not found until T-105. Wired in `:app` (main@14c322a schema 3 `source_link`, main@189deba `RoomPersonalEventStore` + editor settings + official anchor lookup). Remaining: navigation (T-803) |
| T-1001 | Reminder notifications | TODO | | | |
| T-1002 | Official-event reminders | TODO | | | |
| T-1003 | ICS import/export & subscriptions | WIP (UI pending) | main@2a4a56b | R: IcsRoomTest (4) — fixture import into Room (3 created, repeated UID skipped, reader problem reported), duplicates skipped or replaced by UID, unreadable text stores nothing, export → import replaces without duplicates · R: SubscriptionRefresherTest (5) — changed feed replaces cache and stores ETag/Last-Modified, 304 keeps cache, validators dropped after 7 days, failures keep cache, bad URLs not fetched, only due enabled feeds refreshed · R: SubscriptionRefreshWorkTest (2) — periodic work needs a connected network, cancelled when network not allowed, retry only after transient failures · R: IcsDocumentsTest (2) · U: HttpIcsFetcherTest (2, local JDK HTTP server) — 200 with validators, conditional 304, 404, size limit (declared and chunked), read timeout, refused connection, invalid URLs · U: SubscriptionPolicyTest (5), IcsEventMappingTest (6), IcsExportMappingTest (5, incl. round trip and Persian NEXT_DAY rule as RDATE + X-TAQVIM-RECURRENCE), IcsOccurrenceExpanderTest (5, DST, EXDATE/RDATE, UNTIL, window clipping), TaqvimRecurrenceTest (3) · U: IcsRecurrenceDatesAndExtensionsTest (4) · migration test 1→2 (267 tests across core:ics, data:database, data:events, Konsist; licenseCheck green) | ADR-0013: RRULE only for Gregorian SKIP rules with Monday week start, otherwise RDATE list + `X-TAQVIM-RECURRENCE` (3 653 days / 1 000 dates). Room schema 2 (`personal_events.ics_uid`, subscription `last_modified`/`last_checked_at_epoch_millis`). HTTPS only (webcal → https); 5 MiB / 15 s / 30 s limits; WorkManager periodic refresh (Apache-2.0, already in catalog). Pending: subscription UI + its test and the network-allowed preference (T-1500); `icsDataModule` and worker factory not in `:app`; EXDATE/RECURRENCE-ID not stored for personal events, overrides ignored in subscriptions. Wired in `:app` (main@189deba): `icsDataModule`, WorkManager configured with the worker factory. Remaining: nothing calls `SubscriptionRefreshScheduler.update()` yet; network-allowed preference and subscription UI (T-1500) |
| T-1100 | Times tab & report | DONE (navigation pending) | main@ea91bd3 | U: MonthlyReportTest (5) — one row per day for every month of Persian 1400–1410, Gregorian 2024–2026 and Umm al-Qura 1445–1447; HTML with `dir`/`lang`, Persian digits, escaping, polar rows · U: PrayerScheduleTest (7) — zone offset per day (Tehran, Oslo DST), display order, next time and progress (960 sampled instants, across midnight), polar day, localized HH:mm · U: TimesStateMapperTest (7) — no place, countdown and sun progress, expanded list, other days, after the last time, polar day, countdown without duration patterns · U: TimesViewModelTest (3, Turbine) — minute ticks, expand, day navigation, settings changes, report · R: TimesScreenTest (7) — next row announced as selected, countdown and sun path descriptions, actions, polar/missing-place/loading states · S: TimesScreenshotTest — today light/dark × en LTR/fa RTL, expanded, polar, no place (36 tests; lines 255/261, branches 75/87) | Expand/collapse, next-time countdown refreshed each minute, sun arc, monthly report as self-contained HTML printed through an off-screen WebView + `PrintManager` ("Save as PDF" from the system dialog; no PDF generator). Reads a `TimesSettingsSource` port. Countdown falls back to `H:MM` in local digits where duration patterns are missing (DT-009). fa LONG title fixed by ADR-0014 (main@51648bb, 3 screenshots re-recorded). Wired in `:app` (main@e39889d): settings from the stored place (T-1502). Remaining: navigation (T-803) |
| T-1101 | Athan settings | DONE (app wiring pending) | main@df5f30f, main@39f5e17 | U: AthanPreferencesMappingTest (5) — defaults identical for all 24 languages (every athan off); random settings round-trip through proto and serializer (property); stores before T-1101 read as defaults; stored oddities repaired (unknown/duplicate prayers, gaps clamped to ±60, volume 0–100, blank sound = default) · R: UserPreferencesRepositoryTest +1 — athan settings persist across DataStore instances · U: ReschedulePolicyTest — athan switch, vibration, sound, Iran time and chosen place changes recompute PRAYER alarms · U: AthanSettingsViewModelTest (5, Turbine) — Persian digits; exact-alarm warning only while an athan is on and exact alarms are denied; switches and gaps stored within ±60; picked sound adopted, unusable file flagged; volume stored once (debounced); preview play/stop/end/failure · UI: AthanSettingsScreenTest (6, Robolectric) — each control persists through the route; sound pick via document picker; exact-alarm banner opens ACTION_REQUEST_SCHEDULE_EXACT_ALARM; bypass disabled without Fajr · S: AthanSettingsScreenshotTest — light/dark × en LTR/fa RTL, fa with exact-alarm banner (45 data:preferences, 25 data:scheduler, 37 feature:settings tests; `:app` 30 tests green in a clean worktree) | Per-prayer switch and gap (−60…60 min); sound via SAF OpenDocument with persisted read access and preview (full playback T-1102); volume 0–100 %; vibration; DND bypass for Fajr only (stored; enforced in T-1102); "Iran time" UTC+03:30 toggle (stored; used by T-1102). UserPrefs field 14 (optional, no schema bump). Ports AthanSettingsStore/ExactAlarmAccess/AthanSoundLibrary/AthanPreview. Method/Asr/high-latitude pickers left to T-1500. T-604 reschedule matrix extended (place and athan changes → PRAYER). No bundled athan recording (DT-028). App wiring and backup field requested from the navigation-shell work |
| T-1102 | Athan playback | DONE (app wiring pending; OEM checklist not written) | main@c5c1492 | P: AthanPlannerTest (5) — for random instants in 2026, gaps −60…60 and any set of enabled prayers, athans = A-10 times + gaps, strictly after now, ordered, one per prayer and day; after Isha the next is tomorrow's Fajr; a −60 min Fajr gap before midnight is planned; polar day (Tromsø) none; Iran time = UTC+03:30 · U/P: AthanPoliciesTest (3) — any claim sequence sounds each prayer at most once per day; bounded history; silent/vibrate/DND matrix with Fajr bypass only with policy access · U: AthanAlarmsTest (2) — alarm instants from the plan; a fired alarm plays once and calls the T-1103 hook; nothing without a place or on off-plan instants · R: AthanServiceTest (5) — foreground notification (silent channel, Stop and Snooze), MediaPlayer started and stopped; stops after 5 minutes and when the sound ends; silent mode mutes except allowed Fajr bypass; snooze re-arms 10 minutes later · R: AthanSessionTest (4) — picked sound at its volume with vibration, unreadable URI → default alarm sound, audio focus loss and end of sound interrupt · R: AthanPlatformTest (4) (23 tests; lines 311/326, branches 152/191) | Foreground service (mediaPlayback) with MediaPlayer on USAGE_ALARM; audio focus loss (e.g. a call) stops it; vibration; stop/snooze (10 min); silent mode and DND respected unless the Fajr bypass is on with notification policy access; ≤ 5 minutes; persistent once-per-prayer-per-day log; 2-day planning window replanned at each alarm. Heads-up (not full-screen) notification; no screen-off stop (PLAN does not ask). If Android refuses a background foreground start, the notification is posted without playback. No bundled recording (DT-028). Ports AthanSetupSource/AthanPlaybackStarter/AthanEventHook (T-1103 no-op); PRAYER AlarmSource/AlarmDelivery wiring and runtime POST_NOTIFICATIONS / policy-access prompts pending |
| T-1103 | Automation broadcasts (F-14) | TODO | | | |
| T-1200 | Widget framework | TODO | | | |
| T-1201 | Widget: 1×1 date | TODO | | | |
| T-1202 | Widget: 4×1 date + clock | TODO | | | |
| T-1203 | Widget: 2×2 date + events + next prayer | TODO | | | |
| T-1204 | Widget: 4×2 with prayer strip | TODO | | | |
| T-1205 | Widget: Month interactive | TODO | | | |
| T-1206 | Widget: Month bitmap | TODO | | | |
| T-1207 | Widget: Week strip | TODO | | | |
| T-1208 | Widget: Schedule list | TODO | | | |
| T-1209 | Widget: Sun arc | TODO | | | |
| T-1210 | Widget: Moon | TODO | | | |
| T-1211 | Widget: Map | TODO | | | |
| T-1212 | Widget: Countdown/age (F-09) | TODO | | | |
| T-1213 | Persistent notification | TODO | | | |
| T-1214 | Dynamic launcher icon | TODO | | | |
| T-1215 | QS tile, shortcuts, live wallpaper, daydream | TODO | | | |
| T-1300 | Astronomy screen | DONE (navigation pending) | main@e4c1222 | U: AstronomyHeaderCacheTest (4) — Moon and signs once per hour, seasons and eclipses once per UTC day, separate places, LRU eviction; next season, total solar eclipse 2026-08-12 and partial lunar eclipse 2026-08-28 (NASA GSFC catalogs) · U: AstronomyStateMapperTest (6) — summer noon in Tehran (daylight, subsolar latitude, next season on both sides of the 2026 June solstice, Sun progress, picker date 1405-03-31), full moon 2026-08-28, Persian digits, polar day, phase-name sectors, sign positions · U: AstronomyDialogsTest (4) — horoscope with 12 cusps, angles and lots, polar null; year horoscope at the March equinox; Sunday planetary hours start with the Sun; tropical Moon in Scorpio 13–14 periods in 1405 · U: AstronomyViewModelTest (3, Turbine) · R: AstronomyScreenTest (10) — modes, header eclipses, actions, slider, dialogs, polar houses, loading and missing-place states · S: AstronomyScreenshotTest — Moon light/dark × en LTR/fa RTL, Earth light LTR, Sun dark RTL, no place (34 tests; lines 518/548, branches 99/129) | Earth/Moon/Sun modes; instant follows the clock until moved (±day, ±year in the settings' calendar, minute slider, date picker); animated Moon phase and Sun progress; dialogs: horoscope (Placidus cusps, angles, lots; no planets beyond Sun/Moon in `:core:astronomy`), year horoscope at the March equinox, planetary hours, Moon in Scorpio. Moon constellation shown as IAU abbreviation (DT-026). Reads an `AstronomySettingsSource` port; `astronomyFeatureModule` not yet in `:app`. Wired in `:app` (main@189deba, main@e39889d): settings from the stored place. Remaining: navigation (T-803) |
| T-1301 | Map | TODO | | | |
| T-1302 | Compass | DONE (navigation pending) | main@8a40fcf | U: AnglesTest (5) — wrap and signed-turn properties, filter across north, circular mean, 15° announcements with hysteresis (property) · U: RotationMathTest (7) — identity, azimuth about z (property), pitch/roll, headings from gravity + field, rotation-vector and gravity+field paths agree for tilted devices (property), degenerate readings · U: CompassStateMapperTest (6) — Tehran noon (Qibla, Sun, Moon, 24 h path), true north with declination, Qibla turn and alignment, magnetic without a place, Persian digits · U: CompassViewModelTest (4, Turbine) · R: PlatformSensorsTest (6) — ShadowSensorManager injection (rotation vector preferred, fallback, accuracy, unregister, gravity, missing sensors), WMM declination sign/size · R: CompassScreenTest (5) — TalkBack heading description, Qibla/Sun/Moon texts, calibration hint, orientation lock set and restored · S: CompassScreenshotTest — dial light/dark × en LTR/fa RTL, no place (51 compass tests; lines 500/576, branches 197/258) | Rotation vector with accelerometer + magnetometer fallback; circular low-pass; true north via platform `GeomagneticField` (WMM, no bundled coefficients); Qibla arrow with the turn to take; Sun/Moon azimuth ring (no planets: `:core:astronomy` has Sun/Moon only); animated 24 h Sun path; TalkBack live region every 15° with 3° hysteresis; natural-orientation lock while shown. App wiring pending. Wired in `:app` (main@189deba, main@e39889d): settings from the stored place. Remaining: navigation (T-803) |
| T-1303 | Bubble level | DONE (navigation pending) | main@8a40fcf | U: LevelMathTest (5) — classification (scale-invariant property), flat/portrait/landscape tilt (property, face-down/upside-down), calibration per orientation, tolerance, ruler ticks (cm/in), localized decimals · U: LevelViewModelTest (3) — calibration persists across ViewModels, reset, free fall keeps last orientation · R: LevelScreenTest (2) — calibration persists across screens, portrait reading, ruler tab · S: flat light LTR, upright dark RTL, ruler | Orientation from the dominant gravity axis (PLAN "landing" read as lying flat); two-axis bubble flat, one-axis upright; per-orientation offsets through a `LevelCalibrationStore` port (needs a UserPrefs field); cm/in ruler from `DisplayMetrics.xdpi`; scrolls on small phones. Wired in `:app` (main@189deba): calibration in memory only (`SessionLevelCalibrationStore`). Remaining: persist in UserPrefs (T-1500), navigation (T-803) |
| T-1400 | Converter, distance, duration, time zones, QR | DONE (navigation pending) | main@6179966 | U: DurationExpressionTest (3) — 106-expression table: 78 values incl. Persian units/digits, decimals, nesting, ±100 000 days; 28 errors with positions · P: DurationPropertiesTest (3) — fuzz of grammar-alphabet text never throws and stays within limits; generated sums evaluate exactly; presenter parts/totals/digits · U/P: DateToolsTest (5) — written/relative dates in every calendar; distance days/weeks/Persian period/workdays (151 in 1405/1/1→6/22 with a Friday weekend); numeric dates convert back (fa, en) · U: TimeZoneBoardTest (3) — Tehran/Kabul/Los Angeles/Kiritimati offsets, day shifts, Persian digits, search · U: QrEncoderTest (3) — ASCII/Persian/link codes decode back with ZXing's reader, empty, too long · U: ToolsViewModelTest (3, Turbine) · R: ToolsScreenTest (11) — each tool, errors, loading, FileProvider share intent, PNG quiet zone · S: ToolsScreenshotTest — converter light/dark × en LTR/fa RTL, distance, duration, zones, QR (39 tests; lines 535/551, branches 265/289; licenseCheck 539 modules, 0 violations) | Own duration grammar (w d h m s in English and Persian). Converter and distance on the T-500 parser; workdays via a WorkdayCalculator port (≤ 3 660 days). Time-zone board with platform zone names, session-only zone list (persistence in T-1500). QR via ZXing core 3.5.4 (Apache-2.0), shared as PNG through FileProvider. Not done: Nepali (T-105), animal-year compatibility (DT-027). Known cosmetic issues: English tab labels ellipsized at phone width; RTL shows "25 + 1" as "۱ + ۲۵" (needs bidi isolation, T-1701). `toolsFeatureModule` not yet in `:app`. Wired in `:app` (main@189deba): workdays from the default profile. Remaining: stored board zones (T-1500), navigation (T-803) |
| T-1500 | Settings screens | TODO | | | |
| T-1501 | Language & first-run | TODO | | | |
| T-1502 | Location settings | DONE (navigation pending) | main@4d7cae6, main@5980f0f | U: ChosenPlaceMappingTest (5) — city/device/coordinates places round-trip through proto and serializer (property); unusable stored values (unknown source, out-of-range or NaN coordinates, blank/unknown zone) read as no place; validation · R: UserPreferencesRepositoryTest +1 — place persists across DataStore instances · U/P: CoordinateInputTest (4) — displayed coordinates parse back in every digit script (property); ranges and malformed text; Arabic decimal separator; zone ids · U: LocationSettingsViewModelTest (7, Turbine) — stored place in the language's digits; debounced search keeps port ranking; choosing a city stores it; device fix stored with described name and zone; denied/disabled/timed-out/unavailable store nothing; typed coordinates validated and stored · R: LocationSettingsScreenTest (7) — city, device and coordinates paths persist through the route; permission-denied explanation; errors; loading · S: LocationSettingsScreenshotTest — 4 references (39 data:preferences tests, lines 91/92, branches 34/36; 22 feature:settings tests; `:app` 10/10 in a clean worktree) | Place stored in UserPrefs field 13 (optional message, no schema bump). Feature depends only on core via ports LocationSettingsStore, CitySearch, DeviceLocation, PlaceDescriber, TimeZoneIds; location permission requested only on "Use my location". Coordinates and zone ids forced LTR; Arabic comma with Arabic-script digits. Not done: districts (DT-021), map pick (T-1301); backup does not yet carry the place. App wiring pending (replaces `ChosenCitySource` for Times/Astronomy/Compass). Wired in `:app` (main@e39889d): store, city search, device fix, place describer; the stored place feeds Times, Astronomy, Compass and Calendar (replaces `ChosenCitySource`). Remaining: navigation (T-803) |
| T-1503 | Backup/restore UI & privacy dashboard | TODO | | | |
| T-1504 | About, licenses, diagnostics & report | TODO | | | |
| T-1600 | Wear app | TODO | | | |
| T-1700 | TalkBack pass | TODO | | | |
| T-1701 | RTL & font scale | TODO | | | |
| T-1702 | Translations | TODO | | | |
| T-1800 | Baseline/startup profiles, R8, shrinking | TODO | | | |
| T-1801 | Macrobenchmarks | TODO | | | |
| T-1802 | Compose stability | TODO | | | |
| T-1803 | Memory & leaks | TODO | | | |
| T-1804 | Security | TODO | | | |
| T-1900 | Release engineering | TODO | | | |
| T-1901 | Support system | TODO | | | |
| T-1902 | Beta program | TODO | | | |

## Epic summaries

### Epic 0 — Foundation & Delivery Pipeline (completed 2026-09-13)

**Shipped:** ~45-module Gradle skeleton with build-logic conventions and latest-stable catalog (T-000);
dependency license gate with scope-aware allow-list, cache-first POM resolution and LGPL canary negative test
(T-001); Konsist architecture suite (T-002); PR / instrumented / benchmark / release workflows with signed
builds, changelog and CycloneDX SBOM (T-003); 8 custom lint rules active in every module (T-004); test
infrastructure — cited golden fixtures with repo audit, snapshots, time fakes, 54-environment screenshot
matrix (T-005). ADR-0001…0005.

**Tests by type**

| Type | Count |
|---|---|
| Unit (JVM / host, JUnit 6 + Kotest) | 101 |
| Robolectric | 3 |
| Lint detector (`LintDetectorTest`) | 17 |
| Konsist architecture | 33 |
| Screenshot references (Roborazzi) | 1 |
| Static gates | spotless, detekt, Android Lint + custom rules, Konsist, license gate, Kover |

**Coverage:** every module with code ≥ 99.4 % line (Kover); root merged gate (≥ 85 %) and `:core:*` gates
(≥ 95 % line / ≥ 90 % branch) pass. **Benchmarks:** none yet (startup Macrobenchmark skeleton; T-1801).
**License gate:** 437 external modules, 0 violations.

**Open risks**
1. ~~EPL-1.0 test-only exception for `junit:junit` (ADR-0005) awaits owner confirmation.~~ Confirmed 2026-09-13.
2. T-003 acceptance ("workflows green on main") requires pushing to GitHub; not yet pushed (push not approved as of 2026-09-13).
3. ~~Google Maven (dl.google.com) is unreachable from the development network without a proxy/VPN; new
   AndroidX/Google dependencies cannot be fetched locally until it is reachable.~~ Reachable again on 2026-09-13
   (HTTP 200); the license gate still reads POMs cache-first in case it becomes unreachable.
4. The development machine (14 GB) crashed from OOM during unbounded builds; builds are now budgeted and run
   inside a capped systemd scope (ADR-0004 §9). Full gates take ~2–3 min warm.
5. detekt 1.23 and Robolectric 4.17 need a JDK 21 toolchain (ADR-0004 §6, §8); Robolectric emulates API 36.
