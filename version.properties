# Local default version of the Taqvim app (docs/RELEASE.md, T-1900).
# Release builds take the version from the tag instead: ./gradlew -Ptaqvim.version=vX.Y.Z[-beta.N] ...
# versionCode is computed from it: MAJOR × 1 000 000 + MINOR × 10 000 + PATCH × 100 + (N for -beta.N, else 99).
version=0.1.0
