org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true
# build-logic compiles with Gradle's embedded Kotlin, which would otherwise start a second Kotlin daemon
# next to the project's KGP 2.4 daemon (~700 MB). Convention plugins rarely change: compile in-process.
kotlin.compiler.execution.strategy=in-process
