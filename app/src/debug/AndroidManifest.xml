<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only manifest additions (T-1803, ADR-0021). LeakCanary keeps its heap dumps in app-private storage, so its
     legacy external-storage permissions are not needed and stay out of the app (ADR-0017 permission set). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:node="remove" />
</manifest>
